{
  "section": "Глава III. Формулы и двойственность",
  "icon": "🔀",
  "questions": [
    {
      "id": 1,
      "title": "Булева формула в базисе",
      "type": "Определение",
      "formalText": "**Булева формула** в базисе $B$ — выражение $F$, составленное из переменных $X_n = \\{x_1, \\ldots, x_n\\}$ и функций из $B$ по следующим правилам:\n\n1. Переменная $x_i \\in X_n$ является формулой.\n2. Если $f(x_1, \\ldots, x_k) \\in B$ и $F_1, \\ldots, F_k$ — формулы, то выражение $f(F_1, F_2, \\ldots, F_k)$ также является формулой.\n3. Других формул нет.",
      "insight": "Формула — как рецепт: берёшь ингредиенты (переменные) и применяешь операции (функции базиса), комбинируя результаты. Определение индуктивное (рекурсивное): из простых формул строятся сложные подстановкой в базисные функции.",
      "example": {
        "title": "📝 Пример",
        "text": "Базис $B = \\{\\lor, \\land, \\lnot\\}$, переменные $x_1, x_2, x_3$.\n\n$F_1 = x_1 \\lor x_2$ — формула (правило 2: дизъюнкция).\n$F_2 = \\overline{x_3}$ — формула (правило 2: инверсия).\n$F_3 = F_1 \\land F_2 = (x_1 \\lor x_2) \\land \\overline{x_3}$ — формула."
      },
      "note": "Три правила: (1) переменная, (2) подстановка формул в базисную функцию, (3) больше ничего. Это индуктивное определение."
    },
    {
      "id": 2,
      "title": "Подформула и внешняя функция",
      "type": "Определение",
      "formalText": "Если формула $F = f(F_1, F_2, \\ldots, F_k)$, то:\n\n- Формулы $F_1, \\ldots, F_k$ называются **подформулами** формулы $F$.\n- Функция $f$ называется **внешней функцией** формулы $F$.\n- Любая подформула каждой $F_i$ также является подформулой $F$ (транзитивность).",
      "insight": "Если формула — это дерево, то подформулы — поддеревья, а внешняя функция — корень. Как в арифметике: в $(2+3) \\times 5$ подформулы — это $(2+3)$ и $5$, а внешняя функция — умножение. Подформула — рекурсивное понятие: подформулы подформул тоже подформулы.",
      "note": "Назови подформулы и внешнюю функцию. Подчеркни транзитивность: подформулы подформул тоже являются подформулами исходной формулы."
    },
    {
      "id": 3,
      "title": "Двойственная функция",
      "type": "Определение",
      "formalText": "Для булевой функции $f(x_1, x_2, \\ldots, x_n)$ **двойственной** называется функция:\n\n$$f^*(x_1, x_2, \\ldots, x_n) = \\overline{f(\\bar{x}_1, \\bar{x}_2, \\ldots, \\bar{x}_n)}.$$\n\nИначе: инвертируем все аргументы, вычисляем $f$, инвертируем результат.\n\n**Построение по таблице:** столбец $f^*$ получается из столбца $f$ переворотом и инверсией каждого значения.\n\n**Свойства:**\n1. $(f^*)^* = f$ (инволютивность)\n2. $(x \\lor y)^* = x \\land y$ (конъюнкция и дизъюнкция взаимно двойственны)\n3. $(\\bar{x})^* = \\bar{x}$ (инверсия двойственна сама себе)",
      "insight": "Двойственная функция — «зеркальное отражение»: инвертируем входы, вычисляем $f$, инвертируем выход. Столбец $f^*$ = перевёрнутый и инвертированный столбец $f$. Двойственность инволютивна: $(f^*)^* = f$.",
      "example": {
        "title": "📝 Пример",
        "text": "$f(x, y, z) = (x \\lor \\bar{y}) \\land z$.\n\n$f^*(x, y, z) = \\overline{f(\\bar{x}, \\bar{y}, \\bar{z})} = \\overline{(\\bar{x} \\lor y) \\land \\bar{z}}$\n\nПо де Моргану: $= (x \\land \\bar{y}) \\lor z$."
      },
      "note": "Формула: $f^* = \\overline{f(\\bar{x}_1, \\ldots, \\bar{x}_n)}$. Упомяни инволютивность и что $\\lor$ и $\\land$ взаимно двойственны."
    }
  ]
}
