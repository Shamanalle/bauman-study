{
  "num": 9,
  "title": "Итераторы",
  "summary": "Понятие итератора. Типы итераторов: Input, Forward, Bidirectional, Random Access, Contiguous, Output. Операции для каждого типа. Написание своего итератора: using-определения (iterator_category, value_type, difference_type, pointer, reference). Реализация операторов ==, !=, *, ->, ++, --, [], +, -, +=, -=, <, >, <=, >=. Привязка итератора к контейнеру (begin/end). Работа со стандартными алгоритмами.",
  "sections": [
    {
      "heading": "Что такое итератор?",
      "blocks": [
        {
          "type": "definition",
          "term": "Итератор",
          "content": "<strong>Итератор</strong> — тип данных, позволяющий <strong>перебирать элементы</strong> контейнера. Является обёрткой над указателем (в простейшем случае), но может быть и более сложным типом.<br><br>С итераторами вы сталкиваетесь каждый раз при использовании <strong>range-based for</strong>:<br><pre><code>for (auto& elem : container) { ... }</code></pre>"
        },
        {
          "type": "text",
          "content": "Итератор должен обеспечивать <strong>требуемый функционал</strong> (набор операций) в зависимости от своего типа."
        }
      ]
    },
    {
      "heading": "Типы (категории) итераторов",
      "blocks": [
        {
          "type": "text",
          "content": "Итераторы образуют <strong>иерархию</strong> — каждый следующий добавляет новые операции:"
        },
        {
          "type": "table",
          "headers": [
            "Итератор",
            "Операции"
          ],
          "rows": [
            [
              "Input iterator (один проход, одно направление)",
              "==, !=, ->, *, ++ (пре/пост)"
            ],
            [
              "Forward iterator (многопроходный, одно направление)",
              "Те же, без новых"
            ],
            [
              "Bidirectional iterator (оба направления)",
              "+ -- (пре/пост)"
            ],
            [
              "Random access iterator (произвольный доступ)",
              "+ [], +, -, +=, -=, <, >, <=, >="
            ],
            [
              "Contiguous iterator (смежная память)",
              "Те же, без новых"
            ]
          ]
        },
        {
          "type": "text",
          "content": "<strong>Output iterator</strong> стоит особняком — отвечает за <strong>модификацию</strong> данных. Любой итератор чтения может одновременно быть Output iterator (тогда он <strong>изменяемый</strong>)."
        }
      ]
    },
    {
      "heading": "Создание своего итератора",
      "blocks": [
        {
          "type": "text",
          "content": "Для создания <strong>Random Access Output</strong> итератора (самый полный вариант) нужно определить <strong>19 операторов</strong> и 5 <code>using</code>-определений:"
        },
        {
          "type": "example",
          "content": "<pre><code>class StringIterator {\n    char* _pos;\npublic:\n    // Обязательные using-определения:\n    using iterator_category = std::random_access_iterator_tag;\n    using value_type = char;\n    using difference_type = int;\n    using pointer = char*;\n    using reference = char&;\n\n    StringIterator(char* pos) : _pos(pos) {}\n\n    // Операторы сравнения\n    bool operator==(StringIterator const& other) const { return _pos == other._pos; }\n    bool operator!=(StringIterator const& other) const { return _pos != other._pos; }\n\n    // Разыменование и доступ\n    reference operator*() const { return *_pos; }\n    pointer operator-&gt;() const  { return _pos; }\n\n    // Инкремент/декремент\n    StringIterator& operator++()    { ++_pos; return *this; }        // Префиксный\n    StringIterator  operator++(int) { auto tmp = *this; ++_pos; return tmp; } // Постфиксный\n    StringIterator& operator--()    { --_pos; return *this; }\n    StringIterator  operator--(int) { auto tmp = *this; --_pos; return tmp; }\n\n    // Произвольный доступ\n    reference operator[](size_t index) const { return _pos[index]; }\n    StringIterator operator+(difference_type const& d) const { return {_pos + d}; }\n    StringIterator operator-(difference_type const& d) const { return {_pos - d}; }\n    difference_type operator-(StringIterator const& o) const {\n        return std::distance(o._pos, _pos);\n    }\n    StringIterator& operator+=(difference_type const& d) { _pos += d; return *this; }\n    StringIterator& operator-=(difference_type const& d) { _pos -= d; return *this; }\n\n    // Сравнения позиций\n    bool operator&gt;(StringIterator const& o)  const { return _pos &gt; o._pos; }\n    bool operator&lt;(StringIterator const& o)  const { return _pos &lt; o._pos; }\n    bool operator&gt;=(StringIterator const& o) const { return _pos &gt;= o._pos; }\n    bool operator&lt;=(StringIterator const& o) const { return _pos &lt;= o._pos; }\n\n    // Внешняя функция: сдвиг + итератор\n    friend StringIterator operator+(difference_type const& d, StringIterator const& it);\n};</code></pre>"
        },
        {
          "type": "note",
          "content": "Класс итератора <strong>не работает с ресурсами</strong>, поэтому по <strong>правилу нуля</strong> ему не нужны конструктор копирования, оператор присваивания и деструктор."
        }
      ]
    },
    {
      "heading": "Привязка итератора к контейнеру (begin/end)",
      "blocks": [
        {
          "type": "text",
          "content": "Для использования итератора с <code>range-based for</code> и стандартными алгоритмами нужно определить методы <code>begin()</code> и <code>end()</code> в классе контейнера:"
        },
        {
          "type": "example",
          "content": "<pre><code>class String {\n    char* _symbols;\n    size_t _len;\n    size_t _capacity;\npublic:\n    // ...\n    StringIterator begin() const { return StringIterator(&_symbols[0]); }\n    StringIterator end()   const { return StringIterator(&_symbols[_len]); }\n};</code></pre>"
        },
        {
          "type": "note",
          "content": "<code>end()</code> возвращает итератор на элемент <strong>после последнего</strong>. Область памяти, на которую он указывает, должна быть «легальной» (выделенной)."
        }
      ]
    },
    {
      "heading": "Категории итераторов",
      "blocks": [
        {
          "type": "definition",
          "term": "Категории итераторов STL",
          "content": "Итераторы делятся на 5 категорий по возможностям:<br><br>1. <strong>Input</strong> — только чтение, однопроходный (istream_iterator)<br>2. <strong>Output</strong> — только запись, однопроходный (ostream_iterator)<br>3. <strong>Forward</strong> — чтение/запись, многопроходный (forward_list)<br>4. <strong>Bidirectional</strong> — + движение назад (list, set, map)<br>5. <strong>RandomAccess</strong> — + произвольный доступ, арифметика (vector, deque, array)"
        },
        {
          "type": "text",
          "content": "<strong>Зачем нужны категории?</strong> Алгоритмы STL предъявляют <em>минимальные требования</em> к итераторам:<br>• <code>std::find</code> — InputIterator (любой контейнер)<br>• <code>std::reverse</code> — BidirectionalIterator (list, vector, но не forward_list)<br>• <code>std::sort</code> — RandomAccessIterator (vector, deque, но не list!)"
        },
        {
          "type": "example",
          "title": "Почему нельзя сортировать list через std::sort?",
          "content": "Объяснить, почему std::sort(lst.begin(), lst.end()) не компилируется.",
          "solution": "<code>std::list</code> предоставляет только <strong>BidirectionalIterator</strong>, а <code>std::sort</code> требует <strong>RandomAccessIterator</strong> для доступа по индексу (O(1)).<br><br>Решение: использовать метод контейнера <code>lst.sort()</code>, который реализует merge sort за O(n log n) без случайного доступа."
        }
      ]
    },
    {
      "heading": "Написание своего итератора",
      "blocks": [
        {
          "type": "text",
          "content": "Для написания итератора нужно определить 5 типов (через typedef или using) и операторы:"
        },
        {
          "type": "example",
          "title": "Минимальный forward iterator",
          "content": "Реализовать итератор для обхода связного списка.",
          "solution": "<pre><code>class ListIterator {\npublic:\n    using value_type = int;\n    using pointer = int*;\n    using reference = int&;\n    using difference_type = ptrdiff_t;\n    using iterator_category = std::forward_iterator_tag;\n\n    ListIterator(Node* p = nullptr) : ptr(p) {}\n\n    reference operator*() { return ptr->data; }\n    ListIterator& operator++() {\n        ptr = ptr->next;\n        return *this;\n    }\n    bool operator==(const ListIterator& o) const {\n        return ptr == o.ptr;\n    }\n    bool operator!=(const ListIterator& o) const {\n        return !(*this == o);\n    }\nprivate:\n    Node* ptr;\n};</code></pre>"
        }
      ]
    },
    {
      "heading": "Работа со стандартными алгоритмами",
      "blocks": [
        {
          "type": "text",
          "content": "После определения итератора и <code>begin()/end()</code> контейнер автоматически работает со <strong>стандартными алгоритмами STL</strong>:"
        },
        {
          "type": "example",
          "content": "<pre><code>#include &lt;algorithm&gt;\n\nString str(\"ABCDEFG 12345 asdfgh\");\n\n// for_each — вывод каждого символа\nstd::for_each(str.begin(), str.end(), [](auto i) { std::cout &lt;&lt; i; });\n\n// range-based for\nfor (auto i : str) std::cout &lt;&lt; i &lt;&lt; \"|\";\n\n// find — поиск символа\nauto pos = std::find(str.begin(), str.end(), 's') - str.begin();\n\n// count_if — подсчёт цифр\nint nums = std::count_if(str.begin(), str.end(),\n    [](auto i) { return i &gt;= '0' && i &lt;= '9'; });\n\n// sort — сортировка\nstd::sort(str.begin(), str.end());\n\n// max_element, min_element\nstd::cout &lt;&lt; *std::max_element(str.begin(), str.end());\nstd::cout &lt;&lt; *std::min_element(str.begin(), str.end());</code></pre>"
        }
      ]
    }
  ]
}