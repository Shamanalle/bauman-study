{
  "variant": 26,
  "lab": 6,
  "code": "#include <iostream>\n#include <string>\n#include <stdexcept>\n#include <cmath>\n\nusing namespace std;\n\nclass QuadException : public invalid_argument {\nprivate:\n    double err_a, err_b, err_c;\n    double err_discriminant;\n\npublic:\n    QuadException(const string& message, double a, double b, double c)\n        : invalid_argument(message), err_a(a), err_b(b), err_c(c),\n          err_discriminant(b*b - 4*a*c) {}\n\n    void printErrorData() const {\n        cout << \"[Детали исключения] Уравнение не имеет действительных корней:\" << endl;\n        cout << \"  a = \" << err_a << \", b = \" << err_b << \", c = \" << err_c << endl;\n        cout << \"  Дискриминант D = \" << err_discriminant << \" < 0\" << endl;\n    }\n};\n\nclass QuadEquation {\nprivate:\n    double a, b, c;\n    double x1, x2;\n\npublic:\n    QuadEquation(double pa, double pb, double pc) : a(pa), b(pb), c(pc) {\n        double D = b * b - 4 * a * c;\n        if (D < 0) {\n            throw QuadException(\n                \"Квадратное уравнение не имеет действительных корней (D < 0)!\",\n                a, b, c\n            );\n        }\n        x1 = (-b + sqrt(D)) / (2 * a);\n        x2 = (-b - sqrt(D)) / (2 * a);\n    }\n\n    void print() const {\n        cout << \"Уравнение: \" << a << \"x² + \" << b << \"x + \" << c << \" = 0\" << endl;\n        cout << \"  x1 = \" << x1 << \", x2 = \" << x2 << endl;\n    }\n};\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    cout << \"Демонстрация работы БЕЗ возникновения исключения:\" << endl;\n    try {\n        QuadEquation eq1(1, -3, 2);\n        cout << \"Объект успешно создан!\" << endl;\n        eq1.print();\n    }\n    catch (const QuadException& e) {\n        cerr << \"Перехвачено исключение: \" << e.what() << endl;\n        e.printErrorData();\n    }\n    catch (const exception& e) {\n        cerr << \"Стандартное исключение: \" << e.what() << endl;\n    }\n\n    cout << \"\\nДемонстрация работы С возникновением исключения:\" << endl;\n    try {\n        QuadEquation eq2(1, 2, 5);\n\n        cout << \"Этот текст не напечатается.\" << endl;\n        eq2.print();\n    }\n    catch (const QuadException& e) {\n        cerr << \"\\nПерехвачено ожидаемое исключение:\" << endl;\n        cerr << \"Сообщение: \" << e.what() << endl;\n        e.printErrorData();\n    }\n    catch (const invalid_argument& e) {\n        cerr << \"invalid_argument: \" << e.what() << endl;\n    }\n    catch (const exception& e) {\n        cerr << \"Неизвестная ошибка: \" << e.what() << endl;\n    }\n\n    cout << \"\\nПрограмма успешно завершила свою работу.\" << endl;\n    return 0;\n}",
  "codeHtml": "<pre><code>#include &lt;iostream&gt;\n#include &lt;string&gt;\n#include &lt;stdexcept&gt;\n#include &lt;cmath&gt;\n\nusing namespace std;\n\nclass QuadException : public invalid_argument {\nprivate:\n    double err_a, err_b, err_c;\n    double err_discriminant;\n\npublic:\n    QuadException(const string&amp; message, double a, double b, double c)\n        : invalid_argument(message), err_a(a), err_b(b), err_c(c),\n          err_discriminant(b*b - 4*a*c) {}\n\n    void printErrorData() const {\n        cout &lt;&lt; \"[Детали исключения] Уравнение не имеет действительных корней:\" &lt;&lt; endl;\n        cout &lt;&lt; \"  a = \" &lt;&lt; err_a &lt;&lt; \", b = \" &lt;&lt; err_b &lt;&lt; \", c = \" &lt;&lt; err_c &lt;&lt; endl;\n        cout &lt;&lt; \"  Дискриминант D = \" &lt;&lt; err_discriminant &lt;&lt; \" &lt; 0\" &lt;&lt; endl;\n    }\n};\n\nclass QuadEquation {\nprivate:\n    double a, b, c;\n    double x1, x2;\n\npublic:\n    QuadEquation(double pa, double pb, double pc) : a(pa), b(pb), c(pc) {\n        double D = b * b - 4 * a * c;\n        if (D &lt; 0) {\n            throw QuadException(\n                \"Квадратное уравнение не имеет действительных корней (D &lt; 0)!\",\n                a, b, c\n            );\n        }\n        x1 = (-b + sqrt(D)) / (2 * a);\n        x2 = (-b - sqrt(D)) / (2 * a);\n    }\n\n    void print() const {\n        cout &lt;&lt; \"Уравнение: \" &lt;&lt; a &lt;&lt; \"x² + \" &lt;&lt; b &lt;&lt; \"x + \" &lt;&lt; c &lt;&lt; \" = 0\" &lt;&lt; endl;\n        cout &lt;&lt; \"  x1 = \" &lt;&lt; x1 &lt;&lt; \", x2 = \" &lt;&lt; x2 &lt;&lt; endl;\n    }\n};\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    cout &lt;&lt; \"Демонстрация работы БЕЗ возникновения исключения:\" &lt;&lt; endl;\n    try {\n        QuadEquation eq1(1, -3, 2);\n        cout &lt;&lt; \"Объект успешно создан!\" &lt;&lt; endl;\n        eq1.print();\n    }\n    catch (const QuadException&amp; e) {\n        cerr &lt;&lt; \"Перехвачено исключение: \" &lt;&lt; e.what() &lt;&lt; endl;\n        e.printErrorData();\n    }\n    catch (const exception&amp; e) {\n        cerr &lt;&lt; \"Стандартное исключение: \" &lt;&lt; e.what() &lt;&lt; endl;\n    }\n\n    cout &lt;&lt; \"\\nДемонстрация работы С возникновением исключения:\" &lt;&lt; endl;\n    try {\n        QuadEquation eq2(1, 2, 5);\n\n        cout &lt;&lt; \"Этот текст не напечатается.\" &lt;&lt; endl;\n        eq2.print();\n    }\n    catch (const QuadException&amp; e) {\n        cerr &lt;&lt; \"\\nПерехвачено ожидаемое исключение:\" &lt;&lt; endl;\n        cerr &lt;&lt; \"Сообщение: \" &lt;&lt; e.what() &lt;&lt; endl;\n        e.printErrorData();\n    }\n    catch (const invalid_argument&amp; e) {\n        cerr &lt;&lt; \"invalid_argument: \" &lt;&lt; e.what() &lt;&lt; endl;\n    }\n    catch (const exception&amp; e) {\n        cerr &lt;&lt; \"Неизвестная ошибка: \" &lt;&lt; e.what() &lt;&lt; endl;\n    }\n\n    cout &lt;&lt; \"\\nПрограмма успешно завершила свою работу.\" &lt;&lt; endl;\n    return 0;\n}</code></pre>",
  "explanation": "<h2>Разбор кода: Лабораторная работа 6 (Вариант 26)</h2>\n<h2>Полный исходный код программы</h2>\n<pre><code>#include &lt;iostream&gt;\n#include &lt;string&gt;\n#include &lt;stdexcept&gt;\n#include &lt;cmath&gt;\n\nusing namespace std;\n\nclass QuadException : public invalid_argument {\nprivate:\n    double err_a, err_b, err_c;\n    double err_discriminant;\n\npublic:\n    QuadException(const string&amp; message, double a, double b, double c)\n        : invalid_argument(message), err_a(a), err_b(b), err_c(c),\n          err_discriminant(b*b - 4*a*c) {}\n\n    void printErrorData() const {\n        cout &lt;&lt; \"[Детали исключения] Уравнение не имеет действительных корней:\" &lt;&lt; endl;\n        cout &lt;&lt; \"  a = \" &lt;&lt; err_a &lt;&lt; \", b = \" &lt;&lt; err_b &lt;&lt; \", c = \" &lt;&lt; err_c &lt;&lt; endl;\n        cout &lt;&lt; \"  Дискриминант D = \" &lt;&lt; err_discriminant &lt;&lt; \" &lt; 0\" &lt;&lt; endl;\n    }\n};\n\nclass QuadEquation {\nprivate:\n    double a, b, c;\n    double x1, x2;\n\npublic:\n    QuadEquation(double pa, double pb, double pc) : a(pa), b(pb), c(pc) {\n        double D = b * b - 4 * a * c;\n        if (D &lt; 0) {\n            throw QuadException(\n                \"Квадратное уравнение не имеет действительных корней (D &lt; 0)!\",\n                a, b, c\n            );\n        }\n        x1 = (-b + sqrt(D)) / (2 * a);\n        x2 = (-b - sqrt(D)) / (2 * a);\n    }\n\n    void print() const {\n        cout &lt;&lt; \"Уравнение: \" &lt;&lt; a &lt;&lt; \"x² + \" &lt;&lt; b &lt;&lt; \"x + \" &lt;&lt; c &lt;&lt; \" = 0\" &lt;&lt; endl;\n        cout &lt;&lt; \"  x1 = \" &lt;&lt; x1 &lt;&lt; \", x2 = \" &lt;&lt; x2 &lt;&lt; endl;\n    }\n};\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    cout &lt;&lt; \"Демонстрация работы БЕЗ возникновения исключения:\" &lt;&lt; endl;\n    try {\n        QuadEquation eq1(1, -3, 2);\n        cout &lt;&lt; \"Объект успешно создан!\" &lt;&lt; endl;\n        eq1.print();\n    }\n    catch (const QuadException&amp; e) {\n        cerr &lt;&lt; \"Перехвачено исключение: \" &lt;&lt; e.what() &lt;&lt; endl;\n        e.printErrorData();\n    }\n    catch (const exception&amp; e) {\n        cerr &lt;&lt; \"Стандартное исключение: \" &lt;&lt; e.what() &lt;&lt; endl;\n    }\n\n    cout &lt;&lt; \"\\nДемонстрация работы С возникновением исключения:\" &lt;&lt; endl;\n    try {\n        QuadEquation eq2(1, 2, 5);\n\n        cout &lt;&lt; \"Этот текст не напечатается.\" &lt;&lt; endl;\n        eq2.print();\n    }\n    catch (const QuadException&amp; e) {\n        cerr &lt;&lt; \"\\nПерехвачено ожидаемое исключение:\" &lt;&lt; endl;\n        cerr &lt;&lt; \"Сообщение: \" &lt;&lt; e.what() &lt;&lt; endl;\n        e.printErrorData();\n    }\n    catch (const invalid_argument&amp; e) {\n        cerr &lt;&lt; \"invalid_argument: \" &lt;&lt; e.what() &lt;&lt; endl;\n    }\n    catch (const exception&amp; e) {\n        cerr &lt;&lt; \"Неизвестная ошибка: \" &lt;&lt; e.what() &lt;&lt; endl;\n    }\n\n    cout &lt;&lt; \"\\nПрограмма успешно завершила свою работу.\" &lt;&lt; endl;\n    return 0;\n}\n\n</code></pre>\n<hr>\n<h2>1. Свой класс Исключения</h2>\n<pre><code>class QuadException : public invalid_argument {{\nprivate:\n    double err_a, err_b, err_c;\n    double err_discriminant;\n</code></pre>\n<p>Наследник <code class=\"inline-code\">invalid_argument</code>. Хранит коэффициенты и дискриминант \"забракованного\" уравнения.</p>\n<pre><code>    QuadException(const string&amp; message, double a, double b, double c)\n        : invalid_argument(message), err_a(a), err_b(b), err_c(c),\n          err_discriminant(b*b - 4*a*c) {{}}\n</code></pre>\n<p>Дискриминант вычисляется прямо в конструкторе исключения — удобно для отладки.</p>\n<p>Наследуем от <code class=\"inline-code\">invalid_argument</code>. Базовые исключения C++ хранят только текстовое сообщение, а наш класс хранит ещё и специфичные числовые данные, которые помогают диагностировать, <strong>какие именно</strong> значения вызвали ошибку.</p>\n<p>Конструктор вызывает родительский <code class=\"inline-code\">invalid_argument(message)</code> — переданный текст будет доступен через метод <code class=\"inline-code\">e.what()</code>, стандартный для всех исключений C++.</p>\n<p>Метод <code class=\"inline-code\">printErrorData()</code> выводит «забракованные» данные — это требование задания. Благодаря ему при перехвате исключения мы видим не только текст ошибки, но и конкретные значения, которые её вызвали.</p>\n<h2>2. Класс QuadEquation и <code class=\"inline-code\">throw</code></h2>\n<pre><code>    QuadEquation(double pa, double pb, double pc) : a(pa), b(pb), c(pc) {{\n        double D = b * b - 4 * a * c;\n        if (D &lt; 0) {{\n            throw QuadException(\"D &lt; 0!\", a, b, c);\n        }}\n        x1 = (-b + sqrt(D)) / (2 * a);\n        x2 = (-b - sqrt(D)) / (2 * a);\n    }}\n</code></pre>\n<p>Конструктор вычисляет дискриминант D = b² − 4ac. Если D < 0 — действительных корней нет, <code class=\"inline-code\">throw</code>. Если D ≥ 0 — вычисляются корни по формуле.</p>\n<p>При нарушении условия — <strong><code class=\"inline-code\">throw</code></strong>: объект не досоздаётся, программа откатывается к ближайшему блоку <code class=\"inline-code\">catch</code>. Все локальные объекты, созданные до <code class=\"inline-code\">throw</code>, автоматически уничтожаются (стековая раскрутка / stack unwinding).</p>\n<p>Важно: вся ресурсоёмкая работа (выделение памяти, открытие файлов) делается <strong>после</strong> проверки. Если бы мы сначала выделили память, а потом обнаружили ошибку, пришлось бы вручную освобождать ресурсы перед <code class=\"inline-code\">throw</code>.</p>\n<h2>3. Блоки <code class=\"inline-code\">try - catch</code></h2>\n<pre><code>    try {{\n        QuadEquation eq2(1, 2, 5);  // D = 4 - 20 = -16 &lt; 0\n    }}\n    catch (const QuadException&amp; e) {{\n        e.printErrorData();  // покажет a=1, b=2, c=5, D=-16\n    }}\n</code></pre>\n<p>Ловим <code class=\"inline-code\">QuadException</code> первым, затем <code class=\"inline-code\">invalid_argument</code>, затем <code class=\"inline-code\">exception</code>.</p>\n<p><strong><code class=\"inline-code\">try</code></strong> — место, где мы говорим компилятору: «Наблюдай за этим куском кода!». При возникновении <code class=\"inline-code\">throw</code> все операции после него внутри <code class=\"inline-code\">try</code> <strong>не выполняются</strong> — управление мгновенно передаётся в подходящий блок <code class=\"inline-code\">catch</code>.</p>\n<p>Порядок блоков <code class=\"inline-code\">catch</code> критически важен: <strong>от самого специфичного к самому общему</strong>. Первым ловим <code class=\"inline-code\">QuadException</code> (наш пользовательский), затем его родителя <code class=\"inline-code\">invalid_argument</code>, и только потом общий <code class=\"inline-code\">exception</code>. Если поставить <code class=\"inline-code\">exception</code> первым — он перехватит <strong>все</strong> ошибки, и специфичные обработчики никогда не сработают (компилятор даже выдаст предупреждение).</p>",
  "explanationRaw": "# Разбор кода: Лабораторная работа 6 (Вариант 26)\n\n## Полный исходный код программы\n```cpp\n#include <iostream>\n#include <string>\n#include <stdexcept>\n#include <cmath>\n\nusing namespace std;\n\nclass QuadException : public invalid_argument {\nprivate:\n    double err_a, err_b, err_c;\n    double err_discriminant;\n\npublic:\n    QuadException(const string& message, double a, double b, double c)\n        : invalid_argument(message), err_a(a), err_b(b), err_c(c),\n          err_discriminant(b*b - 4*a*c) {}\n\n    void printErrorData() const {\n        cout << \"[Детали исключения] Уравнение не имеет действительных корней:\" << endl;\n        cout << \"  a = \" << err_a << \", b = \" << err_b << \", c = \" << err_c << endl;\n        cout << \"  Дискриминант D = \" << err_discriminant << \" < 0\" << endl;\n    }\n};\n\nclass QuadEquation {\nprivate:\n    double a, b, c;\n    double x1, x2;\n\npublic:\n    QuadEquation(double pa, double pb, double pc) : a(pa), b(pb), c(pc) {\n        double D = b * b - 4 * a * c;\n        if (D < 0) {\n            throw QuadException(\n                \"Квадратное уравнение не имеет действительных корней (D < 0)!\",\n                a, b, c\n            );\n        }\n        x1 = (-b + sqrt(D)) / (2 * a);\n        x2 = (-b - sqrt(D)) / (2 * a);\n    }\n\n    void print() const {\n        cout << \"Уравнение: \" << a << \"x² + \" << b << \"x + \" << c << \" = 0\" << endl;\n        cout << \"  x1 = \" << x1 << \", x2 = \" << x2 << endl;\n    }\n};\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    cout << \"Демонстрация работы БЕЗ возникновения исключения:\" << endl;\n    try {\n        QuadEquation eq1(1, -3, 2);\n        cout << \"Объект успешно создан!\" << endl;\n        eq1.print();\n    }\n    catch (const QuadException& e) {\n        cerr << \"Перехвачено исключение: \" << e.what() << endl;\n        e.printErrorData();\n    }\n    catch (const exception& e) {\n        cerr << \"Стандартное исключение: \" << e.what() << endl;\n    }\n\n    cout << \"\\nДемонстрация работы С возникновением исключения:\" << endl;\n    try {\n        QuadEquation eq2(1, 2, 5);\n\n        cout << \"Этот текст не напечатается.\" << endl;\n        eq2.print();\n    }\n    catch (const QuadException& e) {\n        cerr << \"\\nПерехвачено ожидаемое исключение:\" << endl;\n        cerr << \"Сообщение: \" << e.what() << endl;\n        e.printErrorData();\n    }\n    catch (const invalid_argument& e) {\n        cerr << \"invalid_argument: \" << e.what() << endl;\n    }\n    catch (const exception& e) {\n        cerr << \"Неизвестная ошибка: \" << e.what() << endl;\n    }\n\n    cout << \"\\nПрограмма успешно завершила свою работу.\" << endl;\n    return 0;\n}\n\n```\n\n---\n\n## 1. Свой класс Исключения\n```cpp\nclass QuadException : public invalid_argument {{\nprivate:\n    double err_a, err_b, err_c;\n    double err_discriminant;\n```\nНаследник `invalid_argument`. Хранит коэффициенты и дискриминант \"забракованного\" уравнения.\n\n```cpp\n    QuadException(const string& message, double a, double b, double c)\n        : invalid_argument(message), err_a(a), err_b(b), err_c(c),\n          err_discriminant(b*b - 4*a*c) {{}}\n```\nДискриминант вычисляется прямо в конструкторе исключения — удобно для отладки.\n\nНаследуем от `invalid_argument`. Базовые исключения C++ хранят только текстовое сообщение, а наш класс хранит ещё и специфичные числовые данные, которые помогают диагностировать, **какие именно** значения вызвали ошибку.\n\nКонструктор вызывает родительский `invalid_argument(message)` — переданный текст будет доступен через метод `e.what()`, стандартный для всех исключений C++.\n\nМетод `printErrorData()` выводит «забракованные» данные — это требование задания. Благодаря ему при перехвате исключения мы видим не только текст ошибки, но и конкретные значения, которые её вызвали.\n\n## 2. Класс QuadEquation и `throw`\n```cpp\n    QuadEquation(double pa, double pb, double pc) : a(pa), b(pb), c(pc) {{\n        double D = b * b - 4 * a * c;\n        if (D < 0) {{\n            throw QuadException(\"D < 0!\", a, b, c);\n        }}\n        x1 = (-b + sqrt(D)) / (2 * a);\n        x2 = (-b - sqrt(D)) / (2 * a);\n    }}\n```\nКонструктор вычисляет дискриминант D = b² − 4ac. Если D < 0 — действительных корней нет, `throw`. Если D ≥ 0 — вычисляются корни по формуле.\n\nПри нарушении условия — **`throw`**: объект не досоздаётся, программа откатывается к ближайшему блоку `catch`. Все локальные объекты, созданные до `throw`, автоматически уничтожаются (стековая раскрутка / stack unwinding).\n\nВажно: вся ресурсоёмкая работа (выделение памяти, открытие файлов) делается **после** проверки. Если бы мы сначала выделили память, а потом обнаружили ошибку, пришлось бы вручную освобождать ресурсы перед `throw`.\n\n## 3. Блоки `try - catch`\n```cpp\n    try {{\n        QuadEquation eq2(1, 2, 5);  // D = 4 - 20 = -16 < 0\n    }}\n    catch (const QuadException& e) {{\n        e.printErrorData();  // покажет a=1, b=2, c=5, D=-16\n    }}\n```\nЛовим `QuadException` первым, затем `invalid_argument`, затем `exception`.\n**`try`** — место, где мы говорим компилятору: «Наблюдай за этим куском кода!». При возникновении `throw` все операции после него внутри `try` **не выполняются** — управление мгновенно передаётся в подходящий блок `catch`.\n\nПорядок блоков `catch` критически важен: **от самого специфичного к самому общему**. Первым ловим `QuadException` (наш пользовательский), затем его родителя `invalid_argument`, и только потом общий `exception`. Если поставить `exception` первым — он перехватит **все** ошибки, и специфичные обработчики никогда не сработают (компилятор даже выдаст предупреждение).\n\n",
  "condition": "<h2>Условие лабораторной работы №6 (Вариант 26)</h2>\n<h3>Тема: Обработка исключений (Exceptions) в С++</h3>\n<p><strong>Цель работы:</strong></p>\n<p>Освоить механизм обработки ошибок: создание классов-исключений, throw/catch.</p>\n<h3>Общее задание:</h3>\n<p>1. Создать собственный класс исключения, наследуемый от стандартного.</p>\n<p>2. Разработать целевой класс с валидацией в конструкторе.</p>\n<p>3. В <code class=\"inline-code\">main</code> продемонстрировать перехват через <code class=\"inline-code\">try-catch</code>.</p>\n<h3>Индивидуальное задание:</h3>\n<ul>\n<li>Формула: <em>26 % 9 = <strong>8</strong></em>.</li>\n<li><strong>Целевой класс:</strong> «Квадратное уравнение» (<code class=\"inline-code\">QuadEquation</code>). Поля: коэффициенты a, b, c.</li>\n<li>Если уравнение не имеет действительных корней (дискриминант < 0), генерируется исключение.</li>\n<li><strong>Исключение:</strong> наследник <code class=\"inline-code\">std::invalid_argument</code>.</li>\n</ul>",
  "conditionRaw": "# Условие лабораторной работы №6 (Вариант 26)\n\n## Тема: Обработка исключений (Exceptions) в С++\n\n**Цель работы:**\nОсвоить механизм обработки ошибок: создание классов-исключений, throw/catch.\n\n## Общее задание:\n1. Создать собственный класс исключения, наследуемый от стандартного.\n2. Разработать целевой класс с валидацией в конструкторе.\n3. В `main` продемонстрировать перехват через `try-catch`.\n\n## Индивидуальное задание:\n- Формула: *26 % 9 = **8***.\n- **Целевой класс:** «Квадратное уравнение» (`QuadEquation`). Поля: коэффициенты a, b, c.\n- Если уравнение не имеет действительных корней (дискриминант < 0), генерируется исключение.\n- **Исключение:** наследник `std::invalid_argument`.",
  "theory": "<h2>Теория: Обработка исключений (Exceptions) в C++</h2>\n<p>В данной лабораторной работе изучается механизм перехвата и обработки исключений (ошибок времени выполнения).</p>\n<h3>1. Что такое исключения?</h3>\n<p>Обычно, когда в программе возникает критическая ошибка (например: деление на ноль, выход за границы массива или создание объекта с недопустимыми параметрами), программа просто завершается (падает) с системной ошибкой.</p>\n<p>Исключения (<code class=\"inline-code\">exceptions</code>) — это элегантный способ \"предупредить\" программу об ошибке и передать управление специальному блоку кода, который знает, как эту ошибку \"обработать\" (например, вывести понятное сообщение пользователю), не давая при этом программе \"упасть\".</p>\n<p>Механизм исключений строится на 3 ключевых словах:</p>\n<ul>\n<li><strong><code class=\"inline-code\">try</code> (попробовать):</strong> блок кода, в котором <em>может</em> возникнуть исключение.</li>\n<li><strong><code class=\"inline-code\">throw</code> (бросить):</strong> само действие \"выброса\" (генерации) ошибки, когда функция понимает, что дальше работать нельзя (например, поступили плохие параметры).</li>\n<li><strong><code class=\"inline-code\">catch</code> (поймать):</strong> блок кода, который перехватывает \"брошенную\" ошибку и восстанавливает работоспособность системы.</li>\n</ul>\n<h3>2. Иерархия исключений в C++</h3>\n<p>В C++ есть стандартная библиотека исключений <code class=\"inline-code\"><stdexcept></code>. В её корне лежит класс <code class=\"inline-code\">std::exception</code>. От него наследуются конкретные классы:</p>\n<ul>\n<li><code class=\"inline-code\">std::logic_error</code> (ошибки логики, такие как <code class=\"inline-code\">std::invalid_argument</code> или <code class=\"inline-code\">std::out_of_range</code>)</li>\n<li><code class=\"inline-code\">std::runtime_error</code> (ошибки времени выполнения)</li>\n</ul>\n<p>В лабораторной мы создаем свой собственный тип ошибки (свой класс), наследуясь от стандартного исключения:</p>\n<pre><code>class MyException : public std::out_of_range { ... };\n// или\nclass MyException : public std::invalid_argument { ... };</code></pre>\n<p>Мы обязаны вызвать конструктор базового класса, передав ему текстовое сообщение об ошибке (оно сохранится, и его можно будет получить методом <code class=\"inline-code\">what()</code>).</p>\n<h3>3. Дополнительные данные в объекте исключения</h3>\n<p>Главное преимущество создания <strong>собственного</strong> класса исключения в том, что мы можем положить в него не только текстовое сообщение (как в стандартных ошибках), но и <strong>любые другие переменные</strong>, которые помогут программисту понять, почему произошла ошибка.</p>\n<p>В момент генерации ошибки конструктор целевого класса \"бросает\" исключение и кладет туда \"плохие\" значения:</p>\n<pre><code>throw MyException(\"Ошибка!\", bad_value1, bad_value2);</code></pre>\n<p>А в блоке обработки (<code class=\"inline-code\">catch</code>) мы можем вызвать метод <code class=\"inline-code\">printErrorData()</code>, который напечатает эти данные.</p>\n<h3>4. Порядок блоков <code class=\"inline-code\">catch</code></h3>\n<p>При написании нескольких <code class=\"inline-code\">catch</code> подряд <strong>крайне важно</strong> располагать их от самых специфичных к самым общим.</p>\n<p>1. Сначала ловим <code class=\"inline-code\">catch (const MyException& e)</code> (наследник).</p>\n<p>2. Потом ловим <code class=\"inline-code\">catch (const std::out_of_range& e)</code> (родитель).</p>\n<p>3. Потом <code class=\"inline-code\">catch (const std::exception& e)</code> (самый базовый прародитель всех ошибок).</p>\n<p>Если бы мы поставили <code class=\"inline-code\">catch (const std::exception& e)</code> первым, полиморфизм привел бы к тому, что он \"поймал\" бы <em>все</em> исключения, включая наше. Обратите внимание, что объекты-исключения всегда перехватываются <strong>по константной ссылке</strong> <code class=\"inline-code\">const &</code> (во избежание срезания полей объекта при копировании).</p>",
  "theoryRaw": "# Теория: Обработка исключений (Exceptions) в C++\n\nВ данной лабораторной работе изучается механизм перехвата и обработки исключений (ошибок времени выполнения).\n\n## 1. Что такое исключения?\nОбычно, когда в программе возникает критическая ошибка (например: деление на ноль, выход за границы массива или создание объекта с недопустимыми параметрами), программа просто завершается (падает) с системной ошибкой.\nИсключения (`exceptions`) — это элегантный способ \"предупредить\" программу об ошибке и передать управление специальному блоку кода, который знает, как эту ошибку \"обработать\" (например, вывести понятное сообщение пользователю), не давая при этом программе \"упасть\".\n\nМеханизм исключений строится на 3 ключевых словах:\n- **`try` (попробовать):** блок кода, в котором *может* возникнуть исключение.\n- **`throw` (бросить):** само действие \"выброса\" (генерации) ошибки, когда функция понимает, что дальше работать нельзя (например, поступили плохие параметры).\n- **`catch` (поймать):** блок кода, который перехватывает \"брошенную\" ошибку и восстанавливает работоспособность системы.\n\n## 2. Иерархия исключений в C++\nВ C++ есть стандартная библиотека исключений `<stdexcept>`. В её корне лежит класс `std::exception`. От него наследуются конкретные классы:\n- `std::logic_error` (ошибки логики, такие как `std::invalid_argument` или `std::out_of_range`)\n- `std::runtime_error` (ошибки времени выполнения)\n\nВ лабораторной мы создаем свой собственный тип ошибки (свой класс), наследуясь от стандартного исключения:\n\n```cpp\nclass MyException : public std::out_of_range { ... };\n// или\nclass MyException : public std::invalid_argument { ... };\n```\n\nМы обязаны вызвать конструктор базового класса, передав ему текстовое сообщение об ошибке (оно сохранится, и его можно будет получить методом `what()`).\n\n## 3. Дополнительные данные в объекте исключения\nГлавное преимущество создания **собственного** класса исключения в том, что мы можем положить в него не только текстовое сообщение (как в стандартных ошибках), но и **любые другие переменные**, которые помогут программисту понять, почему произошла ошибка.\n\nВ момент генерации ошибки конструктор целевого класса \"бросает\" исключение и кладет туда \"плохие\" значения:\n\n```cpp\nthrow MyException(\"Ошибка!\", bad_value1, bad_value2);\n```\n\nА в блоке обработки (`catch`) мы можем вызвать метод `printErrorData()`, который напечатает эти данные.\n\n## 4. Порядок блоков `catch`\nПри написании нескольких `catch` подряд **крайне важно** располагать их от самых специфичных к самым общим.\n1. Сначала ловим `catch (const MyException& e)` (наследник).\n2. Потом ловим `catch (const std::out_of_range& e)` (родитель).\n3. Потом `catch (const std::exception& e)` (самый базовый прародитель всех ошибок).\n\nЕсли бы мы поставили `catch (const std::exception& e)` первым, полиморфизм привел бы к тому, что он \"поймал\" бы *все* исключения, включая наше. Обратите внимание, что объекты-исключения всегда перехватываются **по константной ссылке** `const &` (во избежание срезания полей объекта при копировании)."
}