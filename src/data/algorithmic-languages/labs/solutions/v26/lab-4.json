{
  "variant": 26,
  "lab": 4,
  "code": "#include <iostream>\n#include <fstream>\n#include <string>\n#include <algorithm>\n#include <deque>\n#include <list>\n\nusing namespace std;\n\nclass Car {\nprivate:\n    string model;\n    double fuel[3]; // трасса, город, смешанный\n    double maxSpeed;\n    int power;\n\npublic:\n    Car() : model(\"\"), maxSpeed(0), power(0) { fuel[0]=fuel[1]=fuel[2]=0; }\n\n    Car(string m, double f1, double f2, double f3, double s, int p)\n        : model(move(m)), maxSpeed(s), power(p) {\n        fuel[0]=f1; fuel[1]=f2; fuel[2]=f3;\n    }\n\n    Car(const Car& other)\n        : model(other.model), maxSpeed(other.maxSpeed), power(other.power) {\n        for(int i=0;i<3;i++) fuel[i]=other.fuel[i];\n     {}\n\n    Car(Car&& other) noexcept\n        : model(move(other.model)), maxSpeed(other.maxSpeed), power(other.power) {\n        for(int i=0;i<3;i++) fuel[i]=other.fuel[i];\n     {}\n\n    Car& operator=(const Car& other) {\n        if (this != &other) {\n            model = other.model;\n            for(int i=0;i<3;i++) fuel[i]=other.fuel[i];\n            maxSpeed = other.maxSpeed;\n            power = other.power;\n        }\n        return *this;\n    }\n\n    Car& operator=(Car&& other) noexcept {\n        if (this != &other) {\n            model = move(other.model);\n            for(int i=0;i<3;i++) fuel[i]=other.fuel[i];\n            maxSpeed = other.maxSpeed;\n            power = other.power;\n        }\n        return *this;\n    }\n\n    string getModel() const { return model; }\n    double getMixedFuel() const { return fuel[2]; }\n\n    friend ostream& operator<<(ostream& os, const Car& c) {\n        os << \"Авто: \" << c.model << \", Расход (трасса/город/смеш): \"\n           << c.fuel[0] << \"/\" << c.fuel[1] << \"/\" << c.fuel[2]\n           << \" л/100км, Макс: \" << c.maxSpeed << \" км/ч, \" << c.power << \" л.с.\";\n        return os;\n    }\n\n    friend istream& operator>>(istream& is, Car& c) {\n        is >> c.model >> c.fuel[0] >> c.fuel[1] >> c.fuel[2] >> c.maxSpeed >> c.power;\n        return is;\n    }\n};\n\nbool compareByMixedFuel(const Car& a, const Car& b) {\n    return a.getMixedFuel() < b.getMixedFuel();\n}\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    ifstream fin(\"input.txt\");\n\n    deque<Car> items;\n    Car temp;\n\n    while (fin >> temp) {\n        items.push_back(temp);\n    }\n    fin.close();\n\n    ofstream fout(\"output.txt\");\n\n    fout << \"Исходный контейнер (deque):\\n\";\n    cout << \"Исходный контейнер (deque):\\n\";\n    for (const auto& item : items) {\n        fout << item << \"\\n\";\n        cout << item << \"\\n\";\n    }\n\n    sort(items.begin(), items.end(), compareByMixedFuel);\n\n    fout << \"\\nОтсортированный контейнер (по смешанному расходу топлива):\\n\";\n    cout << \"\\nОтсортированный контейнер (по смешанному расходу топлива):\\n\";\n    for (const auto& item : items) {\n        fout << item << \"\\n\";\n        cout << item << \"\\n\";\n    }\n\n    list<Car> copied(items.size());\n    copy(items.begin(), items.end(), copied.begin());\n\n    fout << \"\\nСкопированный контейнер (list):\\n\";\n    cout << \"\\nСкопированный контейнер (list):\\n\";\n    for (const auto& item : copied) {\n        fout << item << \"\\n\";\n        cout << item << \"\\n\";\n    }\n\n    fout.close();\n    cout << \"\\nУспех! Результаты сохранены в файл output.txt\" << endl;\n\n    return 0;\n}",
  "codeHtml": "<pre><code>#include &lt;iostream&gt;\n#include &lt;fstream&gt;\n#include &lt;string&gt;\n#include &lt;algorithm&gt;\n#include &lt;deque&gt;\n#include &lt;list&gt;\n\nusing namespace std;\n\nclass Car {\nprivate:\n    string model;\n    double fuel[3]; // трасса, город, смешанный\n    double maxSpeed;\n    int power;\n\npublic:\n    Car() : model(\"\"), maxSpeed(0), power(0) { fuel[0]=fuel[1]=fuel[2]=0; }\n\n    Car(string m, double f1, double f2, double f3, double s, int p)\n        : model(move(m)), maxSpeed(s), power(p) {\n        fuel[0]=f1; fuel[1]=f2; fuel[2]=f3;\n    }\n\n    Car(const Car&amp; other)\n        : model(other.model), maxSpeed(other.maxSpeed), power(other.power) {\n        for(int i=0;i&lt;3;i++) fuel[i]=other.fuel[i];\n     {}\n\n    Car(Car&amp;&amp; other) noexcept\n        : model(move(other.model)), maxSpeed(other.maxSpeed), power(other.power) {\n        for(int i=0;i&lt;3;i++) fuel[i]=other.fuel[i];\n     {}\n\n    Car&amp; operator=(const Car&amp; other) {\n        if (this != &amp;other) {\n            model = other.model;\n            for(int i=0;i&lt;3;i++) fuel[i]=other.fuel[i];\n            maxSpeed = other.maxSpeed;\n            power = other.power;\n        }\n        return *this;\n    }\n\n    Car&amp; operator=(Car&amp;&amp; other) noexcept {\n        if (this != &amp;other) {\n            model = move(other.model);\n            for(int i=0;i&lt;3;i++) fuel[i]=other.fuel[i];\n            maxSpeed = other.maxSpeed;\n            power = other.power;\n        }\n        return *this;\n    }\n\n    string getModel() const { return model; }\n    double getMixedFuel() const { return fuel[2]; }\n\n    friend ostream&amp; operator&lt;&lt;(ostream&amp; os, const Car&amp; c) {\n        os &lt;&lt; \"Авто: \" &lt;&lt; c.model &lt;&lt; \", Расход (трасса/город/смеш): \"\n           &lt;&lt; c.fuel[0] &lt;&lt; \"/\" &lt;&lt; c.fuel[1] &lt;&lt; \"/\" &lt;&lt; c.fuel[2]\n           &lt;&lt; \" л/100км, Макс: \" &lt;&lt; c.maxSpeed &lt;&lt; \" км/ч, \" &lt;&lt; c.power &lt;&lt; \" л.с.\";\n        return os;\n    }\n\n    friend istream&amp; operator&gt;&gt;(istream&amp; is, Car&amp; c) {\n        is &gt;&gt; c.model &gt;&gt; c.fuel[0] &gt;&gt; c.fuel[1] &gt;&gt; c.fuel[2] &gt;&gt; c.maxSpeed &gt;&gt; c.power;\n        return is;\n    }\n};\n\nbool compareByMixedFuel(const Car&amp; a, const Car&amp; b) {\n    return a.getMixedFuel() &lt; b.getMixedFuel();\n}\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    ifstream fin(\"input.txt\");\n\n    deque&lt;Car&gt; items;\n    Car temp;\n\n    while (fin &gt;&gt; temp) {\n        items.push_back(temp);\n    }\n    fin.close();\n\n    ofstream fout(\"output.txt\");\n\n    fout &lt;&lt; \"Исходный контейнер (deque):\\n\";\n    cout &lt;&lt; \"Исходный контейнер (deque):\\n\";\n    for (const auto&amp; item : items) {\n        fout &lt;&lt; item &lt;&lt; \"\\n\";\n        cout &lt;&lt; item &lt;&lt; \"\\n\";\n    }\n\n    sort(items.begin(), items.end(), compareByMixedFuel);\n\n    fout &lt;&lt; \"\\nОтсортированный контейнер (по смешанному расходу топлива):\\n\";\n    cout &lt;&lt; \"\\nОтсортированный контейнер (по смешанному расходу топлива):\\n\";\n    for (const auto&amp; item : items) {\n        fout &lt;&lt; item &lt;&lt; \"\\n\";\n        cout &lt;&lt; item &lt;&lt; \"\\n\";\n    }\n\n    list&lt;Car&gt; copied(items.size());\n    copy(items.begin(), items.end(), copied.begin());\n\n    fout &lt;&lt; \"\\nСкопированный контейнер (list):\\n\";\n    cout &lt;&lt; \"\\nСкопированный контейнер (list):\\n\";\n    for (const auto&amp; item : copied) {\n        fout &lt;&lt; item &lt;&lt; \"\\n\";\n        cout &lt;&lt; item &lt;&lt; \"\\n\";\n    }\n\n    fout.close();\n    cout &lt;&lt; \"\\nУспех! Результаты сохранены в файл output.txt\" &lt;&lt; endl;\n\n    return 0;\n}</code></pre>",
  "explanation": "<h2>Разбор кода: Лабораторная работа 4 (Вариант 26)</h2>\n<p>Ниже представлен полный исходный код из <code class=\"inline-code\">main.cpp</code> для удобного ознакомления. </p>\n<p>В последующих разделах этот код разбит на логические блоки с детальным объяснением реализации класса <code class=\"inline-code\">Car</code> и использования алгоритмов STL.</p>\n<h3>Полный исходный код программы</h3>\n<pre><code>#include &lt;iostream&gt;\n#include &lt;fstream&gt;\n#include &lt;string&gt;\n#include &lt;algorithm&gt;\n#include &lt;deque&gt;\n#include &lt;list&gt;\n\nusing namespace std;\n\nclass Car {\nprivate:\n    string model;\n    double fuel[3]; // трасса, город, смешанный\n    double maxSpeed;\n    int power;\n\npublic:\n    Car() : model(\"\"), maxSpeed(0), power(0) { fuel[0]=fuel[1]=fuel[2]=0; }\n\n    Car(string m, double f1, double f2, double f3, double s, int p)\n        : model(move(m)), maxSpeed(s), power(p) {\n        fuel[0]=f1; fuel[1]=f2; fuel[2]=f3;\n    }\n\n    Car(const Car&amp; other)\n        : model(other.model), maxSpeed(other.maxSpeed), power(other.power) {\n        for(int i=0;i&lt;3;i++) fuel[i]=other.fuel[i];\n     {}\n\n    Car(Car&amp;&amp; other) noexcept\n        : model(move(other.model)), maxSpeed(other.maxSpeed), power(other.power) {\n        for(int i=0;i&lt;3;i++) fuel[i]=other.fuel[i];\n     {}\n\n    Car&amp; operator=(const Car&amp; other) {\n        if (this != &amp;other) {\n            model = other.model;\n            for(int i=0;i&lt;3;i++) fuel[i]=other.fuel[i];\n            maxSpeed = other.maxSpeed;\n            power = other.power;\n        }\n        return *this;\n    }\n\n    Car&amp; operator=(Car&amp;&amp; other) noexcept {\n        if (this != &amp;other) {\n            model = move(other.model);\n            for(int i=0;i&lt;3;i++) fuel[i]=other.fuel[i];\n            maxSpeed = other.maxSpeed;\n            power = other.power;\n        }\n        return *this;\n    }\n\n    string getModel() const { return model; }\n    double getMixedFuel() const { return fuel[2]; }\n\n    friend ostream&amp; operator&lt;&lt;(ostream&amp; os, const Car&amp; c) {\n        os &lt;&lt; \"Авто: \" &lt;&lt; c.model &lt;&lt; \", Расход (трасса/город/смеш): \"\n           &lt;&lt; c.fuel[0] &lt;&lt; \"/\" &lt;&lt; c.fuel[1] &lt;&lt; \"/\" &lt;&lt; c.fuel[2]\n           &lt;&lt; \" л/100км, Макс: \" &lt;&lt; c.maxSpeed &lt;&lt; \" км/ч, \" &lt;&lt; c.power &lt;&lt; \" л.с.\";\n        return os;\n    }\n\n    friend istream&amp; operator&gt;&gt;(istream&amp; is, Car&amp; c) {\n        is &gt;&gt; c.model &gt;&gt; c.fuel[0] &gt;&gt; c.fuel[1] &gt;&gt; c.fuel[2] &gt;&gt; c.maxSpeed &gt;&gt; c.power;\n        return is;\n    }\n};\n\nbool compareByMixedFuel(const Car&amp; a, const Car&amp; b) {\n    return a.getMixedFuel() &lt; b.getMixedFuel();\n}\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    ifstream fin(\"input.txt\");\n\n    deque&lt;Car&gt; items;\n    Car temp;\n\n    while (fin &gt;&gt; temp) {\n        items.push_back(temp);\n    }\n    fin.close();\n\n    ofstream fout(\"output.txt\");\n\n    fout &lt;&lt; \"Исходный контейнер (deque):\\n\";\n    cout &lt;&lt; \"Исходный контейнер (deque):\\n\";\n    for (const auto&amp; item : items) {\n        fout &lt;&lt; item &lt;&lt; \"\\n\";\n        cout &lt;&lt; item &lt;&lt; \"\\n\";\n    }\n\n    sort(items.begin(), items.end(), compareByMixedFuel);\n\n    fout &lt;&lt; \"\\nОтсортированный контейнер (по смешанному расходу топлива):\\n\";\n    cout &lt;&lt; \"\\nОтсортированный контейнер (по смешанному расходу топлива):\\n\";\n    for (const auto&amp; item : items) {\n        fout &lt;&lt; item &lt;&lt; \"\\n\";\n        cout &lt;&lt; item &lt;&lt; \"\\n\";\n    }\n\n    list&lt;Car&gt; copied(items.size());\n    copy(items.begin(), items.end(), copied.begin());\n\n    fout &lt;&lt; \"\\nСкопированный контейнер (list):\\n\";\n    cout &lt;&lt; \"\\nСкопированный контейнер (list):\\n\";\n    for (const auto&amp; item : copied) {\n        fout &lt;&lt; item &lt;&lt; \"\\n\";\n        cout &lt;&lt; item &lt;&lt; \"\\n\";\n    }\n\n    fout.close();\n    cout &lt;&lt; \"\\nУспех! Результаты сохранены в файл output.txt\" &lt;&lt; endl;\n\n    return 0;\n}\n</code></pre>\n<hr>\n<h3>1. Класс <code class=\"inline-code\">Car</code></h3>\n<h4>Поля класса</h4>\n<pre><code>private:\n    string model;\n    double fuel[3]; // трасса, город, смешанный\n    double maxSpeed;\n    int power;</code></pre>\n<p>Поля класса объявлены как <code class=\"inline-code\">private</code> — это стандартная инкапсуляция. Доступ к ним осуществляется через геттеры для чтения и оператор <code class=\"inline-code\">>></code> для записи.</p>\n<h4>Конструкторы</h4>\n<pre><code>    Car() : model(\"\"), maxSpeed(0), power(0) { fuel[0]=fuel[1]=fuel[2]=0; }</code></pre>\n<p><strong>Конструктор по умолчанию</strong> необходим для работы с STL-контейнерами. Когда мы пишем <code class=\"inline-code\">list<Car> copied(items.size())</code>, контейнер создает <code class=\"inline-code\">items.size()</code> объектов, вызывая для каждого конструктор по умолчанию.</p>\n<pre><code>    Car(string m, double f1, double f2, double f3, double s, int p)\n        : model(move(m)), maxSpeed(s), power(p) {\n        fuel[0]=f1; fuel[1]=f2; fuel[2]=f3;\n    }</code></pre>\n<p><strong>Конструктор с параметрами</strong> инициализирует поля. Заметим <code class=\"inline-code\">move()</code> для строковых полей. Поскольку строки (<code class=\"inline-code\">string</code>) — это ресурсоемкие объекты, которые выделяют память динамически внутри себя, вместо того чтобы целиком копировать строку, мы <strong>перемещаем</strong> байты прямо в поле с помощью <code class=\"inline-code\">move()</code>, что намного быстрее.</p>\n<h4>Конструктор копирования и перемещения</h4>\n<pre><code>    Car(const Car&amp; other)\n        : model(other.model), maxSpeed(other.maxSpeed), power(other.power) {\n        for(int i=0;i&lt;3;i++) fuel[i]=other.fuel[i];\n     {}</code></pre>\n<p><strong>Конструктор копирования</strong> создаёт полную копию объекта. Все поля копируются из <code class=\"inline-code\">other</code>.</p>\n<pre><code>    Car(Car&amp;&amp; other) noexcept\n        : model(move(other.model)), maxSpeed(other.maxSpeed), power(other.power) {\n        for(int i=0;i&lt;3;i++) fuel[i]=other.fuel[i];\n     {}</code></pre>\n<p><strong>Конструктор перемещения</strong> оптимизирует передачу объектов в памяти (например, при вызове <code class=\"inline-code\">push_back</code> и расширении массива под капотом вектора). <code class=\"inline-code\">noexcept</code> указывает, что этот метод никогда не выбросит исключение — тогда STL-контейнеры будут использовать его вместо медленного конструктора копирования.</p>\n<h4>Операторы присваивания</h4>\n<pre><code>    Car&amp; operator=(const Car&amp; other) {\n        if (this != &amp;other) {\n            model = other.model;\n            for(int i=0;i&lt;3;i++) fuel[i]=other.fuel[i];\n            maxSpeed = other.maxSpeed;\n            power = other.power;\n        }\n        return *this;\n    }</code></pre>\n<p><strong>Оператор копирующего присваивания</strong>. Проверка <code class=\"inline-code\">if (this != &other)</code> предотвращает саморазрушение при <code class=\"inline-code\">a = a</code>.</p>\n<pre><code>    Car&amp; operator=(Car&amp;&amp; other) noexcept { ... }</code></pre>\n<p><strong>Оператор перемещающего присваивания</strong>. Аналогичен конструктору перемещения, но для уже существующих объектов. Используется алгоритмом <code class=\"inline-code\">std::copy</code> при перезаписи элементов целевого контейнера.</p>\n<h4>Операторы потокового ввода/вывода</h4>\n<pre><code>    friend istream&amp; operator&gt;&gt;(istream&amp; is, Car&amp; c) {\n        is &gt;&gt; c.model &gt;&gt; c.fuel[0] &gt;&gt; c.fuel[1] &gt;&gt; c.fuel[2] &gt;&gt; c.maxSpeed &gt;&gt; c.power;\n        return is;\n    }</code></pre>\n<p>Мы переопределяем оператор <code class=\"inline-code\">>></code>. Теперь мы можем просто писать <code class=\"inline-code\">fin >> temp</code> и программа сама считает все поля из текстового файла, разбив их по пробелу. Оператор объявлен как <strong><code class=\"inline-code\">friend</code></strong>, чтобы иметь доступ к <code class=\"inline-code\">private</code> полям класса.</p>\n<hr>\n<h3>2. Логика функции <code class=\"inline-code\">main</code></h3>\n<h4>Чтение из файла</h4>\n<pre><code>    deque&lt;Car&gt; items;\n    Car temp;\n    while (fin &gt;&gt; temp) {\n        items.push_back(temp);\n    }</code></pre>\n<p>Мы создаем контейнер <code class=\"inline-code\">deque</code>. В цикле <code class=\"inline-code\">while</code> считываем по одному объекту из файла до тех пор, пока не достигнем его конца. Метод <code class=\"inline-code\">push_back</code> вставляет элемент в конец контейнера, при необходимости самостоятельно увеличивая размер.</p>\n<h4>Сортировка (Алгоритм <code class=\"inline-code\">std::sort</code>)</h4>\n<pre><code>sort(items.begin(), items.end(), compareByMixedFuel);</code></pre>\n<p>Стандартная библиотека <code class=\"inline-code\"><algorithm></code> позволяет очень быстро отсортировать контейнер. Для этого мы передаем <code class=\"inline-code\">begin()</code> (итератор на начало), <code class=\"inline-code\">end()</code> (знак \"за концом\" контейнера) и свою функцию-компаратор, которая объясняет процедуре, какое именно значение нужно считать \"меньшим\" (сравниваем по смешанному расходу топлива).</p>\n<h4>Копирование (Алгоритм <code class=\"inline-code\">std::copy</code>)</h4>\n<pre><code>    list&lt;Car&gt; copied(items.size());\n    copy(items.begin(), items.end(), copied.begin());</code></pre>\n<p>По заданию мы копируем данные из <code class=\"inline-code\">deque</code> в <code class=\"inline-code\">list</code>.</p>\n<p>1. Мы обязаны <strong>выделить память</strong> в целевом контейнере <code class=\"inline-code\">copied</code> через конструктор <code class=\"inline-code\">(items.size())</code>, потому что <code class=\"inline-code\">std::copy</code> только перезаписывает уже выделенные ячейки (не выделяет новую память).</p>\n<p>2. <code class=\"inline-code\">std::copy</code> берёт элементы от <code class=\"inline-code\">begin()</code> до <code class=\"inline-code\">end()</code> исходного контейнера и последовательно присваивает (использует <code class=\"inline-code\">operator=</code>) в целевой.</p>",
  "explanationRaw": "# Разбор кода: Лабораторная работа 4 (Вариант 26)\n\nНиже представлен полный исходный код из `main.cpp` для удобного ознакомления. \nВ последующих разделах этот код разбит на логические блоки с детальным объяснением реализации класса `Car` и использования алгоритмов STL.\n\n## Полный исходный код программы\n```cpp\n#include <iostream>\n#include <fstream>\n#include <string>\n#include <algorithm>\n#include <deque>\n#include <list>\n\nusing namespace std;\n\nclass Car {\nprivate:\n    string model;\n    double fuel[3]; // трасса, город, смешанный\n    double maxSpeed;\n    int power;\n\npublic:\n    Car() : model(\"\"), maxSpeed(0), power(0) { fuel[0]=fuel[1]=fuel[2]=0; }\n\n    Car(string m, double f1, double f2, double f3, double s, int p)\n        : model(move(m)), maxSpeed(s), power(p) {\n        fuel[0]=f1; fuel[1]=f2; fuel[2]=f3;\n    }\n\n    Car(const Car& other)\n        : model(other.model), maxSpeed(other.maxSpeed), power(other.power) {\n        for(int i=0;i<3;i++) fuel[i]=other.fuel[i];\n     {}\n\n    Car(Car&& other) noexcept\n        : model(move(other.model)), maxSpeed(other.maxSpeed), power(other.power) {\n        for(int i=0;i<3;i++) fuel[i]=other.fuel[i];\n     {}\n\n    Car& operator=(const Car& other) {\n        if (this != &other) {\n            model = other.model;\n            for(int i=0;i<3;i++) fuel[i]=other.fuel[i];\n            maxSpeed = other.maxSpeed;\n            power = other.power;\n        }\n        return *this;\n    }\n\n    Car& operator=(Car&& other) noexcept {\n        if (this != &other) {\n            model = move(other.model);\n            for(int i=0;i<3;i++) fuel[i]=other.fuel[i];\n            maxSpeed = other.maxSpeed;\n            power = other.power;\n        }\n        return *this;\n    }\n\n    string getModel() const { return model; }\n    double getMixedFuel() const { return fuel[2]; }\n\n    friend ostream& operator<<(ostream& os, const Car& c) {\n        os << \"Авто: \" << c.model << \", Расход (трасса/город/смеш): \"\n           << c.fuel[0] << \"/\" << c.fuel[1] << \"/\" << c.fuel[2]\n           << \" л/100км, Макс: \" << c.maxSpeed << \" км/ч, \" << c.power << \" л.с.\";\n        return os;\n    }\n\n    friend istream& operator>>(istream& is, Car& c) {\n        is >> c.model >> c.fuel[0] >> c.fuel[1] >> c.fuel[2] >> c.maxSpeed >> c.power;\n        return is;\n    }\n};\n\nbool compareByMixedFuel(const Car& a, const Car& b) {\n    return a.getMixedFuel() < b.getMixedFuel();\n}\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    ifstream fin(\"input.txt\");\n\n    deque<Car> items;\n    Car temp;\n\n    while (fin >> temp) {\n        items.push_back(temp);\n    }\n    fin.close();\n\n    ofstream fout(\"output.txt\");\n\n    fout << \"Исходный контейнер (deque):\\n\";\n    cout << \"Исходный контейнер (deque):\\n\";\n    for (const auto& item : items) {\n        fout << item << \"\\n\";\n        cout << item << \"\\n\";\n    }\n\n    sort(items.begin(), items.end(), compareByMixedFuel);\n\n    fout << \"\\nОтсортированный контейнер (по смешанному расходу топлива):\\n\";\n    cout << \"\\nОтсортированный контейнер (по смешанному расходу топлива):\\n\";\n    for (const auto& item : items) {\n        fout << item << \"\\n\";\n        cout << item << \"\\n\";\n    }\n\n    list<Car> copied(items.size());\n    copy(items.begin(), items.end(), copied.begin());\n\n    fout << \"\\nСкопированный контейнер (list):\\n\";\n    cout << \"\\nСкопированный контейнер (list):\\n\";\n    for (const auto& item : copied) {\n        fout << item << \"\\n\";\n        cout << item << \"\\n\";\n    }\n\n    fout.close();\n    cout << \"\\nУспех! Результаты сохранены в файл output.txt\" << endl;\n\n    return 0;\n}\n\n```\n\n---\n\n## 1. Класс `Car`\n\n### Поля класса\n```cpp\nprivate:\n    string model;\n    double fuel[3]; // трасса, город, смешанный\n    double maxSpeed;\n    int power;\n```\nПоля класса объявлены как `private` — это стандартная инкапсуляция. Доступ к ним осуществляется через геттеры для чтения и оператор `>>` для записи.\n\n### Конструкторы\n```cpp\n    Car() : model(\"\"), maxSpeed(0), power(0) { fuel[0]=fuel[1]=fuel[2]=0; }\n```\n**Конструктор по умолчанию** необходим для работы с STL-контейнерами. Когда мы пишем `list<Car> copied(items.size())`, контейнер создает `items.size()` объектов, вызывая для каждого конструктор по умолчанию.\n\n```cpp\n    Car(string m, double f1, double f2, double f3, double s, int p)\n        : model(move(m)), maxSpeed(s), power(p) {\n        fuel[0]=f1; fuel[1]=f2; fuel[2]=f3;\n    }\n```\n**Конструктор с параметрами** инициализирует поля. Заметим `move()` для строковых полей. Поскольку строки (`string`) — это ресурсоемкие объекты, которые выделяют память динамически внутри себя, вместо того чтобы целиком копировать строку, мы **перемещаем** байты прямо в поле с помощью `move()`, что намного быстрее.\n\n### Конструктор копирования и перемещения\n```cpp\n    Car(const Car& other)\n        : model(other.model), maxSpeed(other.maxSpeed), power(other.power) {\n        for(int i=0;i<3;i++) fuel[i]=other.fuel[i];\n     {}\n```\n**Конструктор копирования** создаёт полную копию объекта. Все поля копируются из `other`.\n\n```cpp\n    Car(Car&& other) noexcept\n        : model(move(other.model)), maxSpeed(other.maxSpeed), power(other.power) {\n        for(int i=0;i<3;i++) fuel[i]=other.fuel[i];\n     {}\n```\n**Конструктор перемещения** оптимизирует передачу объектов в памяти (например, при вызове `push_back` и расширении массива под капотом вектора). `noexcept` указывает, что этот метод никогда не выбросит исключение — тогда STL-контейнеры будут использовать его вместо медленного конструктора копирования.\n\n### Операторы присваивания\n```cpp\n    Car& operator=(const Car& other) {\n        if (this != &other) {\n            model = other.model;\n            for(int i=0;i<3;i++) fuel[i]=other.fuel[i];\n            maxSpeed = other.maxSpeed;\n            power = other.power;\n        }\n        return *this;\n    }\n```\n**Оператор копирующего присваивания**. Проверка `if (this != &other)` предотвращает саморазрушение при `a = a`.\n\n```cpp\n    Car& operator=(Car&& other) noexcept { ... }\n```\n**Оператор перемещающего присваивания**. Аналогичен конструктору перемещения, но для уже существующих объектов. Используется алгоритмом `std::copy` при перезаписи элементов целевого контейнера.\n\n### Операторы потокового ввода/вывода\n```cpp\n    friend istream& operator>>(istream& is, Car& c) {\n        is >> c.model >> c.fuel[0] >> c.fuel[1] >> c.fuel[2] >> c.maxSpeed >> c.power;\n        return is;\n    }\n```\nМы переопределяем оператор `>>`. Теперь мы можем просто писать `fin >> temp` и программа сама считает все поля из текстового файла, разбив их по пробелу. Оператор объявлен как **`friend`**, чтобы иметь доступ к `private` полям класса.\n\n---\n\n## 2. Логика функции `main`\n\n### Чтение из файла\n```cpp\n    deque<Car> items;\n    Car temp;\n    while (fin >> temp) {\n        items.push_back(temp);\n    }\n```\nМы создаем контейнер `deque`. В цикле `while` считываем по одному объекту из файла до тех пор, пока не достигнем его конца. Метод `push_back` вставляет элемент в конец контейнера, при необходимости самостоятельно увеличивая размер.\n\n### Сортировка (Алгоритм `std::sort`)\n```cpp\nsort(items.begin(), items.end(), compareByMixedFuel);\n```\nСтандартная библиотека `<algorithm>` позволяет очень быстро отсортировать контейнер. Для этого мы передаем `begin()` (итератор на начало), `end()` (знак \"за концом\" контейнера) и свою функцию-компаратор, которая объясняет процедуре, какое именно значение нужно считать \"меньшим\" (сравниваем по смешанному расходу топлива).\n\n### Копирование (Алгоритм `std::copy`)\n```cpp\n    list<Car> copied(items.size());\n    copy(items.begin(), items.end(), copied.begin());\n```\nПо заданию мы копируем данные из `deque` в `list`.\n1. Мы обязаны **выделить память** в целевом контейнере `copied` через конструктор `(items.size())`, потому что `std::copy` только перезаписывает уже выделенные ячейки (не выделяет новую память).\n2. `std::copy` берёт элементы от `begin()` до `end()` исходного контейнера и последовательно присваивает (использует `operator=`) в целевой.",
  "condition": "<h2>Условие лабораторной работы №4 (Вариант 26)</h2>\n<h3>Тема: Использование последовательных контейнеров STL и алгоритмов</h3>\n<p><strong>Цель работы:</strong></p>\n<p>Освоить работу со стандартной библиотекой шаблонов (STL) в части последовательных контейнеров (<code class=\"inline-code\">std::vector</code>, <code class=\"inline-code\">std::deque</code>, <code class=\"inline-code\">std::list</code>) и универсальных алгоритмов сортировки и копирования.</p>\n<h3>Общее задание:</h3>\n<p>1. Разработать пользовательский класс согласно своему варианту. Класс должен содержать не менее трех-четырех полей различных типов, конструкторы (в том числе правильные конструкторы копирования и перемещения, если это необходимо), геттеры/сеттеры.</p>\n<p>2. Подготовить текстовый файл <code class=\"inline-code\">input.txt</code> с входными характеристиками нескольких объектов (не менее 5 строк). Перегрузить оператор потока ввода <code class=\"inline-code\">>></code> для этого класса, чтобы удобно считывать объекты из файла за один раз.</p>\n<p>3. Процесс работы программы:</p>\n<ul>\n<li>Прочитать данные из файла и добавить сгенерированные объекты в <strong>исходный контейнер</strong> №1 (добавлять с помощью методов последовательных контейнеров, таких как <code class=\"inline-code\">push_back</code>).</li>\n<li>Напечатать исходный контейнер (вывести объекты, в нем лежащие). (Для этого обязательно перегрузить оператор потока вставки <code class=\"inline-code\"><<</code> для вашего пользовательского класса).</li>\n<li>Отсортировать объекты в этом базовом контейнере согласно критерию (использовать алгоритм <code class=\"inline-code\">std::sort</code> и написать собственный компаратор).</li>\n<li>Распечатать отсортированный контейнер.</li>\n<li>Скопировать данные из первого контейнера во <strong>второй целевой контейнер</strong> другого типа с помощью обобщенного алгоритма <code class=\"inline-code\">std::copy</code>. Для целевого контейнера необходимо предварительно выделить нужное количество памяти.</li>\n<li>Распечатать целевой скопированный контейнер.</li>\n</ul>\n<h3>Индивидуальное задание (Вариант 26 из Таблицы 1):</h3>\n<p>1. <strong>Название класса:</strong> «Автомобиль»</p>\n<ul>\n<li><em>Характеристики (поля):</em> название модели, массив расходов топлива из 3-х элементов (трасса, город, смешанный), макс. скорость, мощность.</li>\n</ul>\n<p>2. <strong>Исходный контейнер (№1):</strong> <code class=\"inline-code\">std::deque</code></p>\n<p>3. <strong>Целевой контейнер (№2):</strong> <code class=\"inline-code\">std::list</code></p>\n<p>4. <strong>Критерий сортировки:</strong> по смешанному расходу топлива.</p>",
  "conditionRaw": "# Условие лабораторной работы №4 (Вариант 26)\n\n## Тема: Использование последовательных контейнеров STL и алгоритмов\n\n**Цель работы:**\nОсвоить работу со стандартной библиотекой шаблонов (STL) в части последовательных контейнеров (`std::vector`, `std::deque`, `std::list`) и универсальных алгоритмов сортировки и копирования.\n\n## Общее задание:\n1. Разработать пользовательский класс согласно своему варианту. Класс должен содержать не менее трех-четырех полей различных типов, конструкторы (в том числе правильные конструкторы копирования и перемещения, если это необходимо), геттеры/сеттеры.\n2. Подготовить текстовый файл `input.txt` с входными характеристиками нескольких объектов (не менее 5 строк). Перегрузить оператор потока ввода `>>` для этого класса, чтобы удобно считывать объекты из файла за один раз.\n3. Процесс работы программы:\n   - Прочитать данные из файла и добавить сгенерированные объекты в **исходный контейнер** №1 (добавлять с помощью методов последовательных контейнеров, таких как `push_back`).\n   - Напечатать исходный контейнер (вывести объекты, в нем лежащие). (Для этого обязательно перегрузить оператор потока вставки `<<` для вашего пользовательского класса).\n   - Отсортировать объекты в этом базовом контейнере согласно критерию (использовать алгоритм `std::sort` и написать собственный компаратор).\n   - Распечатать отсортированный контейнер.\n   - Скопировать данные из первого контейнера во **второй целевой контейнер** другого типа с помощью обобщенного алгоритма `std::copy`. Для целевого контейнера необходимо предварительно выделить нужное количество памяти.\n   - Распечатать целевой скопированный контейнер.\n\n## Индивидуальное задание (Вариант 26 из Таблицы 1):\n1. **Название класса:** «Автомобиль»\n   - *Характеристики (поля):* название модели, массив расходов топлива из 3-х элементов (трасса, город, смешанный), макс. скорость, мощность.\n2. **Исходный контейнер (№1):** `std::deque`\n3. **Целевой контейнер (№2):** `std::list`\n4. **Критерий сортировки:** по смешанному расходу топлива.",
  "theory": "<h2>Теория: Контейнеры и алгоритмы STL в C++</h2>\n<p>Стандартная библиотека шаблонов (STL — Standard Template Library) содержит готовые реализации самых популярных структур данных и алгоритмов. Их использование значительно упрощает жизнь программиста.</p>\n<p>В основе STL лежат три понятия: <strong>Контейнеры</strong>, <strong>Итераторы</strong> и <strong>Алгоритмы</strong>.</p>\n<h3>1. Контейнеры</h3>\n<p>Контейнер — это класс, который хранит в себе набор других объектов (элементов).</p>\n<p>В данной лабораторной мы познакомились с двумя <strong>последовательными</strong> контейнерами (хранят элементы строго в том порядке, в котором их положили):</p>\n<h4><code class=\"inline-code\">std::vector</code> (Вектор)</h4>\n<p>По сути, это <strong>динамический массив</strong>. </p>\n<ul>\n<li>Все элементы лежат в памяти друг за другом (непрерывным блоком).</li>\n<li>Выделяет память \"с запасом\". Когда запас исчерпывается, выделяет новый кусок памяти в 1.5-2 раза больше, старые данные извлекает и <strong>перемещает/копирует</strong> на новое место.</li>\n<li>Плюсы: Очень быстрый доступ по индексу (<code class=\"inline-code\">v[5]</code>). Очень быстрая вставка и удаление в конец (<code class=\"inline-code\">push_back</code>).</li>\n<li>Минусы: Дорогая вставка/удаление в начало или середину (требует сдвига всех остальных элементов). Дорогая переаллокация памяти.</li>\n</ul>\n<h4><code class=\"inline-code\">std::deque</code> (Двухсторонняя очередь, читается как \"дек\")</h4>\n<p>Вектор-подобная структура данных.</p>\n<ul>\n<li>Под капотом элементы лежат \"чанками\" (небольшими кусочками массивов), на которые есть массив указателей.</li>\n<li>Плюсы: Быстрая вставка элементов как в конец (<code class=\"inline-code\">push_back</code>), так и в начало (<code class=\"inline-code\">push_front</code>), без необходимости перемещать весь массив.</li>\n<li>Доступ по индексу чуть медленнее, чем у чистого вектора (надо сначала перескочить на нужный чанк по указателю), но все еще за O(1) и очень быстрый.</li>\n</ul>\n<h4><code class=\"inline-code\">std::list</code> (Двунаправленный список)</h4>\n<p>Классический <strong>двусвязный список</strong>.</p>\n<ul>\n<li>Каждый элемент хранит указатели на предыдущий и следующий.</li>\n<li>Плюсы: Очень быстрая вставка/удаление в произвольную позицию (за O(1), если есть итератор).</li>\n<li>Минусы: Нет доступа по индексу. Для поиска n-го элемента надо пройти по цепочке.</li>\n<li>Сортировка: У <code class=\"inline-code\">list</code> есть <strong>собственный</strong> метод <code class=\"inline-code\">list::sort()</code> (нельзя использовать <code class=\"inline-code\">std::sort</code>, т.к. он требует итераторы произвольного доступа).</li>\n</ul>\n<h3>2. Итераторы</h3>\n<p>Итератор — это \"умный указатель\" на элемент внутри контейнера STL. </p>\n<ul>\n<li><code class=\"inline-code\">begin()</code> возвращает итератор на первый элемент.</li>\n<li><code class=\"inline-code\">end()</code> возвращает итератор на элемент, следующий за последним (конец контейнера).</li>\n</ul>\n<p>Итераторы позволяют алгоритмам работать с <em>любыми</em> контейнерами одинаково.</p>\n<h3>3. Алгоритмы</h3>\n<p>Алгоритмы STL живут в заголовочном файле <code class=\"inline-code\"><algorithm></code>. Мы использовали два:</p>\n<h4>1) <code class=\"inline-code\">std::sort</code></h4>\n<pre><code>std::sort(vector.begin(), vector.end(), myCompareFunc);</code></pre>\n<p>Эта функция реализует быструю сортировку (QuickSort/IntroSort) под капотом за время O(N * log N). Чтобы функции <code class=\"inline-code\">sort</code> было понятно, <strong>по какому критерию</strong> нужно сортировать объекты нашего класса (ведь она не умеет их просто сравнивать оператором <code class=\"inline-code\"><</code>), мы передаем ей третьим параметром свою функцию-компаратор.</p>\n<p><strong>Для <code class=\"inline-code\">std::list</code></strong> алгоритм <code class=\"inline-code\">std::sort</code> нельзя использовать, т.к. <code class=\"inline-code\">std::list</code> предоставляет только двунаправленные итераторы, а не итераторы произвольного доступа. Вместо этого используется встроенный метод <code class=\"inline-code\">list::sort()</code>.</p>\n<h4>2) <code class=\"inline-code\">std::copy</code></h4>\n<pre><code>std::copy(v.begin(), v.end(), d.begin());</code></pre>\n<p>Функция берет элементы из первого контейнера (от <code class=\"inline-code\">begin</code> до <code class=\"inline-code\">end</code>) и последовательно присваивает (оператор <code class=\"inline-code\">=</code>) в другой. ВАЖНО: целевой контейнер <code class=\"inline-code\">d</code> должен заранее иметь нужный размер для вставки, иначе произойдет переполнение (ведь copy не выделяет память, а просто перезаписывает ячейки). </p>\n<p>(Или можно использовать адаптеры вставки <code class=\"inline-code\">std::back_inserter(d)</code>).</p>\n<h3>4. Конструктор копирования vs перемещения</h3>\n<p>В нашем классе есть поля <code class=\"inline-code\">std::string</code>. Класс <code class=\"inline-code\">std::string</code> хранит текст динамически.</p>\n<ul>\n<li>Если вызовется <strong>конструктор копирования</strong>, текст скопируется символ-за-символом. Это дорогая операция.</li>\n<li>Если мы помещаем в класс временный объект <code class=\"inline-code\">string</code>, выгоднее вызвать <strong>конструктор перемещения</strong> (<code class=\"inline-code\">&&</code>), который просто сворует указатель на этот текст (заберет себе ownership), оставив временный объект пустым. Обмен указателями работает за мгновенное O(1). Это крайне полезно, когда контейнер <code class=\"inline-code\">std::vector</code> переполняется и решает переложить все свои данные в новую, расширенную память. Вектор воспользуется вашими конструкторами перемещения!</li>\n</ul>",
  "theoryRaw": "# Теория: Контейнеры и алгоритмы STL в C++\n\nСтандартная библиотека шаблонов (STL — Standard Template Library) содержит готовые реализации самых популярных структур данных и алгоритмов. Их использование значительно упрощает жизнь программиста.\n\nВ основе STL лежат три понятия: **Контейнеры**, **Итераторы** и **Алгоритмы**.\n\n## 1. Контейнеры\nКонтейнер — это класс, который хранит в себе набор других объектов (элементов).\n\nВ данной лабораторной мы познакомились с двумя **последовательными** контейнерами (хранят элементы строго в том порядке, в котором их положили):\n\n### `std::vector` (Вектор)\nПо сути, это **динамический массив**. \n- Все элементы лежат в памяти друг за другом (непрерывным блоком).\n- Выделяет память \"с запасом\". Когда запас исчерпывается, выделяет новый кусок памяти в 1.5-2 раза больше, старые данные извлекает и **перемещает/копирует** на новое место.\n- Плюсы: Очень быстрый доступ по индексу (`v[5]`). Очень быстрая вставка и удаление в конец (`push_back`).\n- Минусы: Дорогая вставка/удаление в начало или середину (требует сдвига всех остальных элементов). Дорогая переаллокация памяти.\n\n### `std::deque` (Двухсторонняя очередь, читается как \"дек\")\nВектор-подобная структура данных.\n- Под капотом элементы лежат \"чанками\" (небольшими кусочками массивов), на которые есть массив указателей.\n- Плюсы: Быстрая вставка элементов как в конец (`push_back`), так и в начало (`push_front`), без необходимости перемещать весь массив.\n- Доступ по индексу чуть медленнее, чем у чистого вектора (надо сначала перескочить на нужный чанк по указателю), но все еще за O(1) и очень быстрый.\n\n### `std::list` (Двунаправленный список)\nКлассический **двусвязный список**.\n- Каждый элемент хранит указатели на предыдущий и следующий.\n- Плюсы: Очень быстрая вставка/удаление в произвольную позицию (за O(1), если есть итератор).\n- Минусы: Нет доступа по индексу. Для поиска n-го элемента надо пройти по цепочке.\n- Сортировка: У `list` есть **собственный** метод `list::sort()` (нельзя использовать `std::sort`, т.к. он требует итераторы произвольного доступа).\n\n## 2. Итераторы\nИтератор — это \"умный указатель\" на элемент внутри контейнера STL. \n- `begin()` возвращает итератор на первый элемент.\n- `end()` возвращает итератор на элемент, следующий за последним (конец контейнера).\nИтераторы позволяют алгоритмам работать с *любыми* контейнерами одинаково.\n\n## 3. Алгоритмы\nАлгоритмы STL живут в заголовочном файле `<algorithm>`. Мы использовали два:\n\n### 1) `std::sort`\n```cpp\nstd::sort(vector.begin(), vector.end(), myCompareFunc);\n```\nЭта функция реализует быструю сортировку (QuickSort/IntroSort) под капотом за время O(N * log N). Чтобы функции `sort` было понятно, **по какому критерию** нужно сортировать объекты нашего класса (ведь она не умеет их просто сравнивать оператором `<`), мы передаем ей третьим параметром свою функцию-компаратор.\n\n**Для `std::list`** алгоритм `std::sort` нельзя использовать, т.к. `std::list` предоставляет только двунаправленные итераторы, а не итераторы произвольного доступа. Вместо этого используется встроенный метод `list::sort()`.\n\n### 2) `std::copy`\n```cpp\nstd::copy(v.begin(), v.end(), d.begin());\n```\nФункция берет элементы из первого контейнера (от `begin` до `end`) и последовательно присваивает (оператор `=`) в другой. ВАЖНО: целевой контейнер `d` должен заранее иметь нужный размер для вставки, иначе произойдет переполнение (ведь copy не выделяет память, а просто перезаписывает ячейки). \n(Или можно использовать адаптеры вставки `std::back_inserter(d)`).\n\n## 4. Конструктор копирования vs перемещения\nВ нашем классе есть поля `std::string`. Класс `std::string` хранит текст динамически.\n- Если вызовется **конструктор копирования**, текст скопируется символ-за-символом. Это дорогая операция.\n- Если мы помещаем в класс временный объект `string`, выгоднее вызвать **конструктор перемещения** (`&&`), который просто сворует указатель на этот текст (заберет себе ownership), оставив временный объект пустым. Обмен указателями работает за мгновенное O(1). Это крайне полезно, когда контейнер `std::vector` переполняется и решает переложить все свои данные в новую, расширенную память. Вектор воспользуется вашими конструкторами перемещения!"
}