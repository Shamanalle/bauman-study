{
  "variant": 21,
  "lab": 3,
  "code": "#include <iostream>\n#include <cmath>\n\nusing namespace std;\n\nclass RealNumber {\nprotected: \n    double value; \n\npublic:\n    RealNumber(double val) : value(val) {}\n\n    virtual double absValue() const {\n        return abs(value);\n    }\n\n    virtual void print() const {\n        cout << \"Вещественное число:\" << endl;\n        cout << \"  Значение: \" << value << endl;\n        cout << \"  Модуль: \" << absValue() << endl;\n    }\n    \n    virtual ~RealNumber() {}\n};\n\nclass ComplexNumber : public RealNumber {\nprivate:\n    double imaginary; \n\npublic:\n    ComplexNumber(double real, double imag) \n        : RealNumber(real), imaginary(imag) {} \n\n    double absValue() const override {\n        return sqrt(value * value + imaginary * imaginary);\n    }\n\n    void print() const override {\n        cout << \"Комплексное число:\" << endl;\n        cout << \"  Действительная часть: \" << value << endl;\n        cout << \"  Мнимая часть: \" << imaginary << endl;\n        cout << \"  Формат: \" << value << (imaginary >= 0 ? \" + \" : \" - \") << abs(imaginary) << \"i\" << endl;\n        cout << \"  Модуль: \" << absValue() << endl;\n    }\n};\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    cout << \"=== Создание объектов ===\" << endl;\n    RealNumber r(-5.5);\n    ComplexNumber c(3.0, -4.0);\n\n    cout << \"\\n=== Прямой вызов функций (объекты) ===\" << endl;\n    r.print();\n    cout << endl;\n    c.print();\n\n    cout << \"\\n=== 1. СТАТИЧЕСКИЙ ПОЛИМОРФИЗМ (Статическое связывание) ===\" << endl;\n\n    RealNumber* ptr_r1 = &r;\n    RealNumber* ptr_r2 = &c;\n\n    cout << \"\\n[Явный вызов базового метода] ptr_r2->RealNumber::print():\" << endl;\n    ptr_r2->RealNumber::print();\n\n    cout << \"\\n=== 2. ДИНАМИЧЕСКИЙ ПОЛИМОРФИЗМ (Динамическое связывание) ===\" << endl;\n\n    cout << \"\\nВызов ptr_r1->print() (указывает на RealNumber):\" << endl;\n    ptr_r1->print();\n\n    cout << \"\\nВызов ptr_r2->print() (указывает на ComplexNumber):\" << endl;\n    ptr_r2->print();\n\n    return 0;\n}",
  "codeHtml": "<pre><code>#include &lt;iostream&gt;\n#include &lt;cmath&gt;\n\nusing namespace std;\n\nclass RealNumber {\nprotected: \n    double value; \n\npublic:\n    RealNumber(double val) : value(val) {}\n\n    virtual double absValue() const {\n        return abs(value);\n    }\n\n    virtual void print() const {\n        cout &lt;&lt; \"Вещественное число:\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Значение: \" &lt;&lt; value &lt;&lt; endl;\n        cout &lt;&lt; \"  Модуль: \" &lt;&lt; absValue() &lt;&lt; endl;\n    }\n    \n    virtual ~RealNumber() {}\n};\n\nclass ComplexNumber : public RealNumber {\nprivate:\n    double imaginary; \n\npublic:\n    ComplexNumber(double real, double imag) \n        : RealNumber(real), imaginary(imag) {} \n\n    double absValue() const override {\n        return sqrt(value * value + imaginary * imaginary);\n    }\n\n    void print() const override {\n        cout &lt;&lt; \"Комплексное число:\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Действительная часть: \" &lt;&lt; value &lt;&lt; endl;\n        cout &lt;&lt; \"  Мнимая часть: \" &lt;&lt; imaginary &lt;&lt; endl;\n        cout &lt;&lt; \"  Формат: \" &lt;&lt; value &lt;&lt; (imaginary &gt;= 0 ? \" + \" : \" - \") &lt;&lt; abs(imaginary) &lt;&lt; \"i\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Модуль: \" &lt;&lt; absValue() &lt;&lt; endl;\n    }\n};\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    cout &lt;&lt; \"=== Создание объектов ===\" &lt;&lt; endl;\n    RealNumber r(-5.5);\n    ComplexNumber c(3.0, -4.0);\n\n    cout &lt;&lt; \"\\n=== Прямой вызов функций (объекты) ===\" &lt;&lt; endl;\n    r.print();\n    cout &lt;&lt; endl;\n    c.print();\n\n    cout &lt;&lt; \"\\n=== 1. СТАТИЧЕСКИЙ ПОЛИМОРФИЗМ (Статическое связывание) ===\" &lt;&lt; endl;\n\n    RealNumber* ptr_r1 = &amp;r;\n    RealNumber* ptr_r2 = &amp;c;\n\n    cout &lt;&lt; \"\\n[Явный вызов базового метода] ptr_r2-&gt;RealNumber::print():\" &lt;&lt; endl;\n    ptr_r2-&gt;RealNumber::print();\n\n    cout &lt;&lt; \"\\n=== 2. ДИНАМИЧЕСКИЙ ПОЛИМОРФИЗМ (Динамическое связывание) ===\" &lt;&lt; endl;\n\n    cout &lt;&lt; \"\\nВызов ptr_r1-&gt;print() (указывает на RealNumber):\" &lt;&lt; endl;\n    ptr_r1-&gt;print();\n\n    cout &lt;&lt; \"\\nВызов ptr_r2-&gt;print() (указывает на ComplexNumber):\" &lt;&lt; endl;\n    ptr_r2-&gt;print();\n\n    return 0;\n}</code></pre>",
  "explanation": "<h2>Разбор кода: Лабораторная работа 3 (Вариант 21)</h2>\n<p>Ниже представлен полный исходный код из <code class=\"inline-code\">main.cpp</code> для удобного ознакомления. </p>\n<p>В последующих разделах этот код разбит на логические блоки с детальным объяснением конструкций наследования и полиморфизма.</p>\n<h3>Полный исходный код программы</h3>\n<pre><code>#include &lt;iostream&gt;\n#include &lt;cmath&gt;\n\nusing namespace std;\n\nclass RealNumber {\nprotected: \n    double value; \n\npublic:\n    RealNumber(double val) : value(val) {}\n\n    virtual double absValue() const {\n        return abs(value);\n    }\n\n    virtual void print() const {\n        cout &lt;&lt; \"Вещественное число:\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Значение: \" &lt;&lt; value &lt;&lt; endl;\n        cout &lt;&lt; \"  Модуль: \" &lt;&lt; absValue() &lt;&lt; endl;\n    }\n    \n    virtual ~RealNumber() {}\n};\n\nclass ComplexNumber : public RealNumber {\nprivate:\n    double imaginary; \n\npublic:\n    ComplexNumber(double real, double imag) \n        : RealNumber(real), imaginary(imag) {} \n\n    double absValue() const override {\n        return sqrt(value * value + imaginary * imaginary);\n    }\n\n    void print() const override {\n        cout &lt;&lt; \"Комплексное число:\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Действительная часть: \" &lt;&lt; value &lt;&lt; endl;\n        cout &lt;&lt; \"  Мнимая часть: \" &lt;&lt; imaginary &lt;&lt; endl;\n        cout &lt;&lt; \"  Формат: \" &lt;&lt; value &lt;&lt; (imaginary &gt;= 0 ? \" + \" : \" - \") &lt;&lt; abs(imaginary) &lt;&lt; \"i\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Модуль: \" &lt;&lt; absValue() &lt;&lt; endl;\n    }\n};\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    cout &lt;&lt; \"=== Создание объектов ===\" &lt;&lt; endl;\n    RealNumber r(-5.5);\n    ComplexNumber c(3.0, -4.0);\n\n    cout &lt;&lt; \"\\n=== Прямой вызов функций (объекты) ===\" &lt;&lt; endl;\n    r.print();\n    cout &lt;&lt; endl;\n    c.print();\n\n    cout &lt;&lt; \"\\n=== 1. СТАТИЧЕСКИЙ ПОЛИМОРФИЗМ (Статическое связывание) ===\" &lt;&lt; endl;\n\n    RealNumber* ptr_r1 = &amp;r;\n    RealNumber* ptr_r2 = &amp;c;\n\n    cout &lt;&lt; \"\\n[Явный вызов базового метода] ptr_r2-&gt;RealNumber::print():\" &lt;&lt; endl;\n    ptr_r2-&gt;RealNumber::print();\n\n    cout &lt;&lt; \"\\n=== 2. ДИНАМИЧЕСКИЙ ПОЛИМОРФИЗМ (Динамическое связывание) ===\" &lt;&lt; endl;\n\n    cout &lt;&lt; \"\\nВызов ptr_r1-&gt;print() (указывает на RealNumber):\" &lt;&lt; endl;\n    ptr_r1-&gt;print();\n\n    cout &lt;&lt; \"\\nВызов ptr_r2-&gt;print() (указывает на ComplexNumber):\" &lt;&lt; endl;\n    ptr_r2-&gt;print();\n\n    return 0;\n}\n</code></pre>\n<hr>\n<h3>1. Базовый Класс <code class=\"inline-code\">RealNumber</code></h3>\n<pre><code>class RealNumber {{\nprotected: \n    double value; </code></pre>\n<p>Ключевое слово <strong><code class=\"inline-code\">protected</code></strong> (защищенный). Если бы мы написали <code class=\"inline-code\">private</code>, то дочерние классы не смогли бы увидеть поле <code class=\"inline-code\">value</code>. <code class=\"inline-code\">protected</code> решает эту проблему: из внешнего кода (в функции <code class=\"inline-code\">main()</code>) написать <code class=\"inline-code\">r.value</code> нельзя, но производный класс им пользоваться может.</p>\n<pre><code>    RealNumber(double val) : value(val) {{}}</code></pre>\n<p>Конструктор со <strong>Списком инициализации</strong>. Запись <code class=\"inline-code\">: value(val)</code> делает то же самое, что и <code class=\"inline-code\">value = val;</code> внутри скобок <code class=\"inline-code\">{{}}</code>, но является более правильной и быстрой в C++.</p>\n<pre><code>    virtual double absValue() const {{\n        return abs(value);\n    }}</code></pre>\n<ul>\n<li>Метод вычисляет модуль (абсолютное значение) вещественного числа с помощью <code class=\"inline-code\">abs()</code> из <code class=\"inline-code\"><cmath></code>.</li>\n<li><strong><code class=\"inline-code\">virtual</code></strong> — это самое главное слово в теме полиморфизма. Оно указывает компилятору, что если кто-то позже унаследует этот класс, он <strong>может переопределить</strong> (<em>override</em>) эту функцию.</li>\n</ul>\n<pre><code>    virtual void print() const {{\n        cout &lt;&lt; \"Вещественное число:\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Значение: \" &lt;&lt; value &lt;&lt; endl;\n        cout &lt;&lt; \"  Модуль: \" &lt;&lt; absValue() &lt;&lt; endl;\n    }}</code></pre>\n<ul>\n<li>Виртуальный метод печати. Внутри вызывается <code class=\"inline-code\">absValue()</code> — и поскольку <code class=\"inline-code\">absValue()</code> тоже виртуальный, при вызове через указатель будет вызвана правильная версия (<code class=\"inline-code\">abs(value)</code> для вещественного или <code class=\"inline-code\">sqrt(re² + im²)</code> для комплексного).</li>\n</ul>\n<pre><code>    virtual ~RealNumber() {{}}</code></pre>\n<ul>\n<li>Обязательно наличие <strong>виртуального деструктора</strong>. Если через указатель родительского класса удалить класс-наследник, виртуальный деструктор гарантирует, что удаление начнется с дочерней части, а не только с родительской (предотвращая утечку памяти).</li>\n</ul>\n<hr>\n<h3>2. Производный класс <code class=\"inline-code\">ComplexNumber</code></h3>\n<pre><code>class ComplexNumber : public RealNumber {{</code></pre>\n<p><strong><code class=\"inline-code\">public RealNumber</code></strong> — мы \"публично наследуемся\". Это означает, что методы <code class=\"inline-code\">print</code> и <code class=\"inline-code\">absValue</code> останутся публичными и в комплексном числе. Комплексное число — это расширенная версия вещественного числа (появляется поле <code class=\"inline-code\">imaginary</code>).</p>\n<pre><code>private:\n    double imaginary;</code></pre>\n<ul>\n<li>Дополнительное поле <code class=\"inline-code\">imaginary</code> — мнимая часть комплексного числа. Объявлено как <code class=\"inline-code\">private</code>.</li>\n</ul>\n<pre><code>    ComplexNumber(double real, double imag) \n        : RealNumber(real), imaginary(imag) {{}} </code></pre>\n<p>Конструктор наследника <em>обязан</em> \"собрать\" родительскую часть. Поэтому мы <strong>явно вызываем родительский конструктор</strong> <code class=\"inline-code\">RealNumber(real)</code> в списке инициализации. Без этого вызова компилятор попытается вызвать <code class=\"inline-code\">RealNumber()</code> без аргументов, которого нет.</p>\n<pre><code>    double absValue() const override {{\n        return sqrt(value * value + imaginary * imaginary);\n    }}</code></pre>\n<ul>\n<li>Переопределённый метод модуля. Для комплексного числа z = a + bi модуль |z| = √(a² + b²).</li>\n<li>Поле <code class=\"inline-code\">value</code> доступно напрямую, т.к. оно <code class=\"inline-code\">protected</code> в базовом классе.</li>\n</ul>\n<pre><code>    void print() const override {{\n        cout &lt;&lt; \"Комплексное число:\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Действительная часть: \" &lt;&lt; value &lt;&lt; endl;\n        cout &lt;&lt; \"  Мнимая часть: \" &lt;&lt; imaginary &lt;&lt; endl;\n        cout &lt;&lt; \"  Формат: \" &lt;&lt; value &lt;&lt; (imaginary &gt;= 0 ? \" + \" : \" - \") &lt;&lt; abs(imaginary) &lt;&lt; \"i\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Модуль: \" &lt;&lt; absValue() &lt;&lt; endl;\n    }}</code></pre>\n<p><strong><code class=\"inline-code\">override</code></strong> (опциональное, но полезное слово появилось в C++11) сообщает компилятору: \"Я хочу переопределить виртуальную функцию родителя. Проверь, чтобы имена и параметры функций точно совпадали\".</p>\n<ul>\n<li>Тернарный оператор <code class=\"inline-code\">(imaginary >= 0 ? \" + \" : \" - \")</code> выбирает знак перед мнимой частью для правильного отображения форматов вроде <code class=\"inline-code\">3 + 4i</code> или <code class=\"inline-code\">3 - 4i</code>.</li>\n</ul>\n<hr>\n<h3>3. Полиморфизм в <code class=\"inline-code\">main</code></h3>\n<pre><code>    RealNumber r(-5.5);\n    ComplexNumber c(3.0, -4.0);</code></pre>\n<ul>\n<li>Создаются два объекта: вещественное число -5.5 (модуль 5.5) и комплексное число 3 - 4i (модуль 5.0).</li>\n</ul>\n<pre><code>    RealNumber* ptr_r2 = &amp;c;</code></pre>\n<p>Это базовая фишка С++: <strong>указатель базового типа может безопасно указывать на объект любого унаследованного типа</strong>. Для компилятора тип указателя — это <code class=\"inline-code\">RealNumber*</code> (родитель). Но в оперативной памяти по этому адресу физически лежит <code class=\"inline-code\">ComplexNumber</code> (наследник).</p>\n<h4>Статическое связывание</h4>\n<pre><code>    ptr_r2-&gt;RealNumber::print(); </code></pre>\n<p>Мы заставляем указатель \"вызвать именно родительскую функцию\" в обход всей динамики. Это статическое связывание (жесткая привязка во время компиляции). Будет выведено «Вещественное число» с модулем <code class=\"inline-code\">abs(3.0) = 3</code>, то есть мнимая часть <strong>игнорируется</strong>.</p>\n<h4>Динамическое связывание</h4>\n<pre><code>    ptr_r2-&gt;print();</code></pre>\n<p>Поскольку <code class=\"inline-code\">print()</code> помечен словом <code class=\"inline-code\">virtual</code>, компилятор не привязывает вызов жестко. Во время работы программы она идет по указателю <code class=\"inline-code\">ptr_r2</code>, заглядывает внутрь объекта <code class=\"inline-code\">c</code>, находит там \"Таблицу виртуальных функций\" и видит: ага, реальный тип объекта — это Комплексное число! Значит, нужно вызвать функцию <code class=\"inline-code\">ComplexNumber::print()</code>. Это и есть динамический полиморфизм.</p>",
  "explanationRaw": "# Разбор кода: Лабораторная работа 3 (Вариант 21)\n\nНиже представлен полный исходный код из `main.cpp` для удобного ознакомления. \nВ последующих разделах этот код разбит на логические блоки с детальным объяснением конструкций наследования и полиморфизма.\n\n## Полный исходный код программы\n```cpp\n#include <iostream>\n#include <cmath>\n\nusing namespace std;\n\nclass RealNumber {\nprotected: \n    double value; \n\npublic:\n    RealNumber(double val) : value(val) {}\n\n    virtual double absValue() const {\n        return abs(value);\n    }\n\n    virtual void print() const {\n        cout << \"Вещественное число:\" << endl;\n        cout << \"  Значение: \" << value << endl;\n        cout << \"  Модуль: \" << absValue() << endl;\n    }\n    \n    virtual ~RealNumber() {}\n};\n\nclass ComplexNumber : public RealNumber {\nprivate:\n    double imaginary; \n\npublic:\n    ComplexNumber(double real, double imag) \n        : RealNumber(real), imaginary(imag) {} \n\n    double absValue() const override {\n        return sqrt(value * value + imaginary * imaginary);\n    }\n\n    void print() const override {\n        cout << \"Комплексное число:\" << endl;\n        cout << \"  Действительная часть: \" << value << endl;\n        cout << \"  Мнимая часть: \" << imaginary << endl;\n        cout << \"  Формат: \" << value << (imaginary >= 0 ? \" + \" : \" - \") << abs(imaginary) << \"i\" << endl;\n        cout << \"  Модуль: \" << absValue() << endl;\n    }\n};\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    cout << \"=== Создание объектов ===\" << endl;\n    RealNumber r(-5.5);\n    ComplexNumber c(3.0, -4.0);\n\n    cout << \"\\n=== Прямой вызов функций (объекты) ===\" << endl;\n    r.print();\n    cout << endl;\n    c.print();\n\n    cout << \"\\n=== 1. СТАТИЧЕСКИЙ ПОЛИМОРФИЗМ (Статическое связывание) ===\" << endl;\n\n    RealNumber* ptr_r1 = &r;\n    RealNumber* ptr_r2 = &c;\n\n    cout << \"\\n[Явный вызов базового метода] ptr_r2->RealNumber::print():\" << endl;\n    ptr_r2->RealNumber::print();\n\n    cout << \"\\n=== 2. ДИНАМИЧЕСКИЙ ПОЛИМОРФИЗМ (Динамическое связывание) ===\" << endl;\n\n    cout << \"\\nВызов ptr_r1->print() (указывает на RealNumber):\" << endl;\n    ptr_r1->print();\n\n    cout << \"\\nВызов ptr_r2->print() (указывает на ComplexNumber):\" << endl;\n    ptr_r2->print();\n\n    return 0;\n}\n\n```\n\n---\n\n## 1. Базовый Класс `RealNumber`\n```cpp\nclass RealNumber {{\nprotected: \n    double value; \n```\nКлючевое слово **`protected`** (защищенный). Если бы мы написали `private`, то дочерние классы не смогли бы увидеть поле `value`. `protected` решает эту проблему: из внешнего кода (в функции `main()`) написать `r.value` нельзя, но производный класс им пользоваться может.\n\n```cpp\n    RealNumber(double val) : value(val) {{}}\n```\nКонструктор со **Списком инициализации**. Запись `: value(val)` делает то же самое, что и `value = val;` внутри скобок `{{}}`, но является более правильной и быстрой в C++.\n\n```cpp\n    virtual double absValue() const {{\n        return abs(value);\n    }}\n```\n- Метод вычисляет модуль (абсолютное значение) вещественного числа с помощью `abs()` из `<cmath>`.\n- **`virtual`** — это самое главное слово в теме полиморфизма. Оно указывает компилятору, что если кто-то позже унаследует этот класс, он **может переопределить** (*override*) эту функцию.\n\n```cpp\n    virtual void print() const {{\n        cout << \"Вещественное число:\" << endl;\n        cout << \"  Значение: \" << value << endl;\n        cout << \"  Модуль: \" << absValue() << endl;\n    }}\n```\n- Виртуальный метод печати. Внутри вызывается `absValue()` — и поскольку `absValue()` тоже виртуальный, при вызове через указатель будет вызвана правильная версия (`abs(value)` для вещественного или `sqrt(re² + im²)` для комплексного).\n\n```cpp\n    virtual ~RealNumber() {{}}\n```\n- Обязательно наличие **виртуального деструктора**. Если через указатель родительского класса удалить класс-наследник, виртуальный деструктор гарантирует, что удаление начнется с дочерней части, а не только с родительской (предотвращая утечку памяти).\n\n---\n\n## 2. Производный класс `ComplexNumber`\n```cpp\nclass ComplexNumber : public RealNumber {{\n```\n**`public RealNumber`** — мы \"публично наследуемся\". Это означает, что методы `print` и `absValue` останутся публичными и в комплексном числе. Комплексное число — это расширенная версия вещественного числа (появляется поле `imaginary`).\n\n```cpp\nprivate:\n    double imaginary;\n```\n- Дополнительное поле `imaginary` — мнимая часть комплексного числа. Объявлено как `private`.\n\n```cpp\n    ComplexNumber(double real, double imag) \n        : RealNumber(real), imaginary(imag) {{}} \n```\nКонструктор наследника *обязан* \"собрать\" родительскую часть. Поэтому мы **явно вызываем родительский конструктор** `RealNumber(real)` в списке инициализации. Без этого вызова компилятор попытается вызвать `RealNumber()` без аргументов, которого нет.\n\n```cpp\n    double absValue() const override {{\n        return sqrt(value * value + imaginary * imaginary);\n    }}\n```\n- Переопределённый метод модуля. Для комплексного числа z = a + bi модуль |z| = √(a² + b²).\n- Поле `value` доступно напрямую, т.к. оно `protected` в базовом классе.\n\n```cpp\n    void print() const override {{\n        cout << \"Комплексное число:\" << endl;\n        cout << \"  Действительная часть: \" << value << endl;\n        cout << \"  Мнимая часть: \" << imaginary << endl;\n        cout << \"  Формат: \" << value << (imaginary >= 0 ? \" + \" : \" - \") << abs(imaginary) << \"i\" << endl;\n        cout << \"  Модуль: \" << absValue() << endl;\n    }}\n```\n**`override`** (опциональное, но полезное слово появилось в C++11) сообщает компилятору: \"Я хочу переопределить виртуальную функцию родителя. Проверь, чтобы имена и параметры функций точно совпадали\".\n- Тернарный оператор `(imaginary >= 0 ? \" + \" : \" - \")` выбирает знак перед мнимой частью для правильного отображения форматов вроде `3 + 4i` или `3 - 4i`.\n\n---\n\n## 3. Полиморфизм в `main`\n\n```cpp\n    RealNumber r(-5.5);\n    ComplexNumber c(3.0, -4.0);\n```\n- Создаются два объекта: вещественное число -5.5 (модуль 5.5) и комплексное число 3 - 4i (модуль 5.0).\n\n```cpp\n    RealNumber* ptr_r2 = &c;\n```\nЭто базовая фишка С++: **указатель базового типа может безопасно указывать на объект любого унаследованного типа**. Для компилятора тип указателя — это `RealNumber*` (родитель). Но в оперативной памяти по этому адресу физически лежит `ComplexNumber` (наследник).\n\n### Статическое связывание\n```cpp\n    ptr_r2->RealNumber::print(); \n```\nМы заставляем указатель \"вызвать именно родительскую функцию\" в обход всей динамики. Это статическое связывание (жесткая привязка во время компиляции). Будет выведено «Вещественное число» с модулем `abs(3.0) = 3`, то есть мнимая часть **игнорируется**.\n\n### Динамическое связывание\n```cpp\n    ptr_r2->print();\n```\nПоскольку `print()` помечен словом `virtual`, компилятор не привязывает вызов жестко. Во время работы программы она идет по указателю `ptr_r2`, заглядывает внутрь объекта `c`, находит там \"Таблицу виртуальных функций\" и видит: ага, реальный тип объекта — это Комплексное число! Значит, нужно вызвать функцию `ComplexNumber::print()`. Это и есть динамический полиморфизм.",
  "condition": "<h2>Условие лабораторной работы №3 (Вариант 21)</h2>\n<h3>Тема: Наследование классов и Полиморфизм</h3>\n<p><strong>Цель работы:</strong></p>\n<p>Изучить отношения между базовыми и производными классами в C++, а также понять механизмы реализации раннего (статического) и позднего (динамического) связывания вызовов функций.</p>\n<h3>Общее задание:</h3>\n<p>1. Создать иерархию из двух классов (базового родителя и класса-наследника).</p>\n<p>2. Класс-наследник должен использовать конструктор базового класса (передавая ему параметры через список инициализации конструктора).</p>\n<p>3. В базовом классе определить виртуальные (virtual) функции. Переопределить эти функции (override) в наследнике.</p>\n<p>4. Добавить в функции-члены вывод информации о том, для какого класса (базового или производного) произошел их вызов, и вывести значения всех полей объекта. Причем внутри переопределенной функции в наследнике в первую очередь должна вызываться функция из базового класса.</p>\n<p>5. Инкапсулировать поля базового класса таким образом, чтобы защитить их от внешнего доступа (из метода main), но разрешить прямой доступ из производного класса-наследника (использовать модификатор <code class=\"inline-code\">protected</code>).</p>\n<p>6. В функции <code class=\"inline-code\">main</code> продемонстрировать разницу между <strong>статическим</strong> (ранним) и <strong>динамическим</strong> (поздним) связыванием:</p>\n<ul>\n<li>Создать указатели базового типа <code class=\"inline-code\">Base*</code>.</li>\n<li>Присвоить им адреса объектов производного типа (<code class=\"inline-code\">Base* b = new Derived()</code>).</li>\n<li>Осуществить вызовы виртуальных и невиртуальных методов и объяснить разницу.</li>\n</ul>\n<h3>Индивидуальное задание:</h3>\n<ul>\n<li><strong>Базовый класс:</strong> «Вещественное число» (<code class=\"inline-code\">RealNumber</code>)</li>\n<li><em>Поля (protected):</em> значение числа (double).</li>\n<li><em>Виртуальные методы:</em></li>\n</ul>\n<p>    1. Расчет модуля (абсолютного значения).</p>\n<p>    2. Печать параметров.</p>\n<ul>\n<li><strong>Производный класс:</strong> «Комплексное число» (<code class=\"inline-code\">ComplexNumber</code>)</li>\n<li><em>Дополнительное поле:</em> мнимая часть комплексного числа.</li>\n<li><em>Переопределение методов:</em></li>\n</ul>\n<p>    1. Расчет модуля по формуле для комплексного числа (√(re² + im²)).</p>\n<p>    2. Вывод комплексного числа в математическом формате (a + bi).</p>",
  "conditionRaw": "# Условие лабораторной работы №3 (Вариант 21)\n\n## Тема: Наследование классов и Полиморфизм\n\n**Цель работы:**\nИзучить отношения между базовыми и производными классами в C++, а также понять механизмы реализации раннего (статического) и позднего (динамического) связывания вызовов функций.\n\n## Общее задание:\n1. Создать иерархию из двух классов (базового родителя и класса-наследника).\n2. Класс-наследник должен использовать конструктор базового класса (передавая ему параметры через список инициализации конструктора).\n3. В базовом классе определить виртуальные (virtual) функции. Переопределить эти функции (override) в наследнике.\n4. Добавить в функции-члены вывод информации о том, для какого класса (базового или производного) произошел их вызов, и вывести значения всех полей объекта. Причем внутри переопределенной функции в наследнике в первую очередь должна вызываться функция из базового класса.\n5. Инкапсулировать поля базового класса таким образом, чтобы защитить их от внешнего доступа (из метода main), но разрешить прямой доступ из производного класса-наследника (использовать модификатор `protected`).\n6. В функции `main` продемонстрировать разницу между **статическим** (ранним) и **динамическим** (поздним) связыванием:\n   - Создать указатели базового типа `Base*`.\n   - Присвоить им адреса объектов производного типа (`Base* b = new Derived()`).\n   - Осуществить вызовы виртуальных и невиртуальных методов и объяснить разницу.\n\n## Индивидуальное задание:\n- **Базовый класс:** «Вещественное число» (`RealNumber`)\n  - *Поля (protected):* значение числа (double).\n  - *Виртуальные методы:*\n    1. Расчет модуля (абсолютного значения).\n    2. Печать параметров.\n- **Производный класс:** «Комплексное число» (`ComplexNumber`)\n  - *Дополнительное поле:* мнимая часть комплексного числа.\n  - *Переопределение методов:*\n    1. Расчет модуля по формуле для комплексного числа (√(re² + im²)).\n    2. Вывод комплексного числа в математическом формате (a + bi).",
  "theory": "<h2>Теория: Наследование и Полиморфизм в C++</h2>\n<p>В данной лабораторной работе изучаются два ключевых принципа ООП: наследование и полиморфизм.</p>\n<h3>1. Наследование</h3>\n<p>Наследование позволяет создать новый класс (наследник / производный) на основе уже существующего (базового родителя). Класс-наследник получает (наследует) все поля и методы базового класса и может добавлять свои собственные.</p>\n<pre><code>class RealNumber { ... };\nclass ComplexNumber : public RealNumber { ... }; </code></pre>\n<p>Слово <code class=\"inline-code\">public</code> перед названием родителя означает \"публичное наследование\". Это означает, что <code class=\"inline-code\">public</code> члены родителя остаются <code class=\"inline-code\">public</code> в наследнике, а <code class=\"inline-code\">protected</code> остаются <code class=\"inline-code\">protected</code>.</p>\n<h4>Модификатор <code class=\"inline-code\">protected</code></h4>\n<p>В ЛР1 мы использовали <code class=\"inline-code\">private</code> для сокрытия полей. Но <code class=\"inline-code\">private</code> поля недоступны даже классам-наследникам. Чтобы поле нельзя было изменить извне (<code class=\"inline-code\">main</code>), но можно было использовать внутри наследников, применяется спецификатор <code class=\"inline-code\">protected</code>. Именно поэтому поля базового класса объявляются как <code class=\"inline-code\">protected</code>.</p>\n<h4>Вызов конструктора родителя</h4>\n<p>Конструктор наследника <strong>обязан</strong> инициализировать часть объекта, которая досталась ему от базового класса. Делается это в списке инициализации:</p>\n<pre><code>ComplexNumber(double real, double imag) : RealNumber(real) {\n    imaginary = imag;\n}</code></pre>\n<h3>2. Полиморфизм</h3>\n<p>Полиморфизм — это свойство системы использовать объекты с одинаковым интерфейсом без информации о типе и внутренней структуре объекта (возможность \"функции работать по-разному\" для разных объектов).</p>\n<h4>Статический полиморфизм (Статическое связывание)</h4>\n<p>Связывание вызова функции с её телом происходит на этапе <strong>компиляции</strong> (раннее связывание).</p>\n<p>Примеры:</p>\n<p>1. Перегрузка функций (две функции с одним именем, но разными типами аргументов).</p>\n<p>2. Вызов обычного не-виртуального метода через указатель базового типа:</p>\n<p>   ```cpp</p>\n<p>   Base* ptr = &derivedObj;</p>\n<p>   ptr->Base::method(); // Всегда вызовет метод базового класса</p>\n<p>   ```</p>\n<h4>Динамический полиморфизм (Динамическое связывание)</h4>\n<p>Связывание происходит на этапе <strong>выполнения</strong> (позднее связывание).</p>\n<p>Достигается за счет использования ключевого слова <code class=\"inline-code\">virtual</code> перед методом в базовом классе.</p>\n<p>Как это работает:</p>\n<p>1. Когда компилятор видит модификатор <code class=\"inline-code\">virtual</code>, он создает специальную невидимую <strong>таблицу виртуальных функций (vtable)</strong> для этого класса.</p>\n<p>2. В объект незаметно добавляется указатель на эту таблицу (<code class=\"inline-code\">vptr</code>).</p>\n<p>3. При вызове виртуального метода через родительский указатель (<code class=\"inline-code\">ptr->print()</code>), программа сначала идет по указателю <code class=\"inline-code\">vptr</code> в таблицу и находит <strong>правильный</strong> адрес функции, который соответствует <em>реальному</em> типу объекта, лежащему в памяти.</p>\n<pre><code>Base* ptr = &amp;derivedObj; // Указатель базовый, но объект наследника\nptr-&gt;print(); // Вызовет Derived::print(), а не базовую функцию!</code></pre>\n<p>Это самая мощная архитектурная особенность С++, позволяющая писать универсальный код для обработки разных типов данных через их общий базовый интерфейс.</p>",
  "theoryRaw": "# Теория: Наследование и Полиморфизм в C++\n\nВ данной лабораторной работе изучаются два ключевых принципа ООП: наследование и полиморфизм.\n\n## 1. Наследование\nНаследование позволяет создать новый класс (наследник / производный) на основе уже существующего (базового родителя). Класс-наследник получает (наследует) все поля и методы базового класса и может добавлять свои собственные.\n\n```cpp\nclass RealNumber { ... };\nclass ComplexNumber : public RealNumber { ... }; \n```\nСлово `public` перед названием родителя означает \"публичное наследование\". Это означает, что `public` члены родителя остаются `public` в наследнике, а `protected` остаются `protected`.\n\n### Модификатор `protected`\nВ ЛР1 мы использовали `private` для сокрытия полей. Но `private` поля недоступны даже классам-наследникам. Чтобы поле нельзя было изменить извне (`main`), но можно было использовать внутри наследников, применяется спецификатор `protected`. Именно поэтому поля базового класса объявляются как `protected`.\n\n### Вызов конструктора родителя\nКонструктор наследника **обязан** инициализировать часть объекта, которая досталась ему от базового класса. Делается это в списке инициализации:\n```cpp\nComplexNumber(double real, double imag) : RealNumber(real) {\n    imaginary = imag;\n}\n```\n\n## 2. Полиморфизм\nПолиморфизм — это свойство системы использовать объекты с одинаковым интерфейсом без информации о типе и внутренней структуре объекта (возможность \"функции работать по-разному\" для разных объектов).\n\n### Статический полиморфизм (Статическое связывание)\nСвязывание вызова функции с её телом происходит на этапе **компиляции** (раннее связывание).\nПримеры:\n1. Перегрузка функций (две функции с одним именем, но разными типами аргументов).\n2. Вызов обычного не-виртуального метода через указатель базового типа:\n   ```cpp\n   Base* ptr = &derivedObj;\n   ptr->Base::method(); // Всегда вызовет метод базового класса\n   ```\n\n### Динамический полиморфизм (Динамическое связывание)\nСвязывание происходит на этапе **выполнения** (позднее связывание).\nДостигается за счет использования ключевого слова `virtual` перед методом в базовом классе.\n\nКак это работает:\n1. Когда компилятор видит модификатор `virtual`, он создает специальную невидимую **таблицу виртуальных функций (vtable)** для этого класса.\n2. В объект незаметно добавляется указатель на эту таблицу (`vptr`).\n3. При вызове виртуального метода через родительский указатель (`ptr->print()`), программа сначала идет по указателю `vptr` в таблицу и находит **правильный** адрес функции, который соответствует *реальному* типу объекта, лежащему в памяти.\n\n```cpp\nBase* ptr = &derivedObj; // Указатель базовый, но объект наследника\nptr->print(); // Вызовет Derived::print(), а не базовую функцию!\n```\nЭто самая мощная архитектурная особенность С++, позволяющая писать универсальный код для обработки разных типов данных через их общий базовый интерфейс."
}