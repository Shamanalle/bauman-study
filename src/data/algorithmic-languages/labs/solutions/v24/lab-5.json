{
  "variant": 24,
  "lab": 5,
  "code": "#include <iostream>\n#include <fstream>\n#include <string>\n#include <set>\n#include <unordered_set>\n\nusing namespace std;\n\nclass BankLoan {\nprivate:\n    string name;\n    double amount;\n    string currency;\n    double interestRate;\n\npublic:\n    BankLoan() : name(\"\"), amount(0.0), currency(\"RUB\"), interestRate(0.0) {}\n\n    BankLoan(string n, double a, string c, double r)\n        : name(move(n)), amount(a), currency(move(c)), interestRate(r) {}\n\n    const string& getName() const { return name; }\n    double getAmount() const { return amount; }\n    const string& getCurrency() const { return currency; }\n    double getInterestRate() const { return interestRate; }\n\n    bool operator<(const BankLoan& other) const {\n        if (interestRate != other.interestRate) return interestRate < other.interestRate;\n        return name < other.name;\n    }\n\n    bool operator==(const BankLoan& other) const {\n        return (amount == other.amount &&\n                name == other.name &&\n                currency == other.currency &&\n                interestRate == other.interestRate);\n    }\n\n    friend ostream& operator<<(ostream& os, const BankLoan& l) {\n        os << \"Кредит: '\" << l.name << \"', Сумма: \" << l.amount\n           << \" \" << l.currency << \", Ставка: \" << l.interestRate << \"%\";\n        return os;\n    }\n\n    friend istream& operator>>(istream& is, BankLoan& l) {\n        is >> l.name >> l.amount >> l.currency >> l.interestRate;\n        return is;\n    }\n};\n\nnamespace std {\n    template<>\n    struct hash<BankLoan> {\n        size_t operator()(const BankLoan& obj) const {\n            size_t h1 = hash<string>()(obj.getName());\n            size_t h2 = hash<double>()(obj.getAmount());\n            return h1 ^ (h2 << 1);\n        }\n    };\n}\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    ifstream fin(\"input.txt\");\n\n    set<BankLoan> orderedSet;\n    unordered_set<BankLoan> hashSet;\n    \n    BankLoan tempObj;\n\n    while (fin >> tempObj) {\n        orderedSet.insert(tempObj); \n        hashSet.insert(tempObj);    \n    }\n    fin.close();\n\n    cout << \"Дерево set (Сортировка по процентной ставке):\\n\";\n    for (const auto& item : orderedSet) {\n        cout << item << endl;\n    }\n\n    cout << \"\\nХэш-таблица unordered_set (Без порядка):\\n\";\n    for (const auto& item : hashSet) {\n        cout << item << endl;\n    }\n\n    return 0;\n}",
  "codeHtml": "<pre><code>#include &lt;iostream&gt;\n#include &lt;fstream&gt;\n#include &lt;string&gt;\n#include &lt;set&gt;\n#include &lt;unordered_set&gt;\n\nusing namespace std;\n\nclass BankLoan {\nprivate:\n    string name;\n    double amount;\n    string currency;\n    double interestRate;\n\npublic:\n    BankLoan() : name(\"\"), amount(0.0), currency(\"RUB\"), interestRate(0.0) {}\n\n    BankLoan(string n, double a, string c, double r)\n        : name(move(n)), amount(a), currency(move(c)), interestRate(r) {}\n\n    const string&amp; getName() const { return name; }\n    double getAmount() const { return amount; }\n    const string&amp; getCurrency() const { return currency; }\n    double getInterestRate() const { return interestRate; }\n\n    bool operator&lt;(const BankLoan&amp; other) const {\n        if (interestRate != other.interestRate) return interestRate &lt; other.interestRate;\n        return name &lt; other.name;\n    }\n\n    bool operator==(const BankLoan&amp; other) const {\n        return (amount == other.amount &amp;&amp;\n                name == other.name &amp;&amp;\n                currency == other.currency &amp;&amp;\n                interestRate == other.interestRate);\n    }\n\n    friend ostream&amp; operator&lt;&lt;(ostream&amp; os, const BankLoan&amp; l) {\n        os &lt;&lt; \"Кредит: '\" &lt;&lt; l.name &lt;&lt; \"', Сумма: \" &lt;&lt; l.amount\n           &lt;&lt; \" \" &lt;&lt; l.currency &lt;&lt; \", Ставка: \" &lt;&lt; l.interestRate &lt;&lt; \"%\";\n        return os;\n    }\n\n    friend istream&amp; operator&gt;&gt;(istream&amp; is, BankLoan&amp; l) {\n        is &gt;&gt; l.name &gt;&gt; l.amount &gt;&gt; l.currency &gt;&gt; l.interestRate;\n        return is;\n    }\n};\n\nnamespace std {\n    template&lt;&gt;\n    struct hash&lt;BankLoan&gt; {\n        size_t operator()(const BankLoan&amp; obj) const {\n            size_t h1 = hash&lt;string&gt;()(obj.getName());\n            size_t h2 = hash&lt;double&gt;()(obj.getAmount());\n            return h1 ^ (h2 &lt;&lt; 1);\n        }\n    };\n}\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    ifstream fin(\"input.txt\");\n\n    set&lt;BankLoan&gt; orderedSet;\n    unordered_set&lt;BankLoan&gt; hashSet;\n    \n    BankLoan tempObj;\n\n    while (fin &gt;&gt; tempObj) {\n        orderedSet.insert(tempObj); \n        hashSet.insert(tempObj);    \n    }\n    fin.close();\n\n    cout &lt;&lt; \"Дерево set (Сортировка по процентной ставке):\\n\";\n    for (const auto&amp; item : orderedSet) {\n        cout &lt;&lt; item &lt;&lt; endl;\n    }\n\n    cout &lt;&lt; \"\\nХэш-таблица unordered_set (Без порядка):\\n\";\n    for (const auto&amp; item : hashSet) {\n        cout &lt;&lt; item &lt;&lt; endl;\n    }\n\n    return 0;\n}</code></pre>",
  "explanation": "<h2>Разбор кода: Лабораторная работа 5 (Вариант 24)</h2>\n<p>Ниже представлен полный исходный код из <code class=\"inline-code\">main.cpp</code> для удобного ознакомления. </p>\n<p>В последующих разделах этот код разбит на логические блоки с детальным объяснением механизмов использования ассоциативных контейнеров.</p>\n<h3>Полный исходный код программы</h3>\n<pre><code>#include &lt;iostream&gt;\n#include &lt;fstream&gt;\n#include &lt;string&gt;\n#include &lt;set&gt;\n#include &lt;unordered_set&gt;\n\nusing namespace std;\n\nclass BankLoan {\nprivate:\n    string name;\n    double amount;\n    string currency;\n    double interestRate;\n\npublic:\n    BankLoan() : name(\"\"), amount(0.0), currency(\"RUB\"), interestRate(0.0) {}\n\n    BankLoan(string n, double a, string c, double r)\n        : name(move(n)), amount(a), currency(move(c)), interestRate(r) {}\n\n    const string&amp; getName() const { return name; }\n    double getAmount() const { return amount; }\n    const string&amp; getCurrency() const { return currency; }\n    double getInterestRate() const { return interestRate; }\n\n    bool operator&lt;(const BankLoan&amp; other) const {\n        if (interestRate != other.interestRate) return interestRate &lt; other.interestRate;\n        return name &lt; other.name;\n    }\n\n    bool operator==(const BankLoan&amp; other) const {\n        return (amount == other.amount &amp;&amp;\n                name == other.name &amp;&amp;\n                currency == other.currency &amp;&amp;\n                interestRate == other.interestRate);\n    }\n\n    friend ostream&amp; operator&lt;&lt;(ostream&amp; os, const BankLoan&amp; l) {\n        os &lt;&lt; \"Кредит: '\" &lt;&lt; l.name &lt;&lt; \"', Сумма: \" &lt;&lt; l.amount\n           &lt;&lt; \" \" &lt;&lt; l.currency &lt;&lt; \", Ставка: \" &lt;&lt; l.interestRate &lt;&lt; \"%\";\n        return os;\n    }\n\n    friend istream&amp; operator&gt;&gt;(istream&amp; is, BankLoan&amp; l) {\n        is &gt;&gt; l.name &gt;&gt; l.amount &gt;&gt; l.currency &gt;&gt; l.interestRate;\n        return is;\n    }\n};\n\nnamespace std {\n    template&lt;&gt;\n    struct hash&lt;BankLoan&gt; {\n        size_t operator()(const BankLoan&amp; obj) const {\n            size_t h1 = hash&lt;string&gt;()(obj.getName());\n            size_t h2 = hash&lt;double&gt;()(obj.getAmount());\n            return h1 ^ (h2 &lt;&lt; 1);\n        }\n    };\n}\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    ifstream fin(\"input.txt\");\n\n    set&lt;BankLoan&gt; orderedSet;\n    unordered_set&lt;BankLoan&gt; hashSet;\n    \n    BankLoan tempObj;\n\n    while (fin &gt;&gt; tempObj) {\n        orderedSet.insert(tempObj); \n        hashSet.insert(tempObj);    \n    }\n    fin.close();\n\n    cout &lt;&lt; \"Дерево set (Сортировка по процентной ставке):\\n\";\n    for (const auto&amp; item : orderedSet) {\n        cout &lt;&lt; item &lt;&lt; endl;\n    }\n\n    cout &lt;&lt; \"\\nХэш-таблица unordered_set (Без порядка):\\n\";\n    for (const auto&amp; item : hashSet) {\n        cout &lt;&lt; item &lt;&lt; endl;\n    }\n\n    return 0;\n}\n</code></pre>\n<hr>\n<h3>1. Как работает <code class=\"inline-code\">set</code></h3>\n<pre><code>    bool operator&lt;(const BankLoan&amp; other) const {\n        if (interestRate != other.interestRate) return interestRate &lt; other.interestRate;\n        return name &lt; other.name;\n    }</code></pre>\n<p>Контейнер <code class=\"inline-code\">set</code> постоянно сортирует элементы \"на лету\", расставляя их по веткам бинарного дерева поиска (Red-Black Tree). Для этого ему нужна <strong>только</strong> одна операция: проверка на <code class=\"inline-code\"><</code> (меньше).</p>\n<p>Особенность работы дерева: оно считает два объекта <strong>абсолютно одинаковыми</strong> (и удаляет дубликат), если <code class=\"inline-code\">A не меньше B, и B не меньше A</code>. </p>\n<p>Если бы мы проверяли <strong>только</strong> поле <code class=\"inline-code\">interestRate</code>:</p>\n<ul>\n<li>Два объекта с одинаковым значением <code class=\"inline-code\">interestRate</code> считались бы эквивалентными.</li>\n<li><code class=\"inline-code\">!(interestRate_a < interestRate_b)</code> и <code class=\"inline-code\">!(interestRate_b < interestRate_a)</code> — оба ложные, значит дерево решит, что это <strong>одна и та же</strong> запись и выбросит вторую.</li>\n</ul>\n<p>Мы избегаем этого, добавляя дополнительный критерий <code class=\"inline-code\">name</code> в случае равных значений основного поля (составное/каскадное условие сортировки).</p>\n<hr>\n<h3>2. Как работает <code class=\"inline-code\">unordered_set</code></h3>\n<h4>Оператор равенства <code class=\"inline-code\">==</code></h4>\n<pre><code>    bool operator==(const BankLoan&amp; other) const {\n        return (amount == other.amount &amp;&amp;\n                name == other.name &amp;&amp;\n                currency == other.currency &amp;&amp;\n                interestRate == other.interestRate);\n    }</code></pre>\n<p>Хэш-контейнеры не используют сортировку. Они кладут объект в \"ящик\" (bucket). Чтобы проверить два объекта на совпадение в этом \"ящике\" (при коллизии хэшей), им нужен явный <strong>оператор равенства</strong> <code class=\"inline-code\">==</code>. Мы сравниваем <strong>все</strong> поля объекта на точное совпадение.</p>\n<h4>Специализация <code class=\"inline-code\">std::hash</code></h4>\n<pre><code>namespace std {\n    template&lt;&gt;\n    struct hash&lt;BankLoan&gt; {\n        size_t operator()(const BankLoan&amp; obj) const {\n            size_t h1 = hash&lt;string&gt;()(obj.getName());\n            size_t h2 = hash&lt;double&gt;()(obj.getAmount());\n            return h1 ^ (h2 &lt;&lt; 1);\n        }\n    };\n}</code></pre>\n<p>Чтобы определить, <strong>в какой именно ящик</strong> (bucket) положить объект, нужна хэш-функция.</p>\n<p>Мы \"передаём\" компилятору кусок кода для <code class=\"inline-code\">std::hash</code>. Алгоритм:</p>\n<p>1. Вычисляется хэш первого поля через стандартный <code class=\"inline-code\">hash<string>()</code> или <code class=\"inline-code\">hash<double>()</code>.</p>\n<p>2. Вычисляется хэш второго ключевого поля.</p>\n<p>3. Хэши смешиваются с помощью оператора <code class=\"inline-code\">^</code> (Исключающее ИЛИ / XOR) и <code class=\"inline-code\"><<</code> (побитовый сдвиг влево), получая одно уникальное число <code class=\"inline-code\">size_t</code>. Сдвиг нужен для того, чтобы комбинация была ассиметричной: <code class=\"inline-code\">hash(a, b) != hash(b, a)</code>.</p>\n<hr>\n<h3>3. Вставка и использование</h3>\n<pre><code>    set&lt;BankLoan&gt; orderedSet;\n    unordered_set&lt;BankLoan&gt; hashSet;\n    \n    while (fin &gt;&gt; tempObj) {\n        orderedSet.insert(tempObj); \n        hashSet.insert(tempObj);    \n    }</code></pre>\n<p>В отличие от массива (<code class=\"inline-code\">vector</code>), где мы писали <code class=\"inline-code\">push_back(tempObj)</code> (засунуть в конец), деревья и таблицы не имеют \"конца\" в классическом понимании. Для них существует метод <code class=\"inline-code\">insert()</code>. </p>\n<ul>\n<li>Для <code class=\"inline-code\">set</code>: метод <code class=\"inline-code\">insert()</code> сам вычисляет правильное место в красно-чёрном дереве, используя наш <code class=\"inline-code\">operator<</code>. Элементы автоматически оказываются отсортированными.</li>\n<li>Для <code class=\"inline-code\">unordered_set</code>: метод <code class=\"inline-code\">insert()</code> вычисляет хэш объекта (через <code class=\"inline-code\">std::hash</code>), определяет номер bucket-а и помещает элемент туда. Если bucket уже занят (коллизия), используется <code class=\"inline-code\">operator==</code> для проверки на дубликат.</li>\n</ul>\n<h4>Вывод</h4>\n<pre><code>    cout &lt;&lt; \"Дерево set (Сортировка по процентной ставке):\\n\";\n    for (const auto&amp; item : orderedSet) {\n        cout &lt;&lt; item &lt;&lt; endl;\n    }</code></pre>\n<p>Обход дерева <code class=\"inline-code\">set</code> через range-based <code class=\"inline-code\">for</code> гарантированно выдаёт элементы в <strong>отсортированном порядке</strong> (по процентной ставке).</p>\n<p>Обход <code class=\"inline-code\">unordered_set</code> выдаёт элементы в <strong>произвольном порядке</strong>, зависящем от значений хэшей.</p>",
  "explanationRaw": "# Разбор кода: Лабораторная работа 5 (Вариант 24)\n\nНиже представлен полный исходный код из `main.cpp` для удобного ознакомления. \nВ последующих разделах этот код разбит на логические блоки с детальным объяснением механизмов использования ассоциативных контейнеров.\n\n## Полный исходный код программы\n```cpp\n#include <iostream>\n#include <fstream>\n#include <string>\n#include <set>\n#include <unordered_set>\n\nusing namespace std;\n\nclass BankLoan {\nprivate:\n    string name;\n    double amount;\n    string currency;\n    double interestRate;\n\npublic:\n    BankLoan() : name(\"\"), amount(0.0), currency(\"RUB\"), interestRate(0.0) {}\n\n    BankLoan(string n, double a, string c, double r)\n        : name(move(n)), amount(a), currency(move(c)), interestRate(r) {}\n\n    const string& getName() const { return name; }\n    double getAmount() const { return amount; }\n    const string& getCurrency() const { return currency; }\n    double getInterestRate() const { return interestRate; }\n\n    bool operator<(const BankLoan& other) const {\n        if (interestRate != other.interestRate) return interestRate < other.interestRate;\n        return name < other.name;\n    }\n\n    bool operator==(const BankLoan& other) const {\n        return (amount == other.amount &&\n                name == other.name &&\n                currency == other.currency &&\n                interestRate == other.interestRate);\n    }\n\n    friend ostream& operator<<(ostream& os, const BankLoan& l) {\n        os << \"Кредит: '\" << l.name << \"', Сумма: \" << l.amount\n           << \" \" << l.currency << \", Ставка: \" << l.interestRate << \"%\";\n        return os;\n    }\n\n    friend istream& operator>>(istream& is, BankLoan& l) {\n        is >> l.name >> l.amount >> l.currency >> l.interestRate;\n        return is;\n    }\n};\n\nnamespace std {\n    template<>\n    struct hash<BankLoan> {\n        size_t operator()(const BankLoan& obj) const {\n            size_t h1 = hash<string>()(obj.getName());\n            size_t h2 = hash<double>()(obj.getAmount());\n            return h1 ^ (h2 << 1);\n        }\n    };\n}\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    ifstream fin(\"input.txt\");\n\n    set<BankLoan> orderedSet;\n    unordered_set<BankLoan> hashSet;\n    \n    BankLoan tempObj;\n\n    while (fin >> tempObj) {\n        orderedSet.insert(tempObj); \n        hashSet.insert(tempObj);    \n    }\n    fin.close();\n\n    cout << \"Дерево set (Сортировка по процентной ставке):\\n\";\n    for (const auto& item : orderedSet) {\n        cout << item << endl;\n    }\n\n    cout << \"\\nХэш-таблица unordered_set (Без порядка):\\n\";\n    for (const auto& item : hashSet) {\n        cout << item << endl;\n    }\n\n    return 0;\n}\n\n```\n\n---\n\n## 1. Как работает `set`\n\n```cpp\n    bool operator<(const BankLoan& other) const {\n        if (interestRate != other.interestRate) return interestRate < other.interestRate;\n        return name < other.name;\n    }\n```\nКонтейнер `set` постоянно сортирует элементы \"на лету\", расставляя их по веткам бинарного дерева поиска (Red-Black Tree). Для этого ему нужна **только** одна операция: проверка на `<` (меньше).\n\nОсобенность работы дерева: оно считает два объекта **абсолютно одинаковыми** (и удаляет дубликат), если `A не меньше B, и B не меньше A`. \nЕсли бы мы проверяли **только** поле `interestRate`:\n- Два объекта с одинаковым значением `interestRate` считались бы эквивалентными.\n- `!(interestRate_a < interestRate_b)` и `!(interestRate_b < interestRate_a)` — оба ложные, значит дерево решит, что это **одна и та же** запись и выбросит вторую.\n\nМы избегаем этого, добавляя дополнительный критерий `name` в случае равных значений основного поля (составное/каскадное условие сортировки).\n\n---\n\n## 2. Как работает `unordered_set`\n\n### Оператор равенства `==`\n```cpp\n    bool operator==(const BankLoan& other) const {\n        return (amount == other.amount &&\n                name == other.name &&\n                currency == other.currency &&\n                interestRate == other.interestRate);\n    }\n```\nХэш-контейнеры не используют сортировку. Они кладут объект в \"ящик\" (bucket). Чтобы проверить два объекта на совпадение в этом \"ящике\" (при коллизии хэшей), им нужен явный **оператор равенства** `==`. Мы сравниваем **все** поля объекта на точное совпадение.\n\n### Специализация `std::hash`\n```cpp\nnamespace std {\n    template<>\n    struct hash<BankLoan> {\n        size_t operator()(const BankLoan& obj) const {\n            size_t h1 = hash<string>()(obj.getName());\n            size_t h2 = hash<double>()(obj.getAmount());\n            return h1 ^ (h2 << 1);\n        }\n    };\n}\n```\nЧтобы определить, **в какой именно ящик** (bucket) положить объект, нужна хэш-функция.\nМы \"передаём\" компилятору кусок кода для `std::hash`. Алгоритм:\n1. Вычисляется хэш первого поля через стандартный `hash<string>()` или `hash<double>()`.\n2. Вычисляется хэш второго ключевого поля.\n3. Хэши смешиваются с помощью оператора `^` (Исключающее ИЛИ / XOR) и `<<` (побитовый сдвиг влево), получая одно уникальное число `size_t`. Сдвиг нужен для того, чтобы комбинация была ассиметричной: `hash(a, b) != hash(b, a)`.\n\n---\n\n## 3. Вставка и использование\n\n```cpp\n    set<BankLoan> orderedSet;\n    unordered_set<BankLoan> hashSet;\n    \n    while (fin >> tempObj) {\n        orderedSet.insert(tempObj); \n        hashSet.insert(tempObj);    \n    }\n```\nВ отличие от массива (`vector`), где мы писали `push_back(tempObj)` (засунуть в конец), деревья и таблицы не имеют \"конца\" в классическом понимании. Для них существует метод `insert()`. \n- Для `set`: метод `insert()` сам вычисляет правильное место в красно-чёрном дереве, используя наш `operator<`. Элементы автоматически оказываются отсортированными.\n- Для `unordered_set`: метод `insert()` вычисляет хэш объекта (через `std::hash`), определяет номер bucket-а и помещает элемент туда. Если bucket уже занят (коллизия), используется `operator==` для проверки на дубликат.\n\n### Вывод\n```cpp\n    cout << \"Дерево set (Сортировка по процентной ставке):\\n\";\n    for (const auto& item : orderedSet) {\n        cout << item << endl;\n    }\n```\nОбход дерева `set` через range-based `for` гарантированно выдаёт элементы в **отсортированном порядке** (по процентной ставке).\nОбход `unordered_set` выдаёт элементы в **произвольном порядке**, зависящем от значений хэшей.",
  "condition": "<h2>Условие лабораторной работы №5 (Вариант 24)</h2>\n<h3>Тема: Использование ассоциативных контейнеров STL (<code class=\"inline-code\">set</code>, <code class=\"inline-code\">map</code>, <code class=\"inline-code\">unordered_set</code>, <code class=\"inline-code\">unordered_map</code>)</h3>\n<p><strong>Цель работы:</strong></p>\n<p>Закрепить знания о работе со стандартной библиотекой шаблонов (STL), научиться использовать ассоциативные контейнеры в C++, основанные на сбалансированных деревьях поиска и на структурах хэш-таблиц. Понять, какие требования к классам предъявляют такие контейнеры.</p>\n<h3>Общее задание:</h3>\n<p>1. Для выполнения данной работы необходимо модернизировать пользовательский класс, написанный в Лабораторной работе №4.</p>\n<p>2. Прочитать данные из файла (как в прошлом задании) и добавить объекты в два ассоциативных контейнера, предназначенных для множеств:</p>\n<ul>\n<li><strong><code class=\"inline-code\">std::set</code></strong> (упорядоченное множество, основанное на бинарном дереве, гарантирует логарифмическую сложность $O(\\log n)$ поиска/вставки).</li>\n<li><strong><code class=\"inline-code\">std::unordered_set</code></strong> (неупорядоченное множество, основанное на хэш-таблице (bucket collection), гарантирует константную сложность $O(1)$ при отсутствии коллизий).</li>\n</ul>\n<p>3. Чтобы объекты пользовательского класса могли быть вставлены в упорядоченный контейнер (дерево) <code class=\"inline-code\">std::set</code>, необходимо:</p>\n<ul>\n<li>Подготовить функцию-компаратор, либо <strong>перегрузить оператор связывания (отношения) «<» (меньше)</strong>.</li>\n<li><em>Важно: если сортировать только по одному параметру, объекты с одинаковым параметром будут считаться математически эквивалентными, и дубликат (по мнению дерева) не добавится во множество. Следует предусмотреть составное условие.</em></li>\n</ul>\n<p>4. Чтобы объекты могли быть вставлены в хэш-таблицу <code class=\"inline-code\">std::unordered_set</code>, необходимо:</p>\n<ul>\n<li>Подготовить функцию (перегрузить оператор) для проверки объектов на точное <strong>эквивалентность (равенство) «==»</strong>.</li>\n<li>Написать <strong>функцию хэширования</strong>. Как правило, для этого программист явно специализирует системный шаблон структуры <code class=\"inline-code\">std::hash<Type></code>. Функция должна возвращать тип данных <code class=\"inline-code\">size_t</code> и использовать логические битовые комбинации (XOR) для генерации хэша на основе полей объекта.</li>\n</ul>\n<p>5. Напечатать состав каждого контейнера (содержимое дерева должно автоматически вывестись отсортированным, а содержимое хэш-таблицы — в произвольном, зависящем от хэшей порядке).</p>\n<h3>Индивидуальное задание (Вариант 24, опирается на задания ЛР4):</h3>\n<ul>\n<li><strong>Класс:</strong> «Банковский кредит» (название, сумма кредита, тип валюты, ставка в % годовых).</li>\n<li><strong>Критерий порядка для <code class=\"inline-code\">set</code>:</strong> Сортировка по процентной ставке.</li>\n</ul>",
  "conditionRaw": "# Условие лабораторной работы №5 (Вариант 24)\n\n## Тема: Использование ассоциативных контейнеров STL (`set`, `map`, `unordered_set`, `unordered_map`)\n\n**Цель работы:**\nЗакрепить знания о работе со стандартной библиотекой шаблонов (STL), научиться использовать ассоциативные контейнеры в C++, основанные на сбалансированных деревьях поиска и на структурах хэш-таблиц. Понять, какие требования к классам предъявляют такие контейнеры.\n\n## Общее задание:\n1. Для выполнения данной работы необходимо модернизировать пользовательский класс, написанный в Лабораторной работе №4.\n2. Прочитать данные из файла (как в прошлом задании) и добавить объекты в два ассоциативных контейнера, предназначенных для множеств:\n   - **`std::set`** (упорядоченное множество, основанное на бинарном дереве, гарантирует логарифмическую сложность $O(\\log n)$ поиска/вставки).\n   - **`std::unordered_set`** (неупорядоченное множество, основанное на хэш-таблице (bucket collection), гарантирует константную сложность $O(1)$ при отсутствии коллизий).\n3. Чтобы объекты пользовательского класса могли быть вставлены в упорядоченный контейнер (дерево) `std::set`, необходимо:\n   - Подготовить функцию-компаратор, либо **перегрузить оператор связывания (отношения) «<» (меньше)**.\n   - *Важно: если сортировать только по одному параметру, объекты с одинаковым параметром будут считаться математически эквивалентными, и дубликат (по мнению дерева) не добавится во множество. Следует предусмотреть составное условие.*\n4. Чтобы объекты могли быть вставлены в хэш-таблицу `std::unordered_set`, необходимо:\n   - Подготовить функцию (перегрузить оператор) для проверки объектов на точное **эквивалентность (равенство) «==»**.\n   - Написать **функцию хэширования**. Как правило, для этого программист явно специализирует системный шаблон структуры `std::hash<Type>`. Функция должна возвращать тип данных `size_t` и использовать логические битовые комбинации (XOR) для генерации хэша на основе полей объекта.\n5. Напечатать состав каждого контейнера (содержимое дерева должно автоматически вывестись отсортированным, а содержимое хэш-таблицы — в произвольном, зависящем от хэшей порядке).\n\n## Индивидуальное задание (Вариант 24, опирается на задания ЛР4):\n- **Класс:** «Банковский кредит» (название, сумма кредита, тип валюты, ставка в % годовых).\n- **Критерий порядка для `set`:** Сортировка по процентной ставке.",
  "theory": "<h2>Теория: Ассоциативные контейнеры в C++ (<code class=\"inline-code\">set</code> и <code class=\"inline-code\">unordered_set</code>)</h2>\n<p>В отличие от последовательных контейнеров (<code class=\"inline-code\">std::vector</code>), где элементы хранятся в порядке их добавления, <strong>ассоциативные контейнеры</strong> сами управляют расположением данных для того, чтобы максимально ускорить их поиск (за счет древовидных или хэш-структур). </p>\n<p>Оба контейнера (<code class=\"inline-code\">std::set</code> и <code class=\"inline-code\">std::unordered_set</code>) хранят только <strong>уникальные</strong> элементы. Если попытаться добавить элемент, который там уже есть, он не добавится повторно.</p>\n<h3>1. Контейнер <code class=\"inline-code\">std::set</code></h3>\n<p><code class=\"inline-code\">std::set</code> — это множество. Под капотом оно реализовано на основе сбалансированного бинарного дерева поиска (обычно это Красно-чёрное дерево Red-Black Tree).</p>\n<ul>\n<li><strong>Сложность поиска/вставки:</strong> $O(\\log n)$ (логарифмическое время).</li>\n<li><strong>Порядок обхода:</strong> Гарантируется, что при обходе контейнера циклом <code class=\"inline-code\">for (auto& item : set)</code> элементы будут выдаваться в <strong>строго отсортированном виде</strong> (от меньшего к большему).</li>\n</ul>\n<h4>Требования к классу</h4>\n<p>Чтобы дерево \"понимало\", какой элемент меньше, а какой больше (кто должен идти \"левее\", а кто \"правее\" в узлах дерева), мы обязаны перегрузить <strong>оператор <code class=\"inline-code\"><</code> (меньше)</strong>.</p>\n<p><em>Важно:</em> Если перегрузить только сравнение по одному полю, то два разных объекта с одинаковым значением этого поля будут восприняты как <em>один и тот же</em> объект (так как <code class=\"inline-code\">!(a<b)</code> и <code class=\"inline-code\">!(b<a)</code> означает в дереве эквивалентность <code class=\"inline-code\">a==b</code>), и второй будет удален. Поэтому обязательно добавляется дополнительный критерий сравнения.</p>\n<h3>2. Контейнер <code class=\"inline-code\">std::unordered_set</code></h3>\n<p><code class=\"inline-code\">std::unordered_set</code> — это хэш-множество (или корзина без порядка). Под капотом реализовано на основе хэш-таблицы (Hash Table).</p>\n<ul>\n<li><strong>Сложность поиска/вставки:</strong> $O(1)$ в среднем (мгновенно), $O(n)$ в худшем случае при коллизиях.</li>\n<li><strong>Порядок обхода:</strong> Элементы лежат хаотично случайным образом, в зависимости от вычисленных хэшей. Никакого порядка нет.</li>\n</ul>\n<h4>Требования к классу</h4>\n<p>Для хранения в хэш-таблице дереву не интересно, кто больше или меньше. Ему важны 2 вещи:</p>\n<p>1. <strong>Функция сравнения на равенство</strong> (оператор <code class=\"inline-code\">==</code>): нужна для проверки двух объектов на совпадение при коллизиях хэшей.</p>\n<p>2. <strong>Как вычислять хэш-код</strong> объекта (целое число, по которому таблица определяет номер \"корзины\"):</p>\n<p>   Для стандартных типов <code class=\"inline-code\">std::hash</code> уже написан. Для нашего собственного класса программист должен реализовать алгоритм хэширования, написав специализацию внутри пространства имен <code class=\"inline-code\">std</code>:</p>\n<p>   ```cpp</p>\n<p>   namespace std {</p>\n<p>       template<></p>\n<p>       struct hash<MyClass> {</p>\n<p>           size_t operator()(const MyClass& obj) const {</p>\n<p>               // Логика математического перемешивания полей</p>\n<p>           }</p>\n<p>       };</p>\n<p>   }</p>\n<p>   ```</p>",
  "theoryRaw": "# Теория: Ассоциативные контейнеры в C++ (`set` и `unordered_set`)\n\nВ отличие от последовательных контейнеров (`std::vector`), где элементы хранятся в порядке их добавления, **ассоциативные контейнеры** сами управляют расположением данных для того, чтобы максимально ускорить их поиск (за счет древовидных или хэш-структур). \n\nОба контейнера (`std::set` и `std::unordered_set`) хранят только **уникальные** элементы. Если попытаться добавить элемент, который там уже есть, он не добавится повторно.\n\n## 1. Контейнер `std::set`\n\n`std::set` — это множество. Под капотом оно реализовано на основе сбалансированного бинарного дерева поиска (обычно это Красно-чёрное дерево Red-Black Tree).\n- **Сложность поиска/вставки:** $O(\\log n)$ (логарифмическое время).\n- **Порядок обхода:** Гарантируется, что при обходе контейнера циклом `for (auto& item : set)` элементы будут выдаваться в **строго отсортированном виде** (от меньшего к большему).\n\n### Требования к классу\nЧтобы дерево \"понимало\", какой элемент меньше, а какой больше (кто должен идти \"левее\", а кто \"правее\" в узлах дерева), мы обязаны перегрузить **оператор `<` (меньше)**.\n\n*Важно:* Если перегрузить только сравнение по одному полю, то два разных объекта с одинаковым значением этого поля будут восприняты как *один и тот же* объект (так как `!(a<b)` и `!(b<a)` означает в дереве эквивалентность `a==b`), и второй будет удален. Поэтому обязательно добавляется дополнительный критерий сравнения.\n\n## 2. Контейнер `std::unordered_set`\n\n`std::unordered_set` — это хэш-множество (или корзина без порядка). Под капотом реализовано на основе хэш-таблицы (Hash Table).\n- **Сложность поиска/вставки:** $O(1)$ в среднем (мгновенно), $O(n)$ в худшем случае при коллизиях.\n- **Порядок обхода:** Элементы лежат хаотично случайным образом, в зависимости от вычисленных хэшей. Никакого порядка нет.\n\n### Требования к классу\nДля хранения в хэш-таблице дереву не интересно, кто больше или меньше. Ему важны 2 вещи:\n1. **Функция сравнения на равенство** (оператор `==`): нужна для проверки двух объектов на совпадение при коллизиях хэшей.\n2. **Как вычислять хэш-код** объекта (целое число, по которому таблица определяет номер \"корзины\"):\n   Для стандартных типов `std::hash` уже написан. Для нашего собственного класса программист должен реализовать алгоритм хэширования, написав специализацию внутри пространства имен `std`:\n   ```cpp\n   namespace std {\n       template<>\n       struct hash<MyClass> {\n           size_t operator()(const MyClass& obj) const {\n               // Логика математического перемешивания полей\n           }\n       };\n   }\n   ```"
}