{
  "variant": 14,
  "lab": 3,
  "code": "#include <iostream>\n#include <string>\n#include <cmath>\n\nusing namespace std;\n\nclass BankAccount {\nprotected:\n    string owner;\n    double initialSum;\n    double rate;       // % годовых\n    int years;         // время существования вклада\n\npublic:\n    BankAccount(string o, double s, double r, int y)\n        : owner(o), initialSum(s), rate(r), years(y) {}\n\n    virtual double calcSum() const {\n        return initialSum * pow(1.0 + rate / 100.0, years);\n    }\n\n    virtual void print() const {\n        cout << \"Банковский счёт:\" << endl;\n        cout << \"  Владелец: \" << owner << endl;\n        cout << \"  Начальная сумма: \" << initialSum << \" руб.\" << endl;\n        cout << \"  Ставка: \" << rate << \"% годовых\" << endl;\n        cout << \"  Срок вклада: \" << years << \" лет\" << endl;\n        cout << \"  Сумма на счёте: \" << calcSum() << \" руб.\" << endl;\n    }\n\n    virtual ~BankAccount() {}\n};\n\nclass PrivilegedAccount : public BankAccount {\nprivate:\n    double creditRate;  // % кредита от суммы на счёте\n\npublic:\n    PrivilegedAccount(string o, double s, double r, int y, double cr)\n        : BankAccount(o, s, r, y), creditRate(cr) {}\n\n    double calcSum() const override {\n        double base = BankAccount::calcSum();\n        return base + base * (creditRate / 100.0);\n    }\n\n    void print() const override {\n        BankAccount::print();\n        cout << \"  Тип: Привилегированный счёт\" << endl;\n        cout << \"  Процент кредита: \" << creditRate << \"%\" << endl;\n        cout << \"  Сумма с кредитом: \" << calcSum() << \" руб.\" << endl;\n    }\n};\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    cout << \"=== Создание объектов ===\" << endl;\n    BankAccount ba(\"Иванов Иван Иванович\", 100000, 5.0, 3);\n    PrivilegedAccount pa(\"Петров Пётр Петрович\", 200000, 7.0, 5, 15.0);\n\n    cout << \"\\n=== Прямой вызов функций (объекты) ===\" << endl;\n    ba.print();\n    cout << endl;\n    pa.print();\n\n    cout << \"\\n=== 1. СТАТИЧЕСКИЙ ПОЛИМОРФИЗМ (Статическое связывание) ===\" << endl;\n\n    BankAccount* ptr_b1 = &ba;\n    BankAccount* ptr_b2 = &pa;\n\n    cout << \"\\n[Явный вызов базового метода] ptr_b2->BankAccount::print():\" << endl;\n    ptr_b2->BankAccount::print();\n\n    cout << \"\\n[Явный вызов базового calcSum()] ptr_b2->BankAccount::calcSum():\" << endl;\n    cout << \"  Результат (без кредита): \" << ptr_b2->BankAccount::calcSum() << endl;\n\n    cout << \"\\n=== 2. ДИНАМИЧЕСКИЙ ПОЛИМОРФИЗМ (Динамическое связывание) ===\" << endl;\n\n    cout << \"\\nВызов ptr_b1->print() (указывает на BankAccount):\" << endl;\n    ptr_b1->print();\n\n    cout << \"\\nВызов ptr_b2->print() (указывает на PrivilegedAccount):\" << endl;\n    ptr_b2->print();\n\n    cout << \"\\nВызов ptr_b2->calcSum() (указывает на PrivilegedAccount):\" << endl;\n    cout << \"  Результат (с кредитом): \" << ptr_b2->calcSum() << endl;\n\n    return 0;\n}",
  "codeHtml": "<pre><code>#include &lt;iostream&gt;\n#include &lt;string&gt;\n#include &lt;cmath&gt;\n\nusing namespace std;\n\nclass BankAccount {\nprotected:\n    string owner;\n    double initialSum;\n    double rate;       // % годовых\n    int years;         // время существования вклада\n\npublic:\n    BankAccount(string o, double s, double r, int y)\n        : owner(o), initialSum(s), rate(r), years(y) {}\n\n    virtual double calcSum() const {\n        return initialSum * pow(1.0 + rate / 100.0, years);\n    }\n\n    virtual void print() const {\n        cout &lt;&lt; \"Банковский счёт:\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Владелец: \" &lt;&lt; owner &lt;&lt; endl;\n        cout &lt;&lt; \"  Начальная сумма: \" &lt;&lt; initialSum &lt;&lt; \" руб.\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Ставка: \" &lt;&lt; rate &lt;&lt; \"% годовых\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Срок вклада: \" &lt;&lt; years &lt;&lt; \" лет\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Сумма на счёте: \" &lt;&lt; calcSum() &lt;&lt; \" руб.\" &lt;&lt; endl;\n    }\n\n    virtual ~BankAccount() {}\n};\n\nclass PrivilegedAccount : public BankAccount {\nprivate:\n    double creditRate;  // % кредита от суммы на счёте\n\npublic:\n    PrivilegedAccount(string o, double s, double r, int y, double cr)\n        : BankAccount(o, s, r, y), creditRate(cr) {}\n\n    double calcSum() const override {\n        double base = BankAccount::calcSum();\n        return base + base * (creditRate / 100.0);\n    }\n\n    void print() const override {\n        BankAccount::print();\n        cout &lt;&lt; \"  Тип: Привилегированный счёт\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Процент кредита: \" &lt;&lt; creditRate &lt;&lt; \"%\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Сумма с кредитом: \" &lt;&lt; calcSum() &lt;&lt; \" руб.\" &lt;&lt; endl;\n    }\n};\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    cout &lt;&lt; \"=== Создание объектов ===\" &lt;&lt; endl;\n    BankAccount ba(\"Иванов Иван Иванович\", 100000, 5.0, 3);\n    PrivilegedAccount pa(\"Петров Пётр Петрович\", 200000, 7.0, 5, 15.0);\n\n    cout &lt;&lt; \"\\n=== Прямой вызов функций (объекты) ===\" &lt;&lt; endl;\n    ba.print();\n    cout &lt;&lt; endl;\n    pa.print();\n\n    cout &lt;&lt; \"\\n=== 1. СТАТИЧЕСКИЙ ПОЛИМОРФИЗМ (Статическое связывание) ===\" &lt;&lt; endl;\n\n    BankAccount* ptr_b1 = &amp;ba;\n    BankAccount* ptr_b2 = &amp;pa;\n\n    cout &lt;&lt; \"\\n[Явный вызов базового метода] ptr_b2-&gt;BankAccount::print():\" &lt;&lt; endl;\n    ptr_b2-&gt;BankAccount::print();\n\n    cout &lt;&lt; \"\\n[Явный вызов базового calcSum()] ptr_b2-&gt;BankAccount::calcSum():\" &lt;&lt; endl;\n    cout &lt;&lt; \"  Результат (без кредита): \" &lt;&lt; ptr_b2-&gt;BankAccount::calcSum() &lt;&lt; endl;\n\n    cout &lt;&lt; \"\\n=== 2. ДИНАМИЧЕСКИЙ ПОЛИМОРФИЗМ (Динамическое связывание) ===\" &lt;&lt; endl;\n\n    cout &lt;&lt; \"\\nВызов ptr_b1-&gt;print() (указывает на BankAccount):\" &lt;&lt; endl;\n    ptr_b1-&gt;print();\n\n    cout &lt;&lt; \"\\nВызов ptr_b2-&gt;print() (указывает на PrivilegedAccount):\" &lt;&lt; endl;\n    ptr_b2-&gt;print();\n\n    cout &lt;&lt; \"\\nВызов ptr_b2-&gt;calcSum() (указывает на PrivilegedAccount):\" &lt;&lt; endl;\n    cout &lt;&lt; \"  Результат (с кредитом): \" &lt;&lt; ptr_b2-&gt;calcSum() &lt;&lt; endl;\n\n    return 0;\n}</code></pre>",
  "explanation": "<h2>Разбор кода: Лабораторная работа 3 (Вариант 14)</h2>\n<p>Ниже представлен полный исходный код из <code class=\"inline-code\">main.cpp</code> для удобного ознакомления. </p>\n<p>В последующих разделах этот код разбит на логические блоки с детальным объяснением конструкций наследования и полиморфизма.</p>\n<h3>Полный исходный код программы</h3>\n<pre><code>#include &lt;iostream&gt;\n#include &lt;string&gt;\n#include &lt;cmath&gt;\n\nusing namespace std;\n\nclass BankAccount {\nprotected:\n    string owner;\n    double initialSum;\n    double rate;       // % годовых\n    int years;         // время существования вклада\n\npublic:\n    BankAccount(string o, double s, double r, int y)\n        : owner(o), initialSum(s), rate(r), years(y) {}\n\n    virtual double calcSum() const {\n        return initialSum * pow(1.0 + rate / 100.0, years);\n    }\n\n    virtual void print() const {\n        cout &lt;&lt; \"Банковский счёт:\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Владелец: \" &lt;&lt; owner &lt;&lt; endl;\n        cout &lt;&lt; \"  Начальная сумма: \" &lt;&lt; initialSum &lt;&lt; \" руб.\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Ставка: \" &lt;&lt; rate &lt;&lt; \"% годовых\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Срок вклада: \" &lt;&lt; years &lt;&lt; \" лет\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Сумма на счёте: \" &lt;&lt; calcSum() &lt;&lt; \" руб.\" &lt;&lt; endl;\n    }\n\n    virtual ~BankAccount() {}\n};\n\nclass PrivilegedAccount : public BankAccount {\nprivate:\n    double creditRate;  // % кредита от суммы на счёте\n\npublic:\n    PrivilegedAccount(string o, double s, double r, int y, double cr)\n        : BankAccount(o, s, r, y), creditRate(cr) {}\n\n    double calcSum() const override {\n        double base = BankAccount::calcSum();\n        return base + base * (creditRate / 100.0);\n    }\n\n    void print() const override {\n        BankAccount::print();\n        cout &lt;&lt; \"  Тип: Привилегированный счёт\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Процент кредита: \" &lt;&lt; creditRate &lt;&lt; \"%\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Сумма с кредитом: \" &lt;&lt; calcSum() &lt;&lt; \" руб.\" &lt;&lt; endl;\n    }\n};\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    cout &lt;&lt; \"=== Создание объектов ===\" &lt;&lt; endl;\n    BankAccount ba(\"Иванов Иван Иванович\", 100000, 5.0, 3);\n    PrivilegedAccount pa(\"Петров Пётр Петрович\", 200000, 7.0, 5, 15.0);\n\n    cout &lt;&lt; \"\\n=== Прямой вызов функций (объекты) ===\" &lt;&lt; endl;\n    ba.print();\n    cout &lt;&lt; endl;\n    pa.print();\n\n    cout &lt;&lt; \"\\n=== 1. СТАТИЧЕСКИЙ ПОЛИМОРФИЗМ (Статическое связывание) ===\" &lt;&lt; endl;\n\n    BankAccount* ptr_b1 = &amp;ba;\n    BankAccount* ptr_b2 = &amp;pa;\n\n    cout &lt;&lt; \"\\n[Явный вызов базового метода] ptr_b2-&gt;BankAccount::print():\" &lt;&lt; endl;\n    ptr_b2-&gt;BankAccount::print();\n\n    cout &lt;&lt; \"\\n[Явный вызов базового calcSum()] ptr_b2-&gt;BankAccount::calcSum():\" &lt;&lt; endl;\n    cout &lt;&lt; \"  Результат (без кредита): \" &lt;&lt; ptr_b2-&gt;BankAccount::calcSum() &lt;&lt; endl;\n\n    cout &lt;&lt; \"\\n=== 2. ДИНАМИЧЕСКИЙ ПОЛИМОРФИЗМ (Динамическое связывание) ===\" &lt;&lt; endl;\n\n    cout &lt;&lt; \"\\nВызов ptr_b1-&gt;print() (указывает на BankAccount):\" &lt;&lt; endl;\n    ptr_b1-&gt;print();\n\n    cout &lt;&lt; \"\\nВызов ptr_b2-&gt;print() (указывает на PrivilegedAccount):\" &lt;&lt; endl;\n    ptr_b2-&gt;print();\n\n    cout &lt;&lt; \"\\nВызов ptr_b2-&gt;calcSum() (указывает на PrivilegedAccount):\" &lt;&lt; endl;\n    cout &lt;&lt; \"  Результат (с кредитом): \" &lt;&lt; ptr_b2-&gt;calcSum() &lt;&lt; endl;\n\n    return 0;\n}\n</code></pre>\n<hr>\n<h3>1. Базовый Класс <code class=\"inline-code\">BankAccount</code></h3>\n<pre><code>class BankAccount {{\nprotected:\n    string owner;\n    double initialSum;\n    double rate;\n    int years;</code></pre>\n<ul>\n<li>Четыре поля, все <code class=\"inline-code\">protected</code>:</li>\n<li><code class=\"inline-code\">owner</code> — ФИО владельца счёта.</li>\n<li><code class=\"inline-code\">initialSum</code> — начальная сумма вклада.</li>\n<li><code class=\"inline-code\">rate</code> — ставка вклада (% годовых).</li>\n<li><code class=\"inline-code\">years</code> — время существования вклада в годах.</li>\n<li><strong><code class=\"inline-code\">protected</code></strong> обеспечивает доступ из наследника <code class=\"inline-code\">PrivilegedAccount</code>, но запрещает из <code class=\"inline-code\">main()</code>.</li>\n</ul>\n<pre><code>    BankAccount(string o, double s, double r, int y)\n        : owner(o), initialSum(s), rate(r), years(y) {{}}</code></pre>\n<ul>\n<li>Конструктор инициализирует все четыре поля через <strong>список инициализации</strong>.</li>\n</ul>\n<pre><code>    virtual double calcSum() const {{\n        return initialSum * pow(1.0 + rate / 100.0, years);\n    }}</code></pre>\n<ul>\n<li>Вычисление суммы по формуле <strong>сложных процентов</strong>: S = P × (1 + r/100)ⁿ, где P — начальная сумма, r — ставка, n — срок.</li>\n<li>Функция <code class=\"inline-code\">pow()</code> из <code class=\"inline-code\"><cmath></code> возводит в степень.</li>\n<li>Пример: 100000 × (1.05)³ = 115762.5.</li>\n<li><strong><code class=\"inline-code\">virtual</code></strong> позволяет привилегированному счёту переопределить расчёт с учётом кредита.</li>\n</ul>\n<pre><code>    virtual void print() const {{\n        cout &lt;&lt; \"Банковский счёт:\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Владелец: \" &lt;&lt; owner &lt;&lt; endl;\n        cout &lt;&lt; \"  Начальная сумма: \" &lt;&lt; initialSum &lt;&lt; \" руб.\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Ставка: \" &lt;&lt; rate &lt;&lt; \"% годовых\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Срок вклада: \" &lt;&lt; years &lt;&lt; \" лет\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Сумма на счёте: \" &lt;&lt; calcSum() &lt;&lt; \" руб.\" &lt;&lt; endl;\n    }}</code></pre>\n<ul>\n<li>Виртуальный метод печати. Вызывает <code class=\"inline-code\">calcSum()</code> — через виртуальность будет вызвана правильная версия.</li>\n</ul>\n<pre><code>    virtual ~BankAccount() {{}}</code></pre>\n<ul>\n<li>Виртуальный деструктор.</li>\n</ul>\n<hr>\n<h3>2. Производный Класс <code class=\"inline-code\">PrivilegedAccount</code></h3>\n<pre><code>class PrivilegedAccount : public BankAccount {{\nprivate:\n    double creditRate;</code></pre>\n<ul>\n<li>Наследует все 4 поля и добавляет <code class=\"inline-code\">creditRate</code> — процент доступного кредита от суммы на счёте.</li>\n</ul>\n<pre><code>    PrivilegedAccount(string o, double s, double r, int y, double cr)\n        : BankAccount(o, s, r, y), creditRate(cr) {{}}</code></pre>\n<ul>\n<li>Конструктор вызывает <code class=\"inline-code\">BankAccount(o, s, r, y)</code> для базовой части, затем инициализирует <code class=\"inline-code\">creditRate</code>.</li>\n</ul>\n<pre><code>    double calcSum() const override {{\n        double base = BankAccount::calcSum();\n        return base + base * (creditRate / 100.0);\n    }}</code></pre>\n<ul>\n<li><strong>Ключевой момент:</strong> сначала вызывается <code class=\"inline-code\">BankAccount::calcSum()</code> для расчёта суммы с процентами, затем добавляется доступный кредит (% от этой суммы).</li>\n<li>Пример: сумма с процентами = 280510, кредит 15% → 280510 + 280510 × 0.15 = 322587.</li>\n</ul>\n<pre><code>    void print() const override {{\n        BankAccount::print();\n        cout &lt;&lt; \"  Тип: Привилегированный счёт\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Процент кредита: \" &lt;&lt; creditRate &lt;&lt; \"%\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Сумма с кредитом: \" &lt;&lt; calcSum() &lt;&lt; \" руб.\" &lt;&lt; endl;\n    }}</code></pre>\n<ul>\n<li><strong>Сначала</strong> <code class=\"inline-code\">BankAccount::print()</code> выводит общую информацию о счёте, затем добавляется информация о кредите.</li>\n</ul>\n<hr>\n<h3>3. Полиморфизм в <code class=\"inline-code\">main</code></h3>\n<pre><code>    BankAccount ba(\"Иванов Иван Иванович\", 100000, 5.0, 3);\n    PrivilegedAccount pa(\"Петров Пётр Петрович\", 200000, 7.0, 5, 15.0);</code></pre>\n<ul>\n<li>Обычный вклад (100000, 5%, 3 года) и привилегированный (200000, 7%, 5 лет, кредит 15%).</li>\n</ul>\n<pre><code>    BankAccount* ptr_b2 = &amp;pa;</code></pre>\n<ul>\n<li><code class=\"inline-code\">BankAccount*</code> указывает на <code class=\"inline-code\">PrivilegedAccount</code>.</li>\n</ul>\n<h4>Статическое связывание</h4>\n<pre><code>    ptr_b2-&gt;BankAccount::print();\n    ptr_b2-&gt;BankAccount::calcSum();</code></pre>\n<ul>\n<li>Явная квалификация вызывает базовые методы. <code class=\"inline-code\">calcSum()</code> считает <strong>без</strong> кредита.</li>\n</ul>\n<h4>Динамическое связывание</h4>\n<pre><code>    ptr_b2-&gt;print();\n    ptr_b2-&gt;calcSum();</code></pre>\n<ul>\n<li>Через vtable вызываются методы <code class=\"inline-code\">PrivilegedAccount</code>. <code class=\"inline-code\">calcSum()</code> возвращает сумму <strong>с</strong> учётом кредита.</li>\n</ul>",
  "explanationRaw": "# Разбор кода: Лабораторная работа 3 (Вариант 14)\n\nНиже представлен полный исходный код из `main.cpp` для удобного ознакомления. \nВ последующих разделах этот код разбит на логические блоки с детальным объяснением конструкций наследования и полиморфизма.\n\n## Полный исходный код программы\n```cpp\n#include <iostream>\n#include <string>\n#include <cmath>\n\nusing namespace std;\n\nclass BankAccount {\nprotected:\n    string owner;\n    double initialSum;\n    double rate;       // % годовых\n    int years;         // время существования вклада\n\npublic:\n    BankAccount(string o, double s, double r, int y)\n        : owner(o), initialSum(s), rate(r), years(y) {}\n\n    virtual double calcSum() const {\n        return initialSum * pow(1.0 + rate / 100.0, years);\n    }\n\n    virtual void print() const {\n        cout << \"Банковский счёт:\" << endl;\n        cout << \"  Владелец: \" << owner << endl;\n        cout << \"  Начальная сумма: \" << initialSum << \" руб.\" << endl;\n        cout << \"  Ставка: \" << rate << \"% годовых\" << endl;\n        cout << \"  Срок вклада: \" << years << \" лет\" << endl;\n        cout << \"  Сумма на счёте: \" << calcSum() << \" руб.\" << endl;\n    }\n\n    virtual ~BankAccount() {}\n};\n\nclass PrivilegedAccount : public BankAccount {\nprivate:\n    double creditRate;  // % кредита от суммы на счёте\n\npublic:\n    PrivilegedAccount(string o, double s, double r, int y, double cr)\n        : BankAccount(o, s, r, y), creditRate(cr) {}\n\n    double calcSum() const override {\n        double base = BankAccount::calcSum();\n        return base + base * (creditRate / 100.0);\n    }\n\n    void print() const override {\n        BankAccount::print();\n        cout << \"  Тип: Привилегированный счёт\" << endl;\n        cout << \"  Процент кредита: \" << creditRate << \"%\" << endl;\n        cout << \"  Сумма с кредитом: \" << calcSum() << \" руб.\" << endl;\n    }\n};\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    cout << \"=== Создание объектов ===\" << endl;\n    BankAccount ba(\"Иванов Иван Иванович\", 100000, 5.0, 3);\n    PrivilegedAccount pa(\"Петров Пётр Петрович\", 200000, 7.0, 5, 15.0);\n\n    cout << \"\\n=== Прямой вызов функций (объекты) ===\" << endl;\n    ba.print();\n    cout << endl;\n    pa.print();\n\n    cout << \"\\n=== 1. СТАТИЧЕСКИЙ ПОЛИМОРФИЗМ (Статическое связывание) ===\" << endl;\n\n    BankAccount* ptr_b1 = &ba;\n    BankAccount* ptr_b2 = &pa;\n\n    cout << \"\\n[Явный вызов базового метода] ptr_b2->BankAccount::print():\" << endl;\n    ptr_b2->BankAccount::print();\n\n    cout << \"\\n[Явный вызов базового calcSum()] ptr_b2->BankAccount::calcSum():\" << endl;\n    cout << \"  Результат (без кредита): \" << ptr_b2->BankAccount::calcSum() << endl;\n\n    cout << \"\\n=== 2. ДИНАМИЧЕСКИЙ ПОЛИМОРФИЗМ (Динамическое связывание) ===\" << endl;\n\n    cout << \"\\nВызов ptr_b1->print() (указывает на BankAccount):\" << endl;\n    ptr_b1->print();\n\n    cout << \"\\nВызов ptr_b2->print() (указывает на PrivilegedAccount):\" << endl;\n    ptr_b2->print();\n\n    cout << \"\\nВызов ptr_b2->calcSum() (указывает на PrivilegedAccount):\" << endl;\n    cout << \"  Результат (с кредитом): \" << ptr_b2->calcSum() << endl;\n\n    return 0;\n}\n\n```\n\n---\n\n## 1. Базовый Класс `BankAccount`\n\n```cpp\nclass BankAccount {{\nprotected:\n    string owner;\n    double initialSum;\n    double rate;\n    int years;\n```\n- Четыре поля, все `protected`:\n  - `owner` — ФИО владельца счёта.\n  - `initialSum` — начальная сумма вклада.\n  - `rate` — ставка вклада (% годовых).\n  - `years` — время существования вклада в годах.\n- **`protected`** обеспечивает доступ из наследника `PrivilegedAccount`, но запрещает из `main()`.\n\n```cpp\n    BankAccount(string o, double s, double r, int y)\n        : owner(o), initialSum(s), rate(r), years(y) {{}}\n```\n- Конструктор инициализирует все четыре поля через **список инициализации**.\n\n```cpp\n    virtual double calcSum() const {{\n        return initialSum * pow(1.0 + rate / 100.0, years);\n    }}\n```\n- Вычисление суммы по формуле **сложных процентов**: S = P × (1 + r/100)ⁿ, где P — начальная сумма, r — ставка, n — срок.\n- Функция `pow()` из `<cmath>` возводит в степень.\n- Пример: 100000 × (1.05)³ = 115762.5.\n- **`virtual`** позволяет привилегированному счёту переопределить расчёт с учётом кредита.\n\n```cpp\n    virtual void print() const {{\n        cout << \"Банковский счёт:\" << endl;\n        cout << \"  Владелец: \" << owner << endl;\n        cout << \"  Начальная сумма: \" << initialSum << \" руб.\" << endl;\n        cout << \"  Ставка: \" << rate << \"% годовых\" << endl;\n        cout << \"  Срок вклада: \" << years << \" лет\" << endl;\n        cout << \"  Сумма на счёте: \" << calcSum() << \" руб.\" << endl;\n    }}\n```\n- Виртуальный метод печати. Вызывает `calcSum()` — через виртуальность будет вызвана правильная версия.\n\n```cpp\n    virtual ~BankAccount() {{}}\n```\n- Виртуальный деструктор.\n\n---\n\n## 2. Производный Класс `PrivilegedAccount`\n\n```cpp\nclass PrivilegedAccount : public BankAccount {{\nprivate:\n    double creditRate;\n```\n- Наследует все 4 поля и добавляет `creditRate` — процент доступного кредита от суммы на счёте.\n\n```cpp\n    PrivilegedAccount(string o, double s, double r, int y, double cr)\n        : BankAccount(o, s, r, y), creditRate(cr) {{}}\n```\n- Конструктор вызывает `BankAccount(o, s, r, y)` для базовой части, затем инициализирует `creditRate`.\n\n```cpp\n    double calcSum() const override {{\n        double base = BankAccount::calcSum();\n        return base + base * (creditRate / 100.0);\n    }}\n```\n- **Ключевой момент:** сначала вызывается `BankAccount::calcSum()` для расчёта суммы с процентами, затем добавляется доступный кредит (% от этой суммы).\n- Пример: сумма с процентами = 280510, кредит 15% → 280510 + 280510 × 0.15 = 322587.\n\n```cpp\n    void print() const override {{\n        BankAccount::print();\n        cout << \"  Тип: Привилегированный счёт\" << endl;\n        cout << \"  Процент кредита: \" << creditRate << \"%\" << endl;\n        cout << \"  Сумма с кредитом: \" << calcSum() << \" руб.\" << endl;\n    }}\n```\n- **Сначала** `BankAccount::print()` выводит общую информацию о счёте, затем добавляется информация о кредите.\n\n---\n\n## 3. Полиморфизм в `main`\n\n```cpp\n    BankAccount ba(\"Иванов Иван Иванович\", 100000, 5.0, 3);\n    PrivilegedAccount pa(\"Петров Пётр Петрович\", 200000, 7.0, 5, 15.0);\n```\n- Обычный вклад (100000, 5%, 3 года) и привилегированный (200000, 7%, 5 лет, кредит 15%).\n\n```cpp\n    BankAccount* ptr_b2 = &pa;\n```\n- `BankAccount*` указывает на `PrivilegedAccount`.\n\n### Статическое связывание\n```cpp\n    ptr_b2->BankAccount::print();\n    ptr_b2->BankAccount::calcSum();\n```\n- Явная квалификация вызывает базовые методы. `calcSum()` считает **без** кредита.\n\n### Динамическое связывание\n```cpp\n    ptr_b2->print();\n    ptr_b2->calcSum();\n```\n- Через vtable вызываются методы `PrivilegedAccount`. `calcSum()` возвращает сумму **с** учётом кредита.",
  "condition": "<h2>Условие лабораторной работы №3 (Вариант 14)</h2>\n<h3>Тема: Наследование классов и Полиморфизм</h3>\n<p><strong>Цель работы:</strong></p>\n<p>Изучить отношения между базовыми и производными классами в C++, а также понять механизмы реализации раннего (статического) и позднего (динамического) связывания вызовов функций.</p>\n<h3>Общее задание:</h3>\n<p>1. Создать иерархию из двух классов (базового родителя и класса-наследника).</p>\n<p>2. Класс-наследник должен использовать конструктор базового класса.</p>\n<p>3. В базовом классе определить виртуальные (<code class=\"inline-code\">virtual</code>) функции. Переопределить их (<code class=\"inline-code\">override</code>) в наследнике.</p>\n<p>4. Внутри переопределенных функций вызывать соответствующие функции из базового класса.</p>\n<p>5. Инкапсулировать поля базового класса с модификатором <code class=\"inline-code\">protected</code>.</p>\n<p>6. В <code class=\"inline-code\">main</code> продемонстрировать статический и динамический полиморфизм.</p>\n<h3>Индивидуальное задание:</h3>\n<ul>\n<li><strong>Базовый класс:</strong> «Простой счёт в банке» (<code class=\"inline-code\">BankAccount</code>)</li>\n<li><em>Поля (protected):</em> ФИО владельца (string), начальная сумма (double), ставка вклада в % годовых (double), время существования вклада в годах (int).</li>\n<li><em>Виртуальные методы:</em></li>\n</ul>\n<p>    1. Вычисление суммы на счёте с учётом начисленных процентов.</p>\n<p>    2. Печать параметров счёта.</p>\n<ul>\n<li><strong>Производный класс:</strong> «Привилегированный счёт» (<code class=\"inline-code\">PrivilegedAccount</code>)</li>\n<li><em>Дополнительное поле:</em> процент кредита (% от доступной суммы на счёте).</li>\n<li><em>Переопределение методов:</em></li>\n</ul>\n<p>    1. Вычисление суммы с учётом доступного кредита.</p>\n<p>    2. Печать параметров (внутри вызываются функции базового класса).</p>",
  "conditionRaw": "# Условие лабораторной работы №3 (Вариант 14)\n\n## Тема: Наследование классов и Полиморфизм\n\n**Цель работы:**\nИзучить отношения между базовыми и производными классами в C++, а также понять механизмы реализации раннего (статического) и позднего (динамического) связывания вызовов функций.\n\n## Общее задание:\n1. Создать иерархию из двух классов (базового родителя и класса-наследника).\n2. Класс-наследник должен использовать конструктор базового класса.\n3. В базовом классе определить виртуальные (`virtual`) функции. Переопределить их (`override`) в наследнике.\n4. Внутри переопределенных функций вызывать соответствующие функции из базового класса.\n5. Инкапсулировать поля базового класса с модификатором `protected`.\n6. В `main` продемонстрировать статический и динамический полиморфизм.\n\n## Индивидуальное задание:\n- **Базовый класс:** «Простой счёт в банке» (`BankAccount`)\n  - *Поля (protected):* ФИО владельца (string), начальная сумма (double), ставка вклада в % годовых (double), время существования вклада в годах (int).\n  - *Виртуальные методы:*\n    1. Вычисление суммы на счёте с учётом начисленных процентов.\n    2. Печать параметров счёта.\n- **Производный класс:** «Привилегированный счёт» (`PrivilegedAccount`)\n  - *Дополнительное поле:* процент кредита (% от доступной суммы на счёте).\n  - *Переопределение методов:*\n    1. Вычисление суммы с учётом доступного кредита.\n    2. Печать параметров (внутри вызываются функции базового класса).",
  "theory": "<h2>Теория: Наследование и Полиморфизм в C++</h2>\n<p>В данной лабораторной работе изучаются два ключевых принципа ООП: наследование и полиморфизм.</p>\n<h3>1. Наследование</h3>\n<p>Наследование позволяет создать новый класс (наследник / производный) на основе уже существующего (базового родителя). Класс-наследник получает (наследует) все поля и методы базового класса и может добавлять свои собственные.</p>\n<pre><code>class RealNumber { ... };\nclass ComplexNumber : public RealNumber { ... }; </code></pre>\n<p>Слово <code class=\"inline-code\">public</code> перед названием родителя означает \"публичное наследование\". Это означает, что <code class=\"inline-code\">public</code> члены родителя остаются <code class=\"inline-code\">public</code> в наследнике, а <code class=\"inline-code\">protected</code> остаются <code class=\"inline-code\">protected</code>.</p>\n<h4>Модификатор <code class=\"inline-code\">protected</code></h4>\n<p>В ЛР1 мы использовали <code class=\"inline-code\">private</code> для сокрытия полей. Но <code class=\"inline-code\">private</code> поля недоступны даже классам-наследникам. Чтобы поле нельзя было изменить извне (<code class=\"inline-code\">main</code>), но можно было использовать внутри наследников, применяется спецификатор <code class=\"inline-code\">protected</code>. Именно поэтому поля базового класса объявляются как <code class=\"inline-code\">protected</code>.</p>\n<h4>Вызов конструктора родителя</h4>\n<p>Конструктор наследника <strong>обязан</strong> инициализировать часть объекта, которая досталась ему от базового класса. Делается это в списке инициализации:</p>\n<pre><code>ComplexNumber(double real, double imag) : RealNumber(real) {\n    imaginary = imag;\n}</code></pre>\n<h3>2. Полиморфизм</h3>\n<p>Полиморфизм — это свойство системы использовать объекты с одинаковым интерфейсом без информации о типе и внутренней структуре объекта (возможность \"функции работать по-разному\" для разных объектов).</p>\n<h4>Статический полиморфизм (Статическое связывание)</h4>\n<p>Связывание вызова функции с её телом происходит на этапе <strong>компиляции</strong> (раннее связывание).</p>\n<p>Примеры:</p>\n<p>1. Перегрузка функций (две функции с одним именем, но разными типами аргументов).</p>\n<p>2. Вызов обычного не-виртуального метода через указатель базового типа:</p>\n<p>   ```cpp</p>\n<p>   Base* ptr = &derivedObj;</p>\n<p>   ptr->Base::method(); // Всегда вызовет метод базового класса</p>\n<p>   ```</p>\n<h4>Динамический полиморфизм (Динамическое связывание)</h4>\n<p>Связывание происходит на этапе <strong>выполнения</strong> (позднее связывание).</p>\n<p>Достигается за счет использования ключевого слова <code class=\"inline-code\">virtual</code> перед методом в базовом классе.</p>\n<p>Как это работает:</p>\n<p>1. Когда компилятор видит модификатор <code class=\"inline-code\">virtual</code>, он создает специальную невидимую <strong>таблицу виртуальных функций (vtable)</strong> для этого класса.</p>\n<p>2. В объект незаметно добавляется указатель на эту таблицу (<code class=\"inline-code\">vptr</code>).</p>\n<p>3. При вызове виртуального метода через родительский указатель (<code class=\"inline-code\">ptr->print()</code>), программа сначала идет по указателю <code class=\"inline-code\">vptr</code> в таблицу и находит <strong>правильный</strong> адрес функции, который соответствует <em>реальному</em> типу объекта, лежащему в памяти.</p>\n<pre><code>Base* ptr = &amp;derivedObj; // Указатель базовый, но объект наследника\nptr-&gt;print(); // Вызовет Derived::print(), а не базовую функцию!</code></pre>\n<p>Это самая мощная архитектурная особенность С++, позволяющая писать универсальный код для обработки разных типов данных через их общий базовый интерфейс.</p>",
  "theoryRaw": "# Теория: Наследование и Полиморфизм в C++\n\nВ данной лабораторной работе изучаются два ключевых принципа ООП: наследование и полиморфизм.\n\n## 1. Наследование\nНаследование позволяет создать новый класс (наследник / производный) на основе уже существующего (базового родителя). Класс-наследник получает (наследует) все поля и методы базового класса и может добавлять свои собственные.\n\n```cpp\nclass RealNumber { ... };\nclass ComplexNumber : public RealNumber { ... }; \n```\nСлово `public` перед названием родителя означает \"публичное наследование\". Это означает, что `public` члены родителя остаются `public` в наследнике, а `protected` остаются `protected`.\n\n### Модификатор `protected`\nВ ЛР1 мы использовали `private` для сокрытия полей. Но `private` поля недоступны даже классам-наследникам. Чтобы поле нельзя было изменить извне (`main`), но можно было использовать внутри наследников, применяется спецификатор `protected`. Именно поэтому поля базового класса объявляются как `protected`.\n\n### Вызов конструктора родителя\nКонструктор наследника **обязан** инициализировать часть объекта, которая досталась ему от базового класса. Делается это в списке инициализации:\n```cpp\nComplexNumber(double real, double imag) : RealNumber(real) {\n    imaginary = imag;\n}\n```\n\n## 2. Полиморфизм\nПолиморфизм — это свойство системы использовать объекты с одинаковым интерфейсом без информации о типе и внутренней структуре объекта (возможность \"функции работать по-разному\" для разных объектов).\n\n### Статический полиморфизм (Статическое связывание)\nСвязывание вызова функции с её телом происходит на этапе **компиляции** (раннее связывание).\nПримеры:\n1. Перегрузка функций (две функции с одним именем, но разными типами аргументов).\n2. Вызов обычного не-виртуального метода через указатель базового типа:\n   ```cpp\n   Base* ptr = &derivedObj;\n   ptr->Base::method(); // Всегда вызовет метод базового класса\n   ```\n\n### Динамический полиморфизм (Динамическое связывание)\nСвязывание происходит на этапе **выполнения** (позднее связывание).\nДостигается за счет использования ключевого слова `virtual` перед методом в базовом классе.\n\nКак это работает:\n1. Когда компилятор видит модификатор `virtual`, он создает специальную невидимую **таблицу виртуальных функций (vtable)** для этого класса.\n2. В объект незаметно добавляется указатель на эту таблицу (`vptr`).\n3. При вызове виртуального метода через родительский указатель (`ptr->print()`), программа сначала идет по указателю `vptr` в таблицу и находит **правильный** адрес функции, который соответствует *реальному* типу объекта, лежащему в памяти.\n\n```cpp\nBase* ptr = &derivedObj; // Указатель базовый, но объект наследника\nptr->print(); // Вызовет Derived::print(), а не базовую функцию!\n```\nЭто самая мощная архитектурная особенность С++, позволяющая писать универсальный код для обработки разных типов данных через их общий базовый интерфейс."
}