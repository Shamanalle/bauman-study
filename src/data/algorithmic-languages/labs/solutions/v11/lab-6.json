{
  "variant": 11,
  "lab": 6,
  "code": "#include <iostream>\n#include <string>\n#include <stdexcept>\n\nusing namespace std;\n\nclass DateException : public out_of_range {\nprivate:\n    int err_day;\n    int err_month;\n    int err_year;\n\npublic:\n    DateException(const string& message, int d, int m, int y)\n        : out_of_range(message), err_day(d), err_month(m), err_year(y) {}\n\n    void printErrorData() const {\n        cout << \"[Детали исключения] Недопустимая дата:\" << endl;\n        cout << \"  День:  \" << err_day   << \" (допустимо: 1-31)\" << endl;\n        cout << \"  Месяц: \" << err_month << \" (допустимо: 1-12)\" << endl;\n        cout << \"  Год:   \" << err_year  << \" (допустимо: 1900-2100)\" << endl;\n    }\n};\n\nclass Date {\nprivate:\n    int day;\n    int month;\n    int year;\n\npublic:\n    Date(int d, int m, int y) {\n        if (d < 1 || d > 31 || m < 1 || m > 12 || y < 1900 || y > 2100) {\n            throw DateException(\n                \"Одно или несколько полей даты выходят за допустимый диапазон!\",\n                d, m, y\n            );\n        }\n        day = d;\n        month = m;\n        year = y;\n    }\n\n    void print() const {\n        cout << \"Дата: \" << day << \".\" << month << \".\" << year << endl;\n    }\n};\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    cout << \"Демонстрация работы БЕЗ возникновения исключения:\" << endl;\n    try {\n        Date validDate(15, 6, 2024);\n        cout << \"Объект успешно создан!\" << endl;\n        validDate.print();\n    }\n    catch (const DateException& e) {\n        cerr << \"Перехвачено исключение: \" << e.what() << endl;\n        e.printErrorData();\n    }\n    catch (const exception& e) {\n        cerr << \"Стандартное исключение: \" << e.what() << endl;\n    }\n\n    cout << \"\\nДемонстрация работы С возникновением исключения:\" << endl;\n    try {\n        Date invalidDate(32, 13, 1800);\n\n        cout << \"Этот текст не напечатается, объект не создан.\" << endl;\n        invalidDate.print();\n    }\n    catch (const DateException& e) {\n        cerr << \"\\nПерехвачено ожидаемое исключение:\" << endl;\n        cerr << \"Сообщение (базовый класс out_of_range): \" << e.what() << endl;\n        e.printErrorData();\n    }\n    catch (const out_of_range& e) {\n        cerr << \"out_of_range перехвачен: \" << e.what() << endl;\n    }\n    catch (const exception& e) {\n        cerr << \"Произошла неизвестная ошибка: \" << e.what() << endl;\n    }\n\n    cout << \"\\nПрограмма успешно завершила свою работу после обработки исключения.\" << endl;\n\n    return 0;\n}",
  "codeHtml": "<pre><code>#include &lt;iostream&gt;\n#include &lt;string&gt;\n#include &lt;stdexcept&gt;\n\nusing namespace std;\n\nclass DateException : public out_of_range {\nprivate:\n    int err_day;\n    int err_month;\n    int err_year;\n\npublic:\n    DateException(const string&amp; message, int d, int m, int y)\n        : out_of_range(message), err_day(d), err_month(m), err_year(y) {}\n\n    void printErrorData() const {\n        cout &lt;&lt; \"[Детали исключения] Недопустимая дата:\" &lt;&lt; endl;\n        cout &lt;&lt; \"  День:  \" &lt;&lt; err_day   &lt;&lt; \" (допустимо: 1-31)\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Месяц: \" &lt;&lt; err_month &lt;&lt; \" (допустимо: 1-12)\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Год:   \" &lt;&lt; err_year  &lt;&lt; \" (допустимо: 1900-2100)\" &lt;&lt; endl;\n    }\n};\n\nclass Date {\nprivate:\n    int day;\n    int month;\n    int year;\n\npublic:\n    Date(int d, int m, int y) {\n        if (d &lt; 1 || d &gt; 31 || m &lt; 1 || m &gt; 12 || y &lt; 1900 || y &gt; 2100) {\n            throw DateException(\n                \"Одно или несколько полей даты выходят за допустимый диапазон!\",\n                d, m, y\n            );\n        }\n        day = d;\n        month = m;\n        year = y;\n    }\n\n    void print() const {\n        cout &lt;&lt; \"Дата: \" &lt;&lt; day &lt;&lt; \".\" &lt;&lt; month &lt;&lt; \".\" &lt;&lt; year &lt;&lt; endl;\n    }\n};\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    cout &lt;&lt; \"Демонстрация работы БЕЗ возникновения исключения:\" &lt;&lt; endl;\n    try {\n        Date validDate(15, 6, 2024);\n        cout &lt;&lt; \"Объект успешно создан!\" &lt;&lt; endl;\n        validDate.print();\n    }\n    catch (const DateException&amp; e) {\n        cerr &lt;&lt; \"Перехвачено исключение: \" &lt;&lt; e.what() &lt;&lt; endl;\n        e.printErrorData();\n    }\n    catch (const exception&amp; e) {\n        cerr &lt;&lt; \"Стандартное исключение: \" &lt;&lt; e.what() &lt;&lt; endl;\n    }\n\n    cout &lt;&lt; \"\\nДемонстрация работы С возникновением исключения:\" &lt;&lt; endl;\n    try {\n        Date invalidDate(32, 13, 1800);\n\n        cout &lt;&lt; \"Этот текст не напечатается, объект не создан.\" &lt;&lt; endl;\n        invalidDate.print();\n    }\n    catch (const DateException&amp; e) {\n        cerr &lt;&lt; \"\\nПерехвачено ожидаемое исключение:\" &lt;&lt; endl;\n        cerr &lt;&lt; \"Сообщение (базовый класс out_of_range): \" &lt;&lt; e.what() &lt;&lt; endl;\n        e.printErrorData();\n    }\n    catch (const out_of_range&amp; e) {\n        cerr &lt;&lt; \"out_of_range перехвачен: \" &lt;&lt; e.what() &lt;&lt; endl;\n    }\n    catch (const exception&amp; e) {\n        cerr &lt;&lt; \"Произошла неизвестная ошибка: \" &lt;&lt; e.what() &lt;&lt; endl;\n    }\n\n    cout &lt;&lt; \"\\nПрограмма успешно завершила свою работу после обработки исключения.\" &lt;&lt; endl;\n\n    return 0;\n}</code></pre>",
  "explanation": "<h2>Разбор кода: Лабораторная работа 6 (Вариант 11)</h2>\n<p>Ниже представлен полный исходный код из <code class=\"inline-code\">main.cpp</code>.</p>\n<p>В последующих разделах этот код разбит на логические блоки с детальным объяснением механизма исключений.</p>\n<h3>Полный исходный код программы</h3>\n<pre><code>#include &lt;iostream&gt;\n#include &lt;string&gt;\n#include &lt;stdexcept&gt;\n\nusing namespace std;\n\nclass DateException : public out_of_range {\nprivate:\n    int err_day;\n    int err_month;\n    int err_year;\n\npublic:\n    DateException(const string&amp; message, int d, int m, int y)\n        : out_of_range(message), err_day(d), err_month(m), err_year(y) {}\n\n    void printErrorData() const {\n        cout &lt;&lt; \"[Детали исключения] Недопустимая дата:\" &lt;&lt; endl;\n        cout &lt;&lt; \"  День:  \" &lt;&lt; err_day   &lt;&lt; \" (допустимо: 1-31)\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Месяц: \" &lt;&lt; err_month &lt;&lt; \" (допустимо: 1-12)\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Год:   \" &lt;&lt; err_year  &lt;&lt; \" (допустимо: 1900-2100)\" &lt;&lt; endl;\n    }\n};\n\nclass Date {\nprivate:\n    int day;\n    int month;\n    int year;\n\npublic:\n    Date(int d, int m, int y) {\n        if (d &lt; 1 || d &gt; 31 || m &lt; 1 || m &gt; 12 || y &lt; 1900 || y &gt; 2100) {\n            throw DateException(\n                \"Одно или несколько полей даты выходят за допустимый диапазон!\",\n                d, m, y\n            );\n        }\n        day = d;\n        month = m;\n        year = y;\n    }\n\n    void print() const {\n        cout &lt;&lt; \"Дата: \" &lt;&lt; day &lt;&lt; \".\" &lt;&lt; month &lt;&lt; \".\" &lt;&lt; year &lt;&lt; endl;\n    }\n};\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    cout &lt;&lt; \"Демонстрация работы БЕЗ возникновения исключения:\" &lt;&lt; endl;\n    try {\n        Date validDate(15, 6, 2024);\n        cout &lt;&lt; \"Объект успешно создан!\" &lt;&lt; endl;\n        validDate.print();\n    }\n    catch (const DateException&amp; e) {\n        cerr &lt;&lt; \"Перехвачено исключение: \" &lt;&lt; e.what() &lt;&lt; endl;\n        e.printErrorData();\n    }\n    catch (const exception&amp; e) {\n        cerr &lt;&lt; \"Стандартное исключение: \" &lt;&lt; e.what() &lt;&lt; endl;\n    }\n\n    cout &lt;&lt; \"\\nДемонстрация работы С возникновением исключения:\" &lt;&lt; endl;\n    try {\n        Date invalidDate(32, 13, 1800);\n\n        cout &lt;&lt; \"Этот текст не напечатается, объект не создан.\" &lt;&lt; endl;\n        invalidDate.print();\n    }\n    catch (const DateException&amp; e) {\n        cerr &lt;&lt; \"\\nПерехвачено ожидаемое исключение:\" &lt;&lt; endl;\n        cerr &lt;&lt; \"Сообщение (базовый класс out_of_range): \" &lt;&lt; e.what() &lt;&lt; endl;\n        e.printErrorData();\n    }\n    catch (const out_of_range&amp; e) {\n        cerr &lt;&lt; \"out_of_range перехвачен: \" &lt;&lt; e.what() &lt;&lt; endl;\n    }\n    catch (const exception&amp; e) {\n        cerr &lt;&lt; \"Произошла неизвестная ошибка: \" &lt;&lt; e.what() &lt;&lt; endl;\n    }\n\n    cout &lt;&lt; \"\\nПрограмма успешно завершила свою работу после обработки исключения.\" &lt;&lt; endl;\n\n    return 0;\n}\n</code></pre>\n<hr>\n<h3>1. Свой класс Исключения</h3>\n<pre><code>class DateException : public out_of_range {{\nprivate:\n    int err_day, err_month, err_year;</code></pre>\n<p>Наследуем от <code class=\"inline-code\">out_of_range</code>. Три поля хранят \"забракованные\" значения — день, месяц и год. Это позволяет в блоке <code class=\"inline-code\">catch</code> точно узнать, чем именно вызвана ошибка.</p>\n<pre><code>    DateException(const string&amp; message, int d, int m, int y)\n        : out_of_range(message), err_day(d), err_month(m), err_year(y) {{}}</code></pre>\n<p>Конструктор вызывает родительский <code class=\"inline-code\">out_of_range(message)</code> и сохраняет плохие значения. Метод <code class=\"inline-code\">e.what()</code> вернёт текстовое сообщение.</p>\n<h3>2. Интеграция в класс Date и блок <code class=\"inline-code\">throw</code></h3>\n<pre><code>    Date(int d, int m, int y) {{\n        if (d &lt; 1 || d &gt; 31 || m &lt; 1 || m &gt; 12 || y &lt; 1900 || y &gt; 2100) {{\n            throw DateException(\"Сообщение!\", d, m, y);\n        }}\n        day = d; month = m; year = y;\n    }}</code></pre>\n<p>В конструкторе <code class=\"inline-code\">Date</code> валидация: день 1–31, месяц 1–12, год 1900–2100. При нарушении — <strong><code class=\"inline-code\">throw</code></strong>: объект не создаётся, программа откатывается к <code class=\"inline-code\">catch</code>.</p>\n<h3>3. Блоки <code class=\"inline-code\">try - catch</code></h3>\n<pre><code>    try {{\n        Date invalidDate(32, 13, 1800);\n        cout &lt;&lt; \"Этот текст не напечатается\";\n    }}</code></pre>\n<p>В <code class=\"inline-code\">try</code> происходит попытка создания. При <code class=\"inline-code\">throw</code> код \"выпрыгивает\" и ищет <code class=\"inline-code\">catch</code>.</p>\n<pre><code>    catch (const DateException&amp; e) {{\n        cerr &lt;&lt; e.what() &lt;&lt; endl;\n        e.printErrorData();\n    }}</code></pre>\n<p>Первым ловим <code class=\"inline-code\">DateException</code> — самый специфичный тип. <code class=\"inline-code\">e.printErrorData()</code> выведет значения <code class=\"inline-code\">(32, 13, 1800)</code>.</p>\n<pre><code>    catch (const out_of_range&amp; e) {{ ... }}\n    catch (const exception&amp; e) {{ ... }}</code></pre>\n<p>Резервные блоки от специфичного к общему. Полиморфизм гарантирует правильный порядок обработки.</p>",
  "explanationRaw": "# Разбор кода: Лабораторная работа 6 (Вариант 11)\n\nНиже представлен полный исходный код из `main.cpp`.\nВ последующих разделах этот код разбит на логические блоки с детальным объяснением механизма исключений.\n\n## Полный исходный код программы\n```cpp\n#include <iostream>\n#include <string>\n#include <stdexcept>\n\nusing namespace std;\n\nclass DateException : public out_of_range {\nprivate:\n    int err_day;\n    int err_month;\n    int err_year;\n\npublic:\n    DateException(const string& message, int d, int m, int y)\n        : out_of_range(message), err_day(d), err_month(m), err_year(y) {}\n\n    void printErrorData() const {\n        cout << \"[Детали исключения] Недопустимая дата:\" << endl;\n        cout << \"  День:  \" << err_day   << \" (допустимо: 1-31)\" << endl;\n        cout << \"  Месяц: \" << err_month << \" (допустимо: 1-12)\" << endl;\n        cout << \"  Год:   \" << err_year  << \" (допустимо: 1900-2100)\" << endl;\n    }\n};\n\nclass Date {\nprivate:\n    int day;\n    int month;\n    int year;\n\npublic:\n    Date(int d, int m, int y) {\n        if (d < 1 || d > 31 || m < 1 || m > 12 || y < 1900 || y > 2100) {\n            throw DateException(\n                \"Одно или несколько полей даты выходят за допустимый диапазон!\",\n                d, m, y\n            );\n        }\n        day = d;\n        month = m;\n        year = y;\n    }\n\n    void print() const {\n        cout << \"Дата: \" << day << \".\" << month << \".\" << year << endl;\n    }\n};\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    cout << \"Демонстрация работы БЕЗ возникновения исключения:\" << endl;\n    try {\n        Date validDate(15, 6, 2024);\n        cout << \"Объект успешно создан!\" << endl;\n        validDate.print();\n    }\n    catch (const DateException& e) {\n        cerr << \"Перехвачено исключение: \" << e.what() << endl;\n        e.printErrorData();\n    }\n    catch (const exception& e) {\n        cerr << \"Стандартное исключение: \" << e.what() << endl;\n    }\n\n    cout << \"\\nДемонстрация работы С возникновением исключения:\" << endl;\n    try {\n        Date invalidDate(32, 13, 1800);\n\n        cout << \"Этот текст не напечатается, объект не создан.\" << endl;\n        invalidDate.print();\n    }\n    catch (const DateException& e) {\n        cerr << \"\\nПерехвачено ожидаемое исключение:\" << endl;\n        cerr << \"Сообщение (базовый класс out_of_range): \" << e.what() << endl;\n        e.printErrorData();\n    }\n    catch (const out_of_range& e) {\n        cerr << \"out_of_range перехвачен: \" << e.what() << endl;\n    }\n    catch (const exception& e) {\n        cerr << \"Произошла неизвестная ошибка: \" << e.what() << endl;\n    }\n\n    cout << \"\\nПрограмма успешно завершила свою работу после обработки исключения.\" << endl;\n\n    return 0;\n}\n\n```\n\n---\n\n## 1. Свой класс Исключения\n```cpp\nclass DateException : public out_of_range {{\nprivate:\n    int err_day, err_month, err_year;\n```\nНаследуем от `out_of_range`. Три поля хранят \"забракованные\" значения — день, месяц и год. Это позволяет в блоке `catch` точно узнать, чем именно вызвана ошибка.\n\n```cpp\n    DateException(const string& message, int d, int m, int y)\n        : out_of_range(message), err_day(d), err_month(m), err_year(y) {{}}\n```\nКонструктор вызывает родительский `out_of_range(message)` и сохраняет плохие значения. Метод `e.what()` вернёт текстовое сообщение.\n\n## 2. Интеграция в класс Date и блок `throw`\n```cpp\n    Date(int d, int m, int y) {{\n        if (d < 1 || d > 31 || m < 1 || m > 12 || y < 1900 || y > 2100) {{\n            throw DateException(\"Сообщение!\", d, m, y);\n        }}\n        day = d; month = m; year = y;\n    }}\n```\nВ конструкторе `Date` валидация: день 1–31, месяц 1–12, год 1900–2100. При нарушении — **`throw`**: объект не создаётся, программа откатывается к `catch`.\n\n## 3. Блоки `try - catch`\n```cpp\n    try {{\n        Date invalidDate(32, 13, 1800);\n        cout << \"Этот текст не напечатается\";\n    }}\n```\nВ `try` происходит попытка создания. При `throw` код \"выпрыгивает\" и ищет `catch`.\n\n```cpp\n    catch (const DateException& e) {{\n        cerr << e.what() << endl;\n        e.printErrorData();\n    }}\n```\nПервым ловим `DateException` — самый специфичный тип. `e.printErrorData()` выведет значения `(32, 13, 1800)`.\n\n```cpp\n    catch (const out_of_range& e) {{ ... }}\n    catch (const exception& e) {{ ... }}\n```\nРезервные блоки от специфичного к общему. Полиморфизм гарантирует правильный порядок обработки.",
  "condition": "<h2>Условие лабораторной работы №6 (Вариант 11)</h2>\n<h3>Тема: Обработка исключений (Exceptions) в С++</h3>\n<p><strong>Цель работы:</strong></p>\n<p>Освоить механизм безопасности и обработки ошибок времени выполнения: создание собственных классов-исключений, генерацию исключений (throw) и их перехват с помощью блоков try-catch.</p>\n<h3>Общее задание:</h3>\n<p>1. Создать собственный класс исключительной ситуации, наследуемый от стандартного исключения.</p>\n<p>2. Класс должен содержать приватные поля для хранения некорректных значений и метод <code class=\"inline-code\">printErrorData()</code>.</p>\n<p>3. Разработать целевой класс. В конструкторе реализовать валидацию параметров и генерацию исключения при недопустимых значениях.</p>\n<p>4. В <code class=\"inline-code\">main</code> продемонстрировать перехват через <code class=\"inline-code\">try-catch</code>.</p>\n<h3>Индивидуальное задание:</h3>\n<ul>\n<li>Формула: <em>11 % 9 = <strong>2</strong></em>.</li>\n<li><strong>Целевой класс:</strong> «Дата» (<code class=\"inline-code\">Date</code>). Поля: число (1–31), месяц (1–12), год (1900–2100).</li>\n<li><strong>Исключение:</strong> наследник <code class=\"inline-code\">std::out_of_range</code>.</li>\n</ul>",
  "conditionRaw": "# Условие лабораторной работы №6 (Вариант 11)\n\n## Тема: Обработка исключений (Exceptions) в С++\n\n**Цель работы:**\nОсвоить механизм безопасности и обработки ошибок времени выполнения: создание собственных классов-исключений, генерацию исключений (throw) и их перехват с помощью блоков try-catch.\n\n## Общее задание:\n1. Создать собственный класс исключительной ситуации, наследуемый от стандартного исключения.\n2. Класс должен содержать приватные поля для хранения некорректных значений и метод `printErrorData()`.\n3. Разработать целевой класс. В конструкторе реализовать валидацию параметров и генерацию исключения при недопустимых значениях.\n4. В `main` продемонстрировать перехват через `try-catch`.\n\n## Индивидуальное задание:\n- Формула: *11 % 9 = **2***.\n- **Целевой класс:** «Дата» (`Date`). Поля: число (1–31), месяц (1–12), год (1900–2100).\n- **Исключение:** наследник `std::out_of_range`.",
  "theory": "<h2>Теория: Обработка исключений (Exceptions) в C++</h2>\n<p>В данной лабораторной работе изучается механизм перехвата и обработки исключений (ошибок времени выполнения).</p>\n<h3>1. Что такое исключения?</h3>\n<p>Обычно, когда в программе возникает критическая ошибка (например: деление на ноль, выход за границы массива или создание объекта с недопустимыми параметрами), программа просто завершается (падает) с системной ошибкой.</p>\n<p>Исключения (<code class=\"inline-code\">exceptions</code>) — это элегантный способ \"предупредить\" программу об ошибке и передать управление специальному блоку кода, который знает, как эту ошибку \"обработать\" (например, вывести понятное сообщение пользователю), не давая при этом программе \"упасть\".</p>\n<p>Механизм исключений строится на 3 ключевых словах:</p>\n<ul>\n<li><strong><code class=\"inline-code\">try</code> (попробовать):</strong> блок кода, в котором <em>может</em> возникнуть исключение.</li>\n<li><strong><code class=\"inline-code\">throw</code> (бросить):</strong> само действие \"выброса\" (генерации) ошибки, когда функция понимает, что дальше работать нельзя (например, поступили плохие параметры).</li>\n<li><strong><code class=\"inline-code\">catch</code> (поймать):</strong> блок кода, который перехватывает \"брошенную\" ошибку и восстанавливает работоспособность системы.</li>\n</ul>\n<h3>2. Иерархия исключений в C++</h3>\n<p>В C++ есть стандартная библиотека исключений <code class=\"inline-code\"><stdexcept></code>. В её корне лежит класс <code class=\"inline-code\">std::exception</code>. От него наследуются конкретные классы:</p>\n<ul>\n<li><code class=\"inline-code\">std::logic_error</code> (ошибки логики, такие как <code class=\"inline-code\">std::invalid_argument</code> или <code class=\"inline-code\">std::out_of_range</code>)</li>\n<li><code class=\"inline-code\">std::runtime_error</code> (ошибки времени выполнения)</li>\n</ul>\n<p>В лабораторной мы создаем свой собственный тип ошибки (свой класс), наследуясь от стандартного исключения:</p>\n<pre><code>class MyException : public std::out_of_range { ... };\n// или\nclass MyException : public std::invalid_argument { ... };</code></pre>\n<p>Мы обязаны вызвать конструктор базового класса, передав ему текстовое сообщение об ошибке (оно сохранится, и его можно будет получить методом <code class=\"inline-code\">what()</code>).</p>\n<h3>3. Дополнительные данные в объекте исключения</h3>\n<p>Главное преимущество создания <strong>собственного</strong> класса исключения в том, что мы можем положить в него не только текстовое сообщение (как в стандартных ошибках), но и <strong>любые другие переменные</strong>, которые помогут программисту понять, почему произошла ошибка.</p>\n<p>В момент генерации ошибки конструктор целевого класса \"бросает\" исключение и кладет туда \"плохие\" значения:</p>\n<pre><code>throw MyException(\"Ошибка!\", bad_value1, bad_value2);</code></pre>\n<p>А в блоке обработки (<code class=\"inline-code\">catch</code>) мы можем вызвать метод <code class=\"inline-code\">printErrorData()</code>, который напечатает эти данные.</p>\n<h3>4. Порядок блоков <code class=\"inline-code\">catch</code></h3>\n<p>При написании нескольких <code class=\"inline-code\">catch</code> подряд <strong>крайне важно</strong> располагать их от самых специфичных к самым общим.</p>\n<p>1. Сначала ловим <code class=\"inline-code\">catch (const MyException& e)</code> (наследник).</p>\n<p>2. Потом ловим <code class=\"inline-code\">catch (const std::out_of_range& e)</code> (родитель).</p>\n<p>3. Потом <code class=\"inline-code\">catch (const std::exception& e)</code> (самый базовый прародитель всех ошибок).</p>\n<p>Если бы мы поставили <code class=\"inline-code\">catch (const std::exception& e)</code> первым, полиморфизм привел бы к тому, что он \"поймал\" бы <em>все</em> исключения, включая наше. Обратите внимание, что объекты-исключения всегда перехватываются <strong>по константной ссылке</strong> <code class=\"inline-code\">const &</code> (во избежание срезания полей объекта при копировании).</p>",
  "theoryRaw": "# Теория: Обработка исключений (Exceptions) в C++\n\nВ данной лабораторной работе изучается механизм перехвата и обработки исключений (ошибок времени выполнения).\n\n## 1. Что такое исключения?\nОбычно, когда в программе возникает критическая ошибка (например: деление на ноль, выход за границы массива или создание объекта с недопустимыми параметрами), программа просто завершается (падает) с системной ошибкой.\nИсключения (`exceptions`) — это элегантный способ \"предупредить\" программу об ошибке и передать управление специальному блоку кода, который знает, как эту ошибку \"обработать\" (например, вывести понятное сообщение пользователю), не давая при этом программе \"упасть\".\n\nМеханизм исключений строится на 3 ключевых словах:\n- **`try` (попробовать):** блок кода, в котором *может* возникнуть исключение.\n- **`throw` (бросить):** само действие \"выброса\" (генерации) ошибки, когда функция понимает, что дальше работать нельзя (например, поступили плохие параметры).\n- **`catch` (поймать):** блок кода, который перехватывает \"брошенную\" ошибку и восстанавливает работоспособность системы.\n\n## 2. Иерархия исключений в C++\nВ C++ есть стандартная библиотека исключений `<stdexcept>`. В её корне лежит класс `std::exception`. От него наследуются конкретные классы:\n- `std::logic_error` (ошибки логики, такие как `std::invalid_argument` или `std::out_of_range`)\n- `std::runtime_error` (ошибки времени выполнения)\n\nВ лабораторной мы создаем свой собственный тип ошибки (свой класс), наследуясь от стандартного исключения:\n\n```cpp\nclass MyException : public std::out_of_range { ... };\n// или\nclass MyException : public std::invalid_argument { ... };\n```\n\nМы обязаны вызвать конструктор базового класса, передав ему текстовое сообщение об ошибке (оно сохранится, и его можно будет получить методом `what()`).\n\n## 3. Дополнительные данные в объекте исключения\nГлавное преимущество создания **собственного** класса исключения в том, что мы можем положить в него не только текстовое сообщение (как в стандартных ошибках), но и **любые другие переменные**, которые помогут программисту понять, почему произошла ошибка.\n\nВ момент генерации ошибки конструктор целевого класса \"бросает\" исключение и кладет туда \"плохие\" значения:\n\n```cpp\nthrow MyException(\"Ошибка!\", bad_value1, bad_value2);\n```\n\nА в блоке обработки (`catch`) мы можем вызвать метод `printErrorData()`, который напечатает эти данные.\n\n## 4. Порядок блоков `catch`\nПри написании нескольких `catch` подряд **крайне важно** располагать их от самых специфичных к самым общим.\n1. Сначала ловим `catch (const MyException& e)` (наследник).\n2. Потом ловим `catch (const std::out_of_range& e)` (родитель).\n3. Потом `catch (const std::exception& e)` (самый базовый прародитель всех ошибок).\n\nЕсли бы мы поставили `catch (const std::exception& e)` первым, полиморфизм привел бы к тому, что он \"поймал\" бы *все* исключения, включая наше. Обратите внимание, что объекты-исключения всегда перехватываются **по константной ссылке** `const &` (во избежание срезания полей объекта при копировании)."
}