{
  "variant": 30,
  "lab": 1,
  "code": "#include <iostream>\n#include <vector>\n#include <string>\n#include <cmath>\n\nusing namespace std;\n\nclass BankDeposit {\nprivate:\n    string owner;\n    double amount;\n    double rate;\n\npublic:\n    BankDeposit(string o, double a, double r) {\n        owner = o;\n        amount = a;\n        rate = r;\n    }\n\n    double calcAmount(int years) const {\n        return amount * pow(1.0 + rate / 100.0, years);\n    }\n\n    string getOwner() const { return owner; }\n    double getAmount() const { return amount; }\n    double getRate() const { return rate; }\n\n    void print() const {\n        cout << \"Владелец: \" << owner\n             << \", Сумма: \" << amount\n             << \", Ставка: \" << rate << \"%\" << endl;\n    }\n};\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    vector<BankDeposit> deposits = {\n        BankDeposit(\"Иванов И.И.\", 100000, 7.5),\n        BankDeposit(\"Петров П.П.\", 250000, 5.0),\n        BankDeposit(\"Сидоров С.С.\", 50000, 10.0),\n        BankDeposit(\"Козлова А.В.\", 180000, 6.5),\n        BankDeposit(\"Морозов Д.Е.\", 300000, 4.5)\n    };\n\n    cout << \"Все вклады:\" << endl;\n    for (const auto& dep : deposits) {\n        dep.print();\n    }\n\n    int years;\n    cout << \"\\nВведите количество лет: \";\n    cin >> years;\n\n    double totalSum = 0;\n    cout << \"\\nСуммы через \" << years << \" лет:\" << endl;\n    for (const auto& dep : deposits) {\n        double futureAmount = dep.calcAmount(years);\n        cout << dep.getOwner() << \": \" << futureAmount << endl;\n        totalSum += futureAmount;\n    }\n    cout << \"\\nОбщая сумма на всех счетах: \" << totalSum << endl;\n\n    return 0;\n}",
  "codeHtml": "<pre><code>#include &lt;iostream&gt;\n#include &lt;vector&gt;\n#include &lt;string&gt;\n#include &lt;cmath&gt;\n\nusing namespace std;\n\nclass BankDeposit {\nprivate:\n    string owner;\n    double amount;\n    double rate;\n\npublic:\n    BankDeposit(string o, double a, double r) {\n        owner = o;\n        amount = a;\n        rate = r;\n    }\n\n    double calcAmount(int years) const {\n        return amount * pow(1.0 + rate / 100.0, years);\n    }\n\n    string getOwner() const { return owner; }\n    double getAmount() const { return amount; }\n    double getRate() const { return rate; }\n\n    void print() const {\n        cout &lt;&lt; \"Владелец: \" &lt;&lt; owner\n             &lt;&lt; \", Сумма: \" &lt;&lt; amount\n             &lt;&lt; \", Ставка: \" &lt;&lt; rate &lt;&lt; \"%\" &lt;&lt; endl;\n    }\n};\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    vector&lt;BankDeposit&gt; deposits = {\n        BankDeposit(\"Иванов И.И.\", 100000, 7.5),\n        BankDeposit(\"Петров П.П.\", 250000, 5.0),\n        BankDeposit(\"Сидоров С.С.\", 50000, 10.0),\n        BankDeposit(\"Козлова А.В.\", 180000, 6.5),\n        BankDeposit(\"Морозов Д.Е.\", 300000, 4.5)\n    };\n\n    cout &lt;&lt; \"Все вклады:\" &lt;&lt; endl;\n    for (const auto&amp; dep : deposits) {\n        dep.print();\n    }\n\n    int years;\n    cout &lt;&lt; \"\\nВведите количество лет: \";\n    cin &gt;&gt; years;\n\n    double totalSum = 0;\n    cout &lt;&lt; \"\\nСуммы через \" &lt;&lt; years &lt;&lt; \" лет:\" &lt;&lt; endl;\n    for (const auto&amp; dep : deposits) {\n        double futureAmount = dep.calcAmount(years);\n        cout &lt;&lt; dep.getOwner() &lt;&lt; \": \" &lt;&lt; futureAmount &lt;&lt; endl;\n        totalSum += futureAmount;\n    }\n    cout &lt;&lt; \"\\nОбщая сумма на всех счетах: \" &lt;&lt; totalSum &lt;&lt; endl;\n\n    return 0;\n}</code></pre>",
  "explanation": "<h2>Разбор кода: Лабораторная работа 1 (Вариант 30)</h2>\n<p>Ниже представлен полный исходный код из <code class=\"inline-code\">main.cpp</code> для удобного ознакомления.</p>\n<p>В последующих разделах этот код разбит на логические блоки с детальным теоретическим объяснением каждого из них.</p>\n<h3>Полный исходный код программы</h3>\n<pre><code>#include &lt;iostream&gt;\n#include &lt;vector&gt;\n#include &lt;string&gt;\n#include &lt;cmath&gt;\n\nusing namespace std;\n\nclass BankDeposit {\nprivate:\n    string owner;\n    double amount;\n    double rate;\n\npublic:\n    BankDeposit(string o, double a, double r) {\n        owner = o;\n        amount = a;\n        rate = r;\n    }\n\n    double calcAmount(int years) const {\n        return amount * pow(1.0 + rate / 100.0, years);\n    }\n\n    string getOwner() const { return owner; }\n    double getAmount() const { return amount; }\n    double getRate() const { return rate; }\n\n    void print() const {\n        cout &lt;&lt; \"Владелец: \" &lt;&lt; owner\n             &lt;&lt; \", Сумма: \" &lt;&lt; amount\n             &lt;&lt; \", Ставка: \" &lt;&lt; rate &lt;&lt; \"%\" &lt;&lt; endl;\n    }\n};\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    vector&lt;BankDeposit&gt; deposits = {\n        BankDeposit(\"Иванов И.И.\", 100000, 7.5),\n        BankDeposit(\"Петров П.П.\", 250000, 5.0),\n        BankDeposit(\"Сидоров С.С.\", 50000, 10.0),\n        BankDeposit(\"Козлова А.В.\", 180000, 6.5),\n        BankDeposit(\"Морозов Д.Е.\", 300000, 4.5)\n    };\n\n    cout &lt;&lt; \"Все вклады:\" &lt;&lt; endl;\n    for (const auto&amp; dep : deposits) {\n        dep.print();\n    }\n\n    int years;\n    cout &lt;&lt; \"\\nВведите количество лет: \";\n    cin &gt;&gt; years;\n\n    double totalSum = 0;\n    cout &lt;&lt; \"\\nСуммы через \" &lt;&lt; years &lt;&lt; \" лет:\" &lt;&lt; endl;\n    for (const auto&amp; dep : deposits) {\n        double futureAmount = dep.calcAmount(years);\n        cout &lt;&lt; dep.getOwner() &lt;&lt; \": \" &lt;&lt; futureAmount &lt;&lt; endl;\n        totalSum += futureAmount;\n    }\n    cout &lt;&lt; \"\\nОбщая сумма на всех счетах: \" &lt;&lt; totalSum &lt;&lt; endl;\n\n    return 0;\n}\n</code></pre>\n<hr>\n<h3>1. Класс <code class=\"inline-code\">BankDeposit</code></h3>\n<pre><code>class BankDeposit {\nprivate:\n    ...</code></pre>\n<ul>\n<li>Ключевое слово <strong><code class=\"inline-code\">class</code></strong> объявляет новый тип данных.</li>\n<li>Блок <strong><code class=\"inline-code\">private:</code></strong> означает, что поля закрыты от прямого доступа извне (из функции <code class=\"inline-code\">main</code>). Это принцип <strong>инкапсуляции</strong>. Мы не можем в <code class=\"inline-code\">main</code> написать: <code class=\"inline-code\">obj.field = 5;</code>.</li>\n</ul>\n<pre><code>public:\n    BankDeposit(...) {\n        ...\n    }</code></pre>\n<ul>\n<li>Блок <strong><code class=\"inline-code\">public:</code></strong> содержит методы, доступные всем.</li>\n<li>Метод <code class=\"inline-code\">BankDeposit(...)</code> без типа возвращаемого значения и с именем, совпадающим с именем класса, называется <strong>конструктором</strong>. Он вызывается автоматически при создании объекта. Его задача — проинициализировать приватные переменные переданными значениями.</li>\n</ul>\n<h4>Методы класса</h4>\n<ul>\n<li><strong>Геттеры</strong> (<code class=\"inline-code\">get...()</code>): так как поля <code class=\"inline-code\">private</code>, для чтения их значений снаружи пишутся специальные функции-читатели.</li>\n<li><strong><code class=\"inline-code\">print()</code></strong>: выводит все параметры объекта в консоль.</li>\n<li>Ключевое слово <strong><code class=\"inline-code\">const</code></strong> в конце метода означает, что он <strong>не изменяет</strong> внутреннее состояние (поля) объекта, а только читает их.</li>\n</ul>\n<h3>2. Главная функция <code class=\"inline-code\">main</code></h3>\n<pre><code>    setlocale(LC_ALL, \"Russian\");</code></pre>\n<ul>\n<li>Устанавливает кодовую страницу для консоли Windows, чтобы кириллица выводилась без \"кракозябр\".</li>\n</ul>\n<pre><code>    vector&lt;BankDeposit&gt; items = { ... };</code></pre>\n<ul>\n<li>Создаётся контейнер <code class=\"inline-code\">vector</code> с объектами класса <code class=\"inline-code\">BankDeposit</code>. В C++ вместо обычных массивов принято использовать <code class=\"inline-code\">vector</code>, так как он автоматически управляет памятью и может менять свой размер.</li>\n</ul>\n<pre><code>    for (const auto&amp; item : items) {\n        item.print();\n    }</code></pre>\n<ul>\n<li>Это <em>Range-based for loop</em> (цикл по диапазону). Он автоматически проходит по всем элементам вектора.</li>\n<li><code class=\"inline-code\">const auto& item</code> означает, что мы берём каждый объект по <strong>константной ссылке</strong>. Это предотвращает лишнее копирование данных в памяти и защищает объект от случайных изменений.</li>\n</ul>\n<h4>Логика задания</h4>\n<p>Вывести параметры всех вкладов и суммарную сумму через заданное число лет (вводится с клавиатуры).</p>\n<p>Для выполнения индивидуального задания мы проходим по контейнеру и используем публичные методы класса для проверки условий и вычисления результатов.</p>",
  "explanationRaw": "# Разбор кода: Лабораторная работа 1 (Вариант 30)\n\nНиже представлен полный исходный код из `main.cpp` для удобного ознакомления.\nВ последующих разделах этот код разбит на логические блоки с детальным теоретическим объяснением каждого из них.\n\n## Полный исходный код программы\n```cpp\n#include <iostream>\n#include <vector>\n#include <string>\n#include <cmath>\n\nusing namespace std;\n\nclass BankDeposit {\nprivate:\n    string owner;\n    double amount;\n    double rate;\n\npublic:\n    BankDeposit(string o, double a, double r) {\n        owner = o;\n        amount = a;\n        rate = r;\n    }\n\n    double calcAmount(int years) const {\n        return amount * pow(1.0 + rate / 100.0, years);\n    }\n\n    string getOwner() const { return owner; }\n    double getAmount() const { return amount; }\n    double getRate() const { return rate; }\n\n    void print() const {\n        cout << \"Владелец: \" << owner\n             << \", Сумма: \" << amount\n             << \", Ставка: \" << rate << \"%\" << endl;\n    }\n};\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    vector<BankDeposit> deposits = {\n        BankDeposit(\"Иванов И.И.\", 100000, 7.5),\n        BankDeposit(\"Петров П.П.\", 250000, 5.0),\n        BankDeposit(\"Сидоров С.С.\", 50000, 10.0),\n        BankDeposit(\"Козлова А.В.\", 180000, 6.5),\n        BankDeposit(\"Морозов Д.Е.\", 300000, 4.5)\n    };\n\n    cout << \"Все вклады:\" << endl;\n    for (const auto& dep : deposits) {\n        dep.print();\n    }\n\n    int years;\n    cout << \"\\nВведите количество лет: \";\n    cin >> years;\n\n    double totalSum = 0;\n    cout << \"\\nСуммы через \" << years << \" лет:\" << endl;\n    for (const auto& dep : deposits) {\n        double futureAmount = dep.calcAmount(years);\n        cout << dep.getOwner() << \": \" << futureAmount << endl;\n        totalSum += futureAmount;\n    }\n    cout << \"\\nОбщая сумма на всех счетах: \" << totalSum << endl;\n\n    return 0;\n}\n\n```\n\n---\n\n## 1. Класс `BankDeposit`\n\n```cpp\nclass BankDeposit {\nprivate:\n    ...\n```\n- Ключевое слово **`class`** объявляет новый тип данных.\n- Блок **`private:`** означает, что поля закрыты от прямого доступа извне (из функции `main`). Это принцип **инкапсуляции**. Мы не можем в `main` написать: `obj.field = 5;`.\n\n```cpp\npublic:\n    BankDeposit(...) {\n        ...\n    }\n```\n- Блок **`public:`** содержит методы, доступные всем.\n- Метод `BankDeposit(...)` без типа возвращаемого значения и с именем, совпадающим с именем класса, называется **конструктором**. Он вызывается автоматически при создании объекта. Его задача — проинициализировать приватные переменные переданными значениями.\n\n### Методы класса\n- **Геттеры** (`get...()`): так как поля `private`, для чтения их значений снаружи пишутся специальные функции-читатели.\n- **`print()`**: выводит все параметры объекта в консоль.\n- Ключевое слово **`const`** в конце метода означает, что он **не изменяет** внутреннее состояние (поля) объекта, а только читает их.\n\n## 2. Главная функция `main`\n\n```cpp\n    setlocale(LC_ALL, \"Russian\");\n```\n- Устанавливает кодовую страницу для консоли Windows, чтобы кириллица выводилась без \"кракозябр\".\n\n```cpp\n    vector<BankDeposit> items = { ... };\n```\n- Создаётся контейнер `vector` с объектами класса `BankDeposit`. В C++ вместо обычных массивов принято использовать `vector`, так как он автоматически управляет памятью и может менять свой размер.\n\n```cpp\n    for (const auto& item : items) {\n        item.print();\n    }\n```\n- Это *Range-based for loop* (цикл по диапазону). Он автоматически проходит по всем элементам вектора.\n- `const auto& item` означает, что мы берём каждый объект по **константной ссылке**. Это предотвращает лишнее копирование данных в памяти и защищает объект от случайных изменений.\n\n### Логика задания\nВывести параметры всех вкладов и суммарную сумму через заданное число лет (вводится с клавиатуры).\nДля выполнения индивидуального задания мы проходим по контейнеру и используем публичные методы класса для проверки условий и вычисления результатов.",
  "condition": "<h2>Условие лабораторной работы №1 (Вариант 30)</h2>\n<h3>Тема: Введение в классы и объекты</h3>\n<p><strong>Цель работы:</strong></p>\n<p>Ознакомиться с базовыми концепциями ООП: инкапсуляция, классы, объекты, конструкторы и методы.</p>\n<h3>Задание по варианту:</h3>\n<ul>\n<li>Формула: <em>30 % 27 = <strong>3</strong></em>.</li>\n<li><strong>Класс:</strong> «Банковский вклад» (<code class=\"inline-code\">BankDeposit</code>).</li>\n<li><strong>Поля (private):</strong> ФИО владельца, текущая сумма, годовая процентная ставка.</li>\n<li><strong>Метод:</strong> расчёт суммы на счёте через заданное число лет (с капитализацией).</li>\n<li><strong>В <code class=\"inline-code\">main</code>:</strong> создать <code class=\"inline-code\">vector</code> объектов, заполнить данными, напечатать параметры.</li>\n<li><strong>Действие:</strong> Вывести параметры всех вкладов и суммарную сумму через заданное число лет (вводится с клавиатуры).</li>\n</ul>",
  "conditionRaw": "# Условие лабораторной работы №1 (Вариант 30)\n\n## Тема: Введение в классы и объекты\n\n**Цель работы:**\nОзнакомиться с базовыми концепциями ООП: инкапсуляция, классы, объекты, конструкторы и методы.\n\n## Задание по варианту:\n- Формула: *30 % 27 = **3***.\n- **Класс:** «Банковский вклад» (`BankDeposit`).\n- **Поля (private):** ФИО владельца, текущая сумма, годовая процентная ставка.\n- **Метод:** расчёт суммы на счёте через заданное число лет (с капитализацией).\n- **В `main`:** создать `vector` объектов, заполнить данными, напечатать параметры.\n- **Действие:** Вывести параметры всех вкладов и суммарную сумму через заданное число лет (вводится с клавиатуры).",
  "theory": "<h2>Теория: Классы и объекты в C++</h2>\n<h3>1. Что такое класс</h3>\n<p>Класс — это пользовательский тип данных, объединяющий данные (<strong>поля</strong>) и функции для их обработки (<strong>методы</strong>). Переменные типа класса называются <strong>объектами</strong>.</p>\n<pre><code>class MyClass {\nprivate:\n    int field1;     // Поле класса (данные)\npublic:\n    void method();  // Метод класса (функция)\n};</code></pre>\n<h3>2. Инкапсуляция и модификаторы доступа</h3>\n<p>Инкапсуляция — принцип ООП, который скрывает внутреннее устройство объекта от внешнего мира. В C++ есть три уровня доступа:</p>\n<ul>\n<li><strong><code class=\"inline-code\">private</code></strong> — доступ только из методов самого класса (по умолчанию для <code class=\"inline-code\">class</code>).</li>\n<li><strong><code class=\"inline-code\">public</code></strong> — доступ отовсюду (из <code class=\"inline-code\">main</code>, из других классов).</li>\n<li><strong><code class=\"inline-code\">protected</code></strong> — доступ из класса и его наследников.</li>\n</ul>\n<p>Поля обычно делают <code class=\"inline-code\">private</code>, а методы — <code class=\"inline-code\">public</code>. Для чтения приватных полей пишут <strong>геттеры</strong> (<code class=\"inline-code\">getField()</code>).</p>\n<h3>3. Конструктор</h3>\n<p>Конструктор — специальный метод, имя которого совпадает с именем класса. Он не имеет типа возвращаемого значения и вызывается автоматически при создании объекта.</p>\n<pre><code>class Apartment {\npublic:\n    Apartment(double area, double price) {\n        totalArea = area;\n        pricePerSqm = price;\n    }\n};</code></pre>\n<p>Конструктор можно перегружать (иметь несколько версий с разным числом параметров).</p>\n<h3>4. Контейнер <code class=\"inline-code\">vector</code></h3>\n<p><code class=\"inline-code\">vector</code> — динамический массив из стандартной библиотеки C++. В отличие от обычного массива, <code class=\"inline-code\">vector</code> автоматически управляет памятью и может менять свой размер.</p>\n<pre><code>vector&lt;Apartment&gt; apartments;\napartments.push_back(Apartment(50.0, 1000.0));</code></pre>\n<h3>5. Цикл <code class=\"inline-code\">for</code> по диапазону (Range-based for)</h3>\n<pre><code>for (const auto&amp; apt : apartments) {\n    apt.print();\n}</code></pre>\n<p>Автоматически проходит по всем элементам контейнера. <code class=\"inline-code\">const auto&</code> берёт каждый элемент по константной ссылке (без копирования).</p>\n<h3>6. Ключевое слово <code class=\"inline-code\">const</code> у методов</h3>\n<pre><code>void print() const { ... }</code></pre>\n<p>Означает, что метод не изменяет поля объекта. Такие методы можно вызывать у <code class=\"inline-code\">const</code>-объектов.</p>",
  "theoryRaw": "# Теория: Классы и объекты в C++\n\n## 1. Что такое класс\nКласс — это пользовательский тип данных, объединяющий данные (**поля**) и функции для их обработки (**методы**). Переменные типа класса называются **объектами**.\n\n```cpp\nclass MyClass {\nprivate:\n    int field1;     // Поле класса (данные)\npublic:\n    void method();  // Метод класса (функция)\n};\n```\n\n## 2. Инкапсуляция и модификаторы доступа\nИнкапсуляция — принцип ООП, который скрывает внутреннее устройство объекта от внешнего мира. В C++ есть три уровня доступа:\n- **`private`** — доступ только из методов самого класса (по умолчанию для `class`).\n- **`public`** — доступ отовсюду (из `main`, из других классов).\n- **`protected`** — доступ из класса и его наследников.\n\nПоля обычно делают `private`, а методы — `public`. Для чтения приватных полей пишут **геттеры** (`getField()`).\n\n## 3. Конструктор\nКонструктор — специальный метод, имя которого совпадает с именем класса. Он не имеет типа возвращаемого значения и вызывается автоматически при создании объекта.\n```cpp\nclass Apartment {\npublic:\n    Apartment(double area, double price) {\n        totalArea = area;\n        pricePerSqm = price;\n    }\n};\n```\nКонструктор можно перегружать (иметь несколько версий с разным числом параметров).\n\n## 4. Контейнер `vector`\n`vector` — динамический массив из стандартной библиотеки C++. В отличие от обычного массива, `vector` автоматически управляет памятью и может менять свой размер.\n```cpp\nvector<Apartment> apartments;\napartments.push_back(Apartment(50.0, 1000.0));\n```\n\n## 5. Цикл `for` по диапазону (Range-based for)\n```cpp\nfor (const auto& apt : apartments) {\n    apt.print();\n}\n```\nАвтоматически проходит по всем элементам контейнера. `const auto&` берёт каждый элемент по константной ссылке (без копирования).\n\n## 6. Ключевое слово `const` у методов\n```cpp\nvoid print() const { ... }\n```\nОзначает, что метод не изменяет поля объекта. Такие методы можно вызывать у `const`-объектов."
}