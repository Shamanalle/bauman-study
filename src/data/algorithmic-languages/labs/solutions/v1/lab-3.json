{
  "variant": 1,
  "lab": 3,
  "code": "#include <iostream>\n#include <cmath>\n\nusing namespace std;\n\nclass Vector2D {\nprotected:\n    double x, y;\n\npublic:\n    Vector2D(double x, double y) : x(x), y(y) {}\n\n    virtual double length() const {\n        return sqrt(x * x + y * y);\n    }\n\n    virtual void print() const {\n        cout << \"Вектор на плоскости (2D):\" << endl;\n        cout << \"  Координаты: (\" << x << \", \" << y << \")\" << endl;\n        cout << \"  Длина: \" << length() << endl;\n    }\n\n    virtual ~Vector2D() {}\n};\n\nclass Vector3D : public Vector2D {\nprivate:\n    double z;\n\npublic:\n    Vector3D(double x, double y, double z) : Vector2D(x, y), z(z) {}\n\n    double length() const override {\n        return sqrt(x * x + y * y + z * z);\n    }\n\n    void print() const override {\n        cout << \"Вектор в пространстве (3D):\" << endl;\n        cout << \"  Координаты: (\" << x << \", \" << y << \", \" << z << \")\" << endl;\n        cout << \"  Длина 2D-проекции: \" << Vector2D::length() << endl;\n        cout << \"  Длина 3D: \" << length() << endl;\n    }\n};\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    cout << \"=== Создание объектов ===\" << endl;\n    Vector2D v2(3.0, 4.0);\n    Vector3D v3(1.0, 2.0, 2.0);\n\n    cout << \"\\n=== Прямой вызов функций (объекты) ===\" << endl;\n    v2.print();\n    cout << endl;\n    v3.print();\n\n    cout << \"\\n=== 1. СТАТИЧЕСКИЙ ПОЛИМОРФИЗМ (Статическое связывание) ===\" << endl;\n\n    Vector2D* ptr_v1 = &v2;\n    Vector2D* ptr_v2 = &v3;\n\n    cout << \"\\n[Явный вызов базового метода] ptr_v2->Vector2D::print():\" << endl;\n    ptr_v2->Vector2D::print();\n\n    cout << \"\\n=== 2. ДИНАМИЧЕСКИЙ ПОЛИМОРФИЗМ (Динамическое связывание) ===\" << endl;\n\n    cout << \"\\nВызов ptr_v1->print() (указывает на Vector2D):\" << endl;\n    ptr_v1->print();\n\n    cout << \"\\nВызов ptr_v2->print() (указывает на Vector3D):\" << endl;\n    ptr_v2->print();\n\n    cout << \"\\nВызов ptr_v2->length() (указывает на Vector3D):\" << endl;\n    cout << \"  Длина = \" << ptr_v2->length() << endl;\n\n    return 0;\n}",
  "codeHtml": "<pre><code>#include &lt;iostream&gt;\n#include &lt;cmath&gt;\n\nusing namespace std;\n\nclass Vector2D {\nprotected:\n    double x, y;\n\npublic:\n    Vector2D(double x, double y) : x(x), y(y) {}\n\n    virtual double length() const {\n        return sqrt(x * x + y * y);\n    }\n\n    virtual void print() const {\n        cout &lt;&lt; \"Вектор на плоскости (2D):\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Координаты: (\" &lt;&lt; x &lt;&lt; \", \" &lt;&lt; y &lt;&lt; \")\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Длина: \" &lt;&lt; length() &lt;&lt; endl;\n    }\n\n    virtual ~Vector2D() {}\n};\n\nclass Vector3D : public Vector2D {\nprivate:\n    double z;\n\npublic:\n    Vector3D(double x, double y, double z) : Vector2D(x, y), z(z) {}\n\n    double length() const override {\n        return sqrt(x * x + y * y + z * z);\n    }\n\n    void print() const override {\n        cout &lt;&lt; \"Вектор в пространстве (3D):\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Координаты: (\" &lt;&lt; x &lt;&lt; \", \" &lt;&lt; y &lt;&lt; \", \" &lt;&lt; z &lt;&lt; \")\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Длина 2D-проекции: \" &lt;&lt; Vector2D::length() &lt;&lt; endl;\n        cout &lt;&lt; \"  Длина 3D: \" &lt;&lt; length() &lt;&lt; endl;\n    }\n};\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    cout &lt;&lt; \"=== Создание объектов ===\" &lt;&lt; endl;\n    Vector2D v2(3.0, 4.0);\n    Vector3D v3(1.0, 2.0, 2.0);\n\n    cout &lt;&lt; \"\\n=== Прямой вызов функций (объекты) ===\" &lt;&lt; endl;\n    v2.print();\n    cout &lt;&lt; endl;\n    v3.print();\n\n    cout &lt;&lt; \"\\n=== 1. СТАТИЧЕСКИЙ ПОЛИМОРФИЗМ (Статическое связывание) ===\" &lt;&lt; endl;\n\n    Vector2D* ptr_v1 = &amp;v2;\n    Vector2D* ptr_v2 = &amp;v3;\n\n    cout &lt;&lt; \"\\n[Явный вызов базового метода] ptr_v2-&gt;Vector2D::print():\" &lt;&lt; endl;\n    ptr_v2-&gt;Vector2D::print();\n\n    cout &lt;&lt; \"\\n=== 2. ДИНАМИЧЕСКИЙ ПОЛИМОРФИЗМ (Динамическое связывание) ===\" &lt;&lt; endl;\n\n    cout &lt;&lt; \"\\nВызов ptr_v1-&gt;print() (указывает на Vector2D):\" &lt;&lt; endl;\n    ptr_v1-&gt;print();\n\n    cout &lt;&lt; \"\\nВызов ptr_v2-&gt;print() (указывает на Vector3D):\" &lt;&lt; endl;\n    ptr_v2-&gt;print();\n\n    cout &lt;&lt; \"\\nВызов ptr_v2-&gt;length() (указывает на Vector3D):\" &lt;&lt; endl;\n    cout &lt;&lt; \"  Длина = \" &lt;&lt; ptr_v2-&gt;length() &lt;&lt; endl;\n\n    return 0;\n}</code></pre>",
  "explanation": "<h2>Разбор кода: Лабораторная работа 3 (Вариант 1)</h2>\n<p>Ниже представлен полный исходный код из <code class=\"inline-code\">main.cpp</code> для удобного ознакомления. </p>\n<p>В последующих разделах этот код разбит на логические блоки с детальным объяснением конструкций наследования и полиморфизма.</p>\n<h3>Полный исходный код программы</h3>\n<pre><code>#include &lt;iostream&gt;\n#include &lt;cmath&gt;\n\nusing namespace std;\n\nclass Vector2D {\nprotected:\n    double x, y;\n\npublic:\n    Vector2D(double x, double y) : x(x), y(y) {}\n\n    virtual double length() const {\n        return sqrt(x * x + y * y);\n    }\n\n    virtual void print() const {\n        cout &lt;&lt; \"Вектор на плоскости (2D):\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Координаты: (\" &lt;&lt; x &lt;&lt; \", \" &lt;&lt; y &lt;&lt; \")\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Длина: \" &lt;&lt; length() &lt;&lt; endl;\n    }\n\n    virtual ~Vector2D() {}\n};\n\nclass Vector3D : public Vector2D {\nprivate:\n    double z;\n\npublic:\n    Vector3D(double x, double y, double z) : Vector2D(x, y), z(z) {}\n\n    double length() const override {\n        return sqrt(x * x + y * y + z * z);\n    }\n\n    void print() const override {\n        cout &lt;&lt; \"Вектор в пространстве (3D):\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Координаты: (\" &lt;&lt; x &lt;&lt; \", \" &lt;&lt; y &lt;&lt; \", \" &lt;&lt; z &lt;&lt; \")\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Длина 2D-проекции: \" &lt;&lt; Vector2D::length() &lt;&lt; endl;\n        cout &lt;&lt; \"  Длина 3D: \" &lt;&lt; length() &lt;&lt; endl;\n    }\n};\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    cout &lt;&lt; \"=== Создание объектов ===\" &lt;&lt; endl;\n    Vector2D v2(3.0, 4.0);\n    Vector3D v3(1.0, 2.0, 2.0);\n\n    cout &lt;&lt; \"\\n=== Прямой вызов функций (объекты) ===\" &lt;&lt; endl;\n    v2.print();\n    cout &lt;&lt; endl;\n    v3.print();\n\n    cout &lt;&lt; \"\\n=== 1. СТАТИЧЕСКИЙ ПОЛИМОРФИЗМ (Статическое связывание) ===\" &lt;&lt; endl;\n\n    Vector2D* ptr_v1 = &amp;v2;\n    Vector2D* ptr_v2 = &amp;v3;\n\n    cout &lt;&lt; \"\\n[Явный вызов базового метода] ptr_v2-&gt;Vector2D::print():\" &lt;&lt; endl;\n    ptr_v2-&gt;Vector2D::print();\n\n    cout &lt;&lt; \"\\n=== 2. ДИНАМИЧЕСКИЙ ПОЛИМОРФИЗМ (Динамическое связывание) ===\" &lt;&lt; endl;\n\n    cout &lt;&lt; \"\\nВызов ptr_v1-&gt;print() (указывает на Vector2D):\" &lt;&lt; endl;\n    ptr_v1-&gt;print();\n\n    cout &lt;&lt; \"\\nВызов ptr_v2-&gt;print() (указывает на Vector3D):\" &lt;&lt; endl;\n    ptr_v2-&gt;print();\n\n    cout &lt;&lt; \"\\nВызов ptr_v2-&gt;length() (указывает на Vector3D):\" &lt;&lt; endl;\n    cout &lt;&lt; \"  Длина = \" &lt;&lt; ptr_v2-&gt;length() &lt;&lt; endl;\n\n    return 0;\n}\n</code></pre>\n<hr>\n<h3>1. Базовый Класс <code class=\"inline-code\">Vector2D</code></h3>\n<pre><code>class Vector2D {{\nprotected:\n    double x, y;</code></pre>\n<ul>\n<li>Ключевое слово <strong><code class=\"inline-code\">class</code></strong> объявляет базовый класс для вектора на плоскости.</li>\n<li>Блок <strong><code class=\"inline-code\">protected:</code></strong> означает, что поля <code class=\"inline-code\">x</code> и <code class=\"inline-code\">y</code> (координаты конца вектора) <strong>закрыты</strong> от прямого доступа из внешнего кода (из функции <code class=\"inline-code\">main</code> написать <code class=\"inline-code\">v.x = 5</code> нельзя), но при этом <strong>доступны</strong> классам-наследникам. Это принципиальное отличие от <code class=\"inline-code\">private</code>, который закрывает доступ вообще для всех, включая наследников.</li>\n</ul>\n<pre><code>public:\n    Vector2D(double x, double y) : x(x), y(y) {{}}</code></pre>\n<ul>\n<li><strong>Конструктор</strong> со <strong>списком инициализации</strong> (<code class=\"inline-code\">: x(x), y(y)</code>). Такая запись эквивалентна присвоению <code class=\"inline-code\">this->x = x; this->y = y;</code> внутри тела конструктора, но является более эффективной и считается стандартной практикой в C++.</li>\n</ul>\n<pre><code>    virtual double length() const {{\n        return sqrt(x * x + y * y);\n    }}</code></pre>\n<ul>\n<li>Метод вычисляет длину вектора на плоскости по формуле √(x² + y²) с использованием функции <code class=\"inline-code\">sqrt()</code> из заголовочного файла <code class=\"inline-code\"><cmath></code>.</li>\n<li>Ключевое слово <strong><code class=\"inline-code\">virtual</code></strong> — это самое главное слово в теме полиморфизма. Оно указывает компилятору, что если кто-то позже унаследует этот класс, он <strong>может переопределить</strong> (<em>override</em>) эту функцию. Без <code class=\"inline-code\">virtual</code> переопределение в наследнике не будет работать через указатели базового типа.</li>\n<li>Ключевое слово <strong><code class=\"inline-code\">const</code></strong> после скобок параметров означает, что метод не изменяет внутреннее состояние (поля) объекта, а только читает их.</li>\n</ul>\n<pre><code>    virtual void print() const {{\n        cout &lt;&lt; \"Вектор на плоскости (2D):\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Координаты: (\" &lt;&lt; x &lt;&lt; \", \" &lt;&lt; y &lt;&lt; \")\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Длина: \" &lt;&lt; length() &lt;&lt; endl;\n    }}</code></pre>\n<ul>\n<li>Виртуальный метод для печати параметров вектора. Обратите внимание, что внутри <code class=\"inline-code\">print()</code> вызывается <code class=\"inline-code\">length()</code> — и поскольку <code class=\"inline-code\">length()</code> тоже виртуальный, при вызове через указатель будет вызвана правильная версия (2D или 3D) в зависимости от реального типа объекта.</li>\n</ul>\n<pre><code>    virtual ~Vector2D() {{}}</code></pre>\n<ul>\n<li><strong>Виртуальный деструктор</strong>. Обязателен, если класс используется как базовый. Без него при удалении объекта наследника через указатель базового типа (<code class=\"inline-code\">delete ptr</code>) деструктор наследника не будет вызван, что приведет к утечке памяти.</li>\n</ul>\n<hr>\n<h3>2. Производный Класс <code class=\"inline-code\">Vector3D</code></h3>\n<pre><code>class Vector3D : public Vector2D {{</code></pre>\n<ul>\n<li>Запись <strong><code class=\"inline-code\">: public Vector2D</code></strong> означает публичное наследование. Класс <code class=\"inline-code\">Vector3D</code> получает все <code class=\"inline-code\">public</code> и <code class=\"inline-code\">protected</code> члены из <code class=\"inline-code\">Vector2D</code>. То есть поля <code class=\"inline-code\">x</code> и <code class=\"inline-code\">y</code> доступны напрямую внутри <code class=\"inline-code\">Vector3D</code>, но остаются недоступны из внешнего кода.</li>\n</ul>\n<pre><code>private:\n    double z;</code></pre>\n<ul>\n<li>Дополнительное поле <code class=\"inline-code\">z</code> — третья координата. Объявлено как <code class=\"inline-code\">private</code>, т.к. никто больше от <code class=\"inline-code\">Vector3D</code> наследоваться не будет.</li>\n</ul>\n<pre><code>    Vector3D(double x, double y, double z) : Vector2D(x, y), z(z) {{}}</code></pre>\n<ul>\n<li>Конструктор наследника <strong>обязан</strong> инициализировать родительскую часть. Поэтому мы <strong>явно вызываем конструктор базового класса</strong> <code class=\"inline-code\">Vector2D(x, y)</code> в списке инициализации. Затем инициализируем собственное поле <code class=\"inline-code\">z(z)</code>.</li>\n<li>Без явного вызова <code class=\"inline-code\">Vector2D(x, y)</code> компилятор попытается вызвать конструктор без параметров <code class=\"inline-code\">Vector2D()</code>, которого не существует, и выдаст ошибку компиляции.</li>\n</ul>\n<pre><code>    double length() const override {{\n        return sqrt(x * x + y * y + z * z);\n    }}</code></pre>\n<ul>\n<li><strong>Переопределённый</strong> метод <code class=\"inline-code\">length()</code>. Формула длины вектора в 3D: √(x² + y² + z²).</li>\n<li>Ключевое слово <strong><code class=\"inline-code\">override</code></strong> (появилось в C++11) говорит компилятору: «Я хочу переопределить виртуальную функцию родителя. Проверь, чтобы имя и параметры точно совпадали». Если мы опечатаемся в имени — компилятор выдаст ошибку, а не молча создаст новую функцию.</li>\n</ul>\n<pre><code>    void print() const override {{\n        cout &lt;&lt; \"Вектор в пространстве (3D):\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Координаты: (\" &lt;&lt; x &lt;&lt; \", \" &lt;&lt; y &lt;&lt; \", \" &lt;&lt; z &lt;&lt; \")\" &lt;&lt; endl;\n        cout &lt;&lt; \"  Длина 2D-проекции: \" &lt;&lt; Vector2D::length() &lt;&lt; endl;\n        cout &lt;&lt; \"  Длина 3D: \" &lt;&lt; length() &lt;&lt; endl;\n    }}</code></pre>\n<ul>\n<li>Переопределённая функция печати. Обратите внимание на <strong>вызов базового метода</strong> <code class=\"inline-code\">Vector2D::length()</code> — это позволяет показать длину 2D-проекции вектора для сравнения с полной 3D-длиной.</li>\n<li>Доступ к полям <code class=\"inline-code\">x</code> и <code class=\"inline-code\">y</code> возможен напрямую, потому что они объявлены как <code class=\"inline-code\">protected</code> в базовом классе.</li>\n</ul>\n<hr>\n<h3>3. Полиморфизм в <code class=\"inline-code\">main</code></h3>\n<pre><code>    Vector2D v2(3.0, 4.0);\n    Vector3D v3(1.0, 2.0, 2.0);</code></pre>\n<ul>\n<li>Создаются два объекта: вектор на плоскости (3, 4) с длиной 5, и вектор в пространстве (1, 2, 2) с длиной 3.</li>\n</ul>\n<pre><code>    Vector2D* ptr_v1 = &amp;v2;\n    Vector2D* ptr_v2 = &amp;v3;</code></pre>\n<ul>\n<li>Оба указателя имеют тип <code class=\"inline-code\">Vector2D*</code> (базовый класс). Но <code class=\"inline-code\">ptr_v2</code> указывает на объект типа <code class=\"inline-code\">Vector3D</code> (наследник). <strong>Указатель базового типа может безопасно указывать на объект любого унаследованного типа</strong> — это базовая фишка C++ и основа полиморфизма.</li>\n</ul>\n<h4>Статическое связывание</h4>\n<pre><code>    ptr_v2-&gt;Vector2D::print();</code></pre>\n<ul>\n<li>Мы <strong>принудительно вызываем метод базового класса</strong> в обход динамической диспетчеризации. Запись <code class=\"inline-code\">Vector2D::print()</code> — это <strong>явная квалификация</strong> имени метода. Несмотря на то, что <code class=\"inline-code\">ptr_v2</code> указывает на <code class=\"inline-code\">Vector3D</code>, будет вызвана именно функция <code class=\"inline-code\">Vector2D::print()</code>. Это статическое связывание (решение принимается на этапе компиляции).</li>\n</ul>\n<h4>Динамическое связывание</h4>\n<pre><code>    ptr_v2-&gt;print();</code></pre>\n<ul>\n<li>Поскольку <code class=\"inline-code\">print()</code> помечен словом <code class=\"inline-code\">virtual</code>, компилятор <strong>не привязывает</strong> вызов жестко. Во время работы программы она идет по указателю <code class=\"inline-code\">ptr_v2</code>, заглядывает внутрь объекта <code class=\"inline-code\">v3</code>, находит там «Таблицу виртуальных функций» и видит: реальный тип объекта — это <code class=\"inline-code\">Vector3D</code>! Значит, нужно вызвать функцию <code class=\"inline-code\">Vector3D::print()</code>. Это и есть динамический полиморфизм.</li>\n</ul>\n<pre><code>    ptr_v2-&gt;length();</code></pre>\n<ul>\n<li>Аналогично, <code class=\"inline-code\">length()</code> вызовет <code class=\"inline-code\">Vector3D::length()</code>, возвращая 3D-длину вектора, а не 2D-проекцию.</li>\n</ul>",
  "explanationRaw": "# Разбор кода: Лабораторная работа 3 (Вариант 1)\n\nНиже представлен полный исходный код из `main.cpp` для удобного ознакомления. \nВ последующих разделах этот код разбит на логические блоки с детальным объяснением конструкций наследования и полиморфизма.\n\n## Полный исходный код программы\n```cpp\n#include <iostream>\n#include <cmath>\n\nusing namespace std;\n\nclass Vector2D {\nprotected:\n    double x, y;\n\npublic:\n    Vector2D(double x, double y) : x(x), y(y) {}\n\n    virtual double length() const {\n        return sqrt(x * x + y * y);\n    }\n\n    virtual void print() const {\n        cout << \"Вектор на плоскости (2D):\" << endl;\n        cout << \"  Координаты: (\" << x << \", \" << y << \")\" << endl;\n        cout << \"  Длина: \" << length() << endl;\n    }\n\n    virtual ~Vector2D() {}\n};\n\nclass Vector3D : public Vector2D {\nprivate:\n    double z;\n\npublic:\n    Vector3D(double x, double y, double z) : Vector2D(x, y), z(z) {}\n\n    double length() const override {\n        return sqrt(x * x + y * y + z * z);\n    }\n\n    void print() const override {\n        cout << \"Вектор в пространстве (3D):\" << endl;\n        cout << \"  Координаты: (\" << x << \", \" << y << \", \" << z << \")\" << endl;\n        cout << \"  Длина 2D-проекции: \" << Vector2D::length() << endl;\n        cout << \"  Длина 3D: \" << length() << endl;\n    }\n};\n\nint main() {\n    setlocale(LC_ALL, \"Russian\");\n\n    cout << \"=== Создание объектов ===\" << endl;\n    Vector2D v2(3.0, 4.0);\n    Vector3D v3(1.0, 2.0, 2.0);\n\n    cout << \"\\n=== Прямой вызов функций (объекты) ===\" << endl;\n    v2.print();\n    cout << endl;\n    v3.print();\n\n    cout << \"\\n=== 1. СТАТИЧЕСКИЙ ПОЛИМОРФИЗМ (Статическое связывание) ===\" << endl;\n\n    Vector2D* ptr_v1 = &v2;\n    Vector2D* ptr_v2 = &v3;\n\n    cout << \"\\n[Явный вызов базового метода] ptr_v2->Vector2D::print():\" << endl;\n    ptr_v2->Vector2D::print();\n\n    cout << \"\\n=== 2. ДИНАМИЧЕСКИЙ ПОЛИМОРФИЗМ (Динамическое связывание) ===\" << endl;\n\n    cout << \"\\nВызов ptr_v1->print() (указывает на Vector2D):\" << endl;\n    ptr_v1->print();\n\n    cout << \"\\nВызов ptr_v2->print() (указывает на Vector3D):\" << endl;\n    ptr_v2->print();\n\n    cout << \"\\nВызов ptr_v2->length() (указывает на Vector3D):\" << endl;\n    cout << \"  Длина = \" << ptr_v2->length() << endl;\n\n    return 0;\n}\n\n```\n\n---\n\n## 1. Базовый Класс `Vector2D`\n\n```cpp\nclass Vector2D {{\nprotected:\n    double x, y;\n```\n- Ключевое слово **`class`** объявляет базовый класс для вектора на плоскости.\n- Блок **`protected:`** означает, что поля `x` и `y` (координаты конца вектора) **закрыты** от прямого доступа из внешнего кода (из функции `main` написать `v.x = 5` нельзя), но при этом **доступны** классам-наследникам. Это принципиальное отличие от `private`, который закрывает доступ вообще для всех, включая наследников.\n\n```cpp\npublic:\n    Vector2D(double x, double y) : x(x), y(y) {{}}\n```\n- **Конструктор** со **списком инициализации** (`: x(x), y(y)`). Такая запись эквивалентна присвоению `this->x = x; this->y = y;` внутри тела конструктора, но является более эффективной и считается стандартной практикой в C++.\n\n```cpp\n    virtual double length() const {{\n        return sqrt(x * x + y * y);\n    }}\n```\n- Метод вычисляет длину вектора на плоскости по формуле √(x² + y²) с использованием функции `sqrt()` из заголовочного файла `<cmath>`.\n- Ключевое слово **`virtual`** — это самое главное слово в теме полиморфизма. Оно указывает компилятору, что если кто-то позже унаследует этот класс, он **может переопределить** (*override*) эту функцию. Без `virtual` переопределение в наследнике не будет работать через указатели базового типа.\n- Ключевое слово **`const`** после скобок параметров означает, что метод не изменяет внутреннее состояние (поля) объекта, а только читает их.\n\n```cpp\n    virtual void print() const {{\n        cout << \"Вектор на плоскости (2D):\" << endl;\n        cout << \"  Координаты: (\" << x << \", \" << y << \")\" << endl;\n        cout << \"  Длина: \" << length() << endl;\n    }}\n```\n- Виртуальный метод для печати параметров вектора. Обратите внимание, что внутри `print()` вызывается `length()` — и поскольку `length()` тоже виртуальный, при вызове через указатель будет вызвана правильная версия (2D или 3D) в зависимости от реального типа объекта.\n\n```cpp\n    virtual ~Vector2D() {{}}\n```\n- **Виртуальный деструктор**. Обязателен, если класс используется как базовый. Без него при удалении объекта наследника через указатель базового типа (`delete ptr`) деструктор наследника не будет вызван, что приведет к утечке памяти.\n\n---\n\n## 2. Производный Класс `Vector3D`\n\n```cpp\nclass Vector3D : public Vector2D {{\n```\n- Запись **`: public Vector2D`** означает публичное наследование. Класс `Vector3D` получает все `public` и `protected` члены из `Vector2D`. То есть поля `x` и `y` доступны напрямую внутри `Vector3D`, но остаются недоступны из внешнего кода.\n\n```cpp\nprivate:\n    double z;\n```\n- Дополнительное поле `z` — третья координата. Объявлено как `private`, т.к. никто больше от `Vector3D` наследоваться не будет.\n\n```cpp\n    Vector3D(double x, double y, double z) : Vector2D(x, y), z(z) {{}}\n```\n- Конструктор наследника **обязан** инициализировать родительскую часть. Поэтому мы **явно вызываем конструктор базового класса** `Vector2D(x, y)` в списке инициализации. Затем инициализируем собственное поле `z(z)`.\n- Без явного вызова `Vector2D(x, y)` компилятор попытается вызвать конструктор без параметров `Vector2D()`, которого не существует, и выдаст ошибку компиляции.\n\n```cpp\n    double length() const override {{\n        return sqrt(x * x + y * y + z * z);\n    }}\n```\n- **Переопределённый** метод `length()`. Формула длины вектора в 3D: √(x² + y² + z²).\n- Ключевое слово **`override`** (появилось в C++11) говорит компилятору: «Я хочу переопределить виртуальную функцию родителя. Проверь, чтобы имя и параметры точно совпадали». Если мы опечатаемся в имени — компилятор выдаст ошибку, а не молча создаст новую функцию.\n\n```cpp\n    void print() const override {{\n        cout << \"Вектор в пространстве (3D):\" << endl;\n        cout << \"  Координаты: (\" << x << \", \" << y << \", \" << z << \")\" << endl;\n        cout << \"  Длина 2D-проекции: \" << Vector2D::length() << endl;\n        cout << \"  Длина 3D: \" << length() << endl;\n    }}\n```\n- Переопределённая функция печати. Обратите внимание на **вызов базового метода** `Vector2D::length()` — это позволяет показать длину 2D-проекции вектора для сравнения с полной 3D-длиной.\n- Доступ к полям `x` и `y` возможен напрямую, потому что они объявлены как `protected` в базовом классе.\n\n---\n\n## 3. Полиморфизм в `main`\n\n```cpp\n    Vector2D v2(3.0, 4.0);\n    Vector3D v3(1.0, 2.0, 2.0);\n```\n- Создаются два объекта: вектор на плоскости (3, 4) с длиной 5, и вектор в пространстве (1, 2, 2) с длиной 3.\n\n```cpp\n    Vector2D* ptr_v1 = &v2;\n    Vector2D* ptr_v2 = &v3;\n```\n- Оба указателя имеют тип `Vector2D*` (базовый класс). Но `ptr_v2` указывает на объект типа `Vector3D` (наследник). **Указатель базового типа может безопасно указывать на объект любого унаследованного типа** — это базовая фишка C++ и основа полиморфизма.\n\n### Статическое связывание\n```cpp\n    ptr_v2->Vector2D::print();\n```\n- Мы **принудительно вызываем метод базового класса** в обход динамической диспетчеризации. Запись `Vector2D::print()` — это **явная квалификация** имени метода. Несмотря на то, что `ptr_v2` указывает на `Vector3D`, будет вызвана именно функция `Vector2D::print()`. Это статическое связывание (решение принимается на этапе компиляции).\n\n### Динамическое связывание\n```cpp\n    ptr_v2->print();\n```\n- Поскольку `print()` помечен словом `virtual`, компилятор **не привязывает** вызов жестко. Во время работы программы она идет по указателю `ptr_v2`, заглядывает внутрь объекта `v3`, находит там «Таблицу виртуальных функций» и видит: реальный тип объекта — это `Vector3D`! Значит, нужно вызвать функцию `Vector3D::print()`. Это и есть динамический полиморфизм.\n\n```cpp\n    ptr_v2->length();\n```\n- Аналогично, `length()` вызовет `Vector3D::length()`, возвращая 3D-длину вектора, а не 2D-проекцию.",
  "condition": "<h2>Условие лабораторной работы №3 (Вариант 1)</h2>\n<h3>Тема: Наследование классов и Полиморфизм</h3>\n<p><strong>Цель работы:</strong></p>\n<p>Изучить отношения между базовыми и производными классами в C++, а также понять механизмы реализации раннего (статического) и позднего (динамического) связывания вызовов функций.</p>\n<h3>Общее задание:</h3>\n<p>1. Создать иерархию из двух классов (базового родителя и класса-наследника).</p>\n<p>2. Класс-наследник должен использовать конструктор базового класса (передавая ему параметры через список инициализации конструктора).</p>\n<p>3. В базовом классе определить виртуальные (virtual) функции. Переопределить эти функции (override) в наследнике.</p>\n<p>4. Добавить в функции-члены вывод информации о том, для какого класса (базового или производного) произошел их вызов, и вывести значения всех полей объекта. Причем внутри переопределенной функции в наследнике в первую очередь должна вызываться функция из базового класса.</p>\n<p>5. Инкапсулировать поля базового класса таким образом, чтобы защитить их от внешнего доступа (из метода main), но разрешить прямой доступ из производного класса-наследника (использовать модификатор <code class=\"inline-code\">protected</code>).</p>\n<p>6. В функции <code class=\"inline-code\">main</code> продемонстрировать разницу между <strong>статическим</strong> (ранним) и <strong>динамическим</strong> (поздним) связыванием:</p>\n<ul>\n<li>Создать указатели базового типа <code class=\"inline-code\">Base*</code>.</li>\n<li>Присвоить им адреса объектов производного типа (<code class=\"inline-code\">Base* b = new Derived()</code>).</li>\n<li>Осуществить вызовы виртуальных и невиртуальных методов и объяснить разницу.</li>\n</ul>\n<h3>Индивидуальное задание:</h3>\n<ul>\n<li><strong>Базовый класс:</strong> «Вектор на плоскости» (<code class=\"inline-code\">Vector2D</code>)</li>\n<li><em>Поля (protected):</em> координаты конца вектора x, y (начало в точке 0, 0).</li>\n<li><em>Виртуальные методы:</em></li>\n</ul>\n<p>    1. Вычисление длины вектора (√(x² + y²)).</p>\n<p>    2. Печать полей и длины вектора.</p>\n<ul>\n<li><strong>Производный класс:</strong> «Вектор в трехмерном пространстве» (<code class=\"inline-code\">Vector3D</code>)</li>\n<li><em>Дополнительное поле:</em> координата z.</li>\n<li><em>Переопределение методов:</em></li>\n</ul>\n<p>    1. Вычисление длины вектора (√(x² + y² + z²)).</p>\n<p>    2. Печать полей и длины вектора.</p>",
  "conditionRaw": "# Условие лабораторной работы №3 (Вариант 1)\n\n## Тема: Наследование классов и Полиморфизм\n\n**Цель работы:**\nИзучить отношения между базовыми и производными классами в C++, а также понять механизмы реализации раннего (статического) и позднего (динамического) связывания вызовов функций.\n\n## Общее задание:\n1. Создать иерархию из двух классов (базового родителя и класса-наследника).\n2. Класс-наследник должен использовать конструктор базового класса (передавая ему параметры через список инициализации конструктора).\n3. В базовом классе определить виртуальные (virtual) функции. Переопределить эти функции (override) в наследнике.\n4. Добавить в функции-члены вывод информации о том, для какого класса (базового или производного) произошел их вызов, и вывести значения всех полей объекта. Причем внутри переопределенной функции в наследнике в первую очередь должна вызываться функция из базового класса.\n5. Инкапсулировать поля базового класса таким образом, чтобы защитить их от внешнего доступа (из метода main), но разрешить прямой доступ из производного класса-наследника (использовать модификатор `protected`).\n6. В функции `main` продемонстрировать разницу между **статическим** (ранним) и **динамическим** (поздним) связыванием:\n   - Создать указатели базового типа `Base*`.\n   - Присвоить им адреса объектов производного типа (`Base* b = new Derived()`).\n   - Осуществить вызовы виртуальных и невиртуальных методов и объяснить разницу.\n\n## Индивидуальное задание:\n- **Базовый класс:** «Вектор на плоскости» (`Vector2D`)\n  - *Поля (protected):* координаты конца вектора x, y (начало в точке 0, 0).\n  - *Виртуальные методы:*\n    1. Вычисление длины вектора (√(x² + y²)).\n    2. Печать полей и длины вектора.\n- **Производный класс:** «Вектор в трехмерном пространстве» (`Vector3D`)\n  - *Дополнительное поле:* координата z.\n  - *Переопределение методов:*\n    1. Вычисление длины вектора (√(x² + y² + z²)).\n    2. Печать полей и длины вектора.",
  "theory": "<h2>Теория: Наследование и Полиморфизм в C++</h2>\n<p>В данной лабораторной работе изучаются два ключевых принципа ООП: наследование и полиморфизм.</p>\n<h3>1. Наследование</h3>\n<p>Наследование позволяет создать новый класс (наследник / производный) на основе уже существующего (базового родителя). Класс-наследник получает (наследует) все поля и методы базового класса и может добавлять свои собственные.</p>\n<pre><code>class RealNumber { ... };\nclass ComplexNumber : public RealNumber { ... }; </code></pre>\n<p>Слово <code class=\"inline-code\">public</code> перед названием родителя означает \"публичное наследование\". Это означает, что <code class=\"inline-code\">public</code> члены родителя остаются <code class=\"inline-code\">public</code> в наследнике, а <code class=\"inline-code\">protected</code> остаются <code class=\"inline-code\">protected</code>.</p>\n<h4>Модификатор <code class=\"inline-code\">protected</code></h4>\n<p>В ЛР1 мы использовали <code class=\"inline-code\">private</code> для сокрытия полей. Но <code class=\"inline-code\">private</code> поля недоступны даже классам-наследникам. Чтобы поле нельзя было изменить извне (<code class=\"inline-code\">main</code>), но можно было использовать внутри наследников, применяется спецификатор <code class=\"inline-code\">protected</code>. Именно поэтому поля базового класса объявляются как <code class=\"inline-code\">protected</code>.</p>\n<h4>Вызов конструктора родителя</h4>\n<p>Конструктор наследника <strong>обязан</strong> инициализировать часть объекта, которая досталась ему от базового класса. Делается это в списке инициализации:</p>\n<pre><code>ComplexNumber(double real, double imag) : RealNumber(real) {\n    imaginary = imag;\n}</code></pre>\n<h3>2. Полиморфизм</h3>\n<p>Полиморфизм — это свойство системы использовать объекты с одинаковым интерфейсом без информации о типе и внутренней структуре объекта (возможность \"функции работать по-разному\" для разных объектов).</p>\n<h4>Статический полиморфизм (Статическое связывание)</h4>\n<p>Связывание вызова функции с её телом происходит на этапе <strong>компиляции</strong> (раннее связывание).</p>\n<p>Примеры:</p>\n<p>1. Перегрузка функций (две функции с одним именем, но разными типами аргументов).</p>\n<p>2. Вызов обычного не-виртуального метода через указатель базового типа:</p>\n<p>   ```cpp</p>\n<p>   Base* ptr = &derivedObj;</p>\n<p>   ptr->Base::method(); // Всегда вызовет метод базового класса</p>\n<p>   ```</p>\n<h4>Динамический полиморфизм (Динамическое связывание)</h4>\n<p>Связывание происходит на этапе <strong>выполнения</strong> (позднее связывание).</p>\n<p>Достигается за счет использования ключевого слова <code class=\"inline-code\">virtual</code> перед методом в базовом классе.</p>\n<p>Как это работает:</p>\n<p>1. Когда компилятор видит модификатор <code class=\"inline-code\">virtual</code>, он создает специальную невидимую <strong>таблицу виртуальных функций (vtable)</strong> для этого класса.</p>\n<p>2. В объект незаметно добавляется указатель на эту таблицу (<code class=\"inline-code\">vptr</code>).</p>\n<p>3. При вызове виртуального метода через родительский указатель (<code class=\"inline-code\">ptr->print()</code>), программа сначала идет по указателю <code class=\"inline-code\">vptr</code> в таблицу и находит <strong>правильный</strong> адрес функции, который соответствует <em>реальному</em> типу объекта, лежащему в памяти.</p>\n<pre><code>Base* ptr = &amp;derivedObj; // Указатель базовый, но объект наследника\nptr-&gt;print(); // Вызовет Derived::print(), а не базовую функцию!</code></pre>\n<p>Это самая мощная архитектурная особенность С++, позволяющая писать универсальный код для обработки разных типов данных через их общий базовый интерфейс.</p>",
  "theoryRaw": "# Теория: Наследование и Полиморфизм в C++\n\nВ данной лабораторной работе изучаются два ключевых принципа ООП: наследование и полиморфизм.\n\n## 1. Наследование\nНаследование позволяет создать новый класс (наследник / производный) на основе уже существующего (базового родителя). Класс-наследник получает (наследует) все поля и методы базового класса и может добавлять свои собственные.\n\n```cpp\nclass RealNumber { ... };\nclass ComplexNumber : public RealNumber { ... }; \n```\nСлово `public` перед названием родителя означает \"публичное наследование\". Это означает, что `public` члены родителя остаются `public` в наследнике, а `protected` остаются `protected`.\n\n### Модификатор `protected`\nВ ЛР1 мы использовали `private` для сокрытия полей. Но `private` поля недоступны даже классам-наследникам. Чтобы поле нельзя было изменить извне (`main`), но можно было использовать внутри наследников, применяется спецификатор `protected`. Именно поэтому поля базового класса объявляются как `protected`.\n\n### Вызов конструктора родителя\nКонструктор наследника **обязан** инициализировать часть объекта, которая досталась ему от базового класса. Делается это в списке инициализации:\n```cpp\nComplexNumber(double real, double imag) : RealNumber(real) {\n    imaginary = imag;\n}\n```\n\n## 2. Полиморфизм\nПолиморфизм — это свойство системы использовать объекты с одинаковым интерфейсом без информации о типе и внутренней структуре объекта (возможность \"функции работать по-разному\" для разных объектов).\n\n### Статический полиморфизм (Статическое связывание)\nСвязывание вызова функции с её телом происходит на этапе **компиляции** (раннее связывание).\nПримеры:\n1. Перегрузка функций (две функции с одним именем, но разными типами аргументов).\n2. Вызов обычного не-виртуального метода через указатель базового типа:\n   ```cpp\n   Base* ptr = &derivedObj;\n   ptr->Base::method(); // Всегда вызовет метод базового класса\n   ```\n\n### Динамический полиморфизм (Динамическое связывание)\nСвязывание происходит на этапе **выполнения** (позднее связывание).\nДостигается за счет использования ключевого слова `virtual` перед методом в базовом классе.\n\nКак это работает:\n1. Когда компилятор видит модификатор `virtual`, он создает специальную невидимую **таблицу виртуальных функций (vtable)** для этого класса.\n2. В объект незаметно добавляется указатель на эту таблицу (`vptr`).\n3. При вызове виртуального метода через родительский указатель (`ptr->print()`), программа сначала идет по указателю `vptr` в таблицу и находит **правильный** адрес функции, который соответствует *реальному* типу объекта, лежащему в памяти.\n\n```cpp\nBase* ptr = &derivedObj; // Указатель базовый, но объект наследника\nptr->print(); // Вызовет Derived::print(), а не базовую функцию!\n```\nЭто самая мощная архитектурная особенность С++, позволяющая писать универсальный код для обработки разных типов данных через их общий базовый интерфейс."
}